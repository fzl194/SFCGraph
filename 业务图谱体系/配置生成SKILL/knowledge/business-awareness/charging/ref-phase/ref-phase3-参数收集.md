# Phase 3 参考文件：参数收集

## ⚠ 阶段执行约束（必须遵守）

**本文件仅用于 Phase 3。执行本文件内容前必须满足以下前置条件，否则 STOP：**

1. **用户必须已确认方案**：必须已通过 GATE-1（用户在 Phase 2 明确确认了匹配结果）。如果用户未确认方案，**STOP**，回到 Phase 2 等待用户确认。
2. **本阶段完成后必须 STOP**：执行完 §1~§4 后，**必须生成并展示 `xxxLLD.md` 后停止执行**，等待用户在 Phase 4（GATE-2）确认。`LLD` 就是 markdown 形式的规划数据表，不再拆成第二份同义文档。不得自动进入配置生成。
3. **禁止在本阶段生成任何 MML 配置命令**：本阶段只收集和推断参数，不生成配置。
4. **禁止与方案同轮输出**：如果当前轮 assistant 刚展示过 `xxx方案.md` 并请求 GATE-1 确认，则本轮不得进入本文件内容；只有用户在下一条消息中明确确认方案后，才能开始生成 `xxxLLD.md`。

---

> 本文件定义 Phase 3 的 pipeline 要求、输出模板和操作规则。
> 业务知识（参数语义、默认值、推导规则）由 Agent 从图谱和知识库动态加载。

---

## 1. Pipeline 步骤

### Step 1: 从需求和现网中推断参数

**操作**：尽可能从以下来源自动推断参数值，并把参数区分为**固定参数**和**变化参数**：
- 用户需求描述（业务名、匹配条件、计费方式等）
- 现网配置（命名规律、已有参数值）
- 业务图谱中的方案示例
- 知识库中的默认值

**分类规则**：
- **固定参数**：通常不随本次规划变化，可由现网、图谱、知识库、默认值稳定确定
- **变化参数**：随用户意图、资源规划、目标值变化，必须让用户补充或确认

**必须加载**：
- `kb/08-PFCP-N4接口与URR.md` — URRID、RG、计量方式等参数语义
- `kb/13-计费三件套配置.md` — 三件套参数推导规则
- `04-command-graph.md` — 读取 CommandParameter 定义，确认参数枚举值和约束

**推断规则**（现网命名规律）：
- 如果现网 FILTER 命名为 `filter_xxx`，则新 FILTER 建议用相同前缀
- 如果现网 URR 命名为 `urr_xxx`，则新 URR 建议用相同前缀
- 优先级参考现网已有 RULE 的 PRIORITY 分布，保持合理间距

### Step 2: 列出参数表

**操作**：对每条业务按模板展示参数，标注所有"待提供"和"待确认"项。`LLD` 就是这份 markdown 参数表。

**必须加载**：
- `04-command-graph.md` — 读取 ConfigObject 的关键参数列表，构建参数表
- `01-business-graph.md` — 读取 SemanticObject 定义，确认两侧参数差异

### Step 3: 全局参数

**操作**：列出不属于单条业务的全局参数（UserProfile、默认URR组、SMF系统级等）。

**必须加载**（当 DP-CH-02 包含 SMF 时）：
- `kb/18-SMF侧对象体系与协同约束.md` — SMF侧参数差异和协同要求

---

## 2. 输出格式模板

### 业务参数表

```markdown
### 业务 {N}: {业务名称}

| 参数项 | 参数类型 | UPF(UDG)侧 | SMF(UNC)侧 | 来源 | 状态 |
|--------|----------|-----------|-----------|------|------|
| **识别条件** |  |  |  |  |  |
| 匹配方式 | 变化参数 | {L34/L7 URL/L7协议} | — | 需求/推断 | 已知/待确认 |
| L34协议 | 变化参数 | {ANY/TCP/...} | — | 需求 | 已知/待确认 |
| L7 URL/协议 | 变化参数 | {具体值} | — | 需求 | 已知/待确认 |
| **计费参数** |  |  |  |  |  |
| 计费方式 | 固定参数 | {值} | 同UPF | DP-CH-01 | 已知 |
| URRID | 变化参数 | {数值} | **必须与UPF一致** | 需用户提供 | **待提供** |
| RG(费率组) | 变化参数 | {数值} | **必须与UPF一致** | 需用户提供 | **待提供** |
| 计量方式 | 固定参数/变化参数 | {值} | **必须与UPF一致** | 需求/推断 | 已知/待确认 |
| **命名** |  |  |  |  |  |
| FILTER名 | 固定参数 | {建议值} | — | 推断 | 已知/待确认 |
| URR名 | 固定参数 | {建议值} | {建议值} | 推断 | 已知/待确认 |
| RULE名 | 固定参数 | {建议值} | **必须与UPF一致** | 推断 | 已知/待确认 |
| **其他** |  |  |  |  |  |
| 优先级 | 变化参数 | {数值} | {数值} | 见优先级分析 | 已知/待确认 |
```

### 全局参数表

```markdown
### 全局参数

| 参数项 | 参数类型 | UPF(UDG)侧 | SMF(UNC)侧 | 来源 | 状态 |
|--------|----------|-----------|-----------|------|------|
| UserProfile名 | 固定参数 | {值} | **必须与UPF一致** | 现网/需求 | 已知/待确认 |
| 默认URR组 | 固定参数 | {值} | {值} | 现网/推断 | 已知/待确认 |
| 配额耗尽动作 | 变化参数 | {值} | — | DP-CH-04 | 已知 |
| SMF系统级参数 | 固定参数 | — | {值} | 现网/推断 | 已知/待确认 |
```

---

## 3. 优先级分析流程（必须独立执行，必须用户确认）

> **核心规则**：**数字越小，优先级越高**。不可更改。

### 步骤

1. 从现网配置中提取**所有 RULE 的 PRIORITY 值**（不只是计费 RULE，要提取全部 RULE），用 Bash grep 搜索 `PRIORITY` 参数
2. 分析现网优先级分布规律（现网数据是最权威判断依据）
3. 根据用户描述的业务优先级关系，计算新规则 PRIORITY（间距取10的倍数）
4. 输出分析表（包含现网所有 RULE 的优先级 + 新 RULE 的拟插入位置）
5. **STOP。将分析表展示给用户，等待用户确认优先级值。**

### STOP 条件

**完成分析表后必须 STOP，输出分析表并明确要求用户确认优先级。在用户确认之前：**

- **禁止**生成带 PRIORITY 的 RULE 命令
- **禁止**进入 Phase 5 配置生成
- 用户要求调整优先级 → 重新计算，再次等待确认

### 输出格式

```
## 优先级分析

**规则**：数字越小优先级越高（固定规则）

**现网 PRIORITY 分布（全部 RULE）**：
| RULE 名称 | PRIORITY | 优先级排名 | 说明 |
|-----------|----------|-----------|------|
| 现网rule1 | 21 | 第1（最高） | {业务描述} |
| 现网rule2 | 31 | 第2 | {业务描述} |
| **新rule_xxx** | **{拟提议值}** | **第N（插入位置）** | **{新业务描述}** |

**请确认新规则的优先级设置是否合理。如需调整请告知。**
```

### 禁止事项

- **禁止**自行假设优先级顺序，必须先从现网提取全部 RULE 的 PRIORITY 数据
- **禁止**只提取计费 RULE 的优先级，必须提取现网所有 RULE 的优先级分布
- **禁止**在用户未确认优先级前生成任何带 PRIORITY 的 RULE 命令

---

## 4. 注意事项

- 参数表中每个字段都应标注"来源"（需求/现网/推断/图谱/知识库）和"状态"（已知/待确认/待提供）
- 两侧共用参数（URRID、RG、USAGERPTMODE 等）必须标注"**必须与UPF一致**"
- SMF侧参数差异需要从`kb/18-SMF侧对象体系与协同约束.md`确认，不要凭记忆填写
- 遇到不确定的参数语义，加载 `04-command-graph.md` 中对应的 CommandParameter 定义确认
