server:
  host: "0.0.0.0"
  port: 8000

command_graph:
  assets_root: "../CommandGraph/data/assets"
  doc_root: ".."

feature_graph:
  assets_root: "../FeatureGraph/data"
  doc_root: ".."

task_graph:
  assets_root: "../ConfigTask/assert"
  doc_root: ".."

wiki:
  assets_root: "../assets"
