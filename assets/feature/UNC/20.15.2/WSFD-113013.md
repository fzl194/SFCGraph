---
id: UNC@20.15.2@Feature@WSFD-113013
type: Feature
name: NRF全故障业务逃生(Bypass)
nf: UNC
version: 20.15.2
feature_code: WSFD-113013
is_directory: false
catalog_section: 自治网络解决方案基本包
parent_feature_code: WSFD-114000
applicable_nf:
- AMF
- SMF
feature_category: operations
config_relevance: required
first_release_version: 20.10.0
status: active
---

# NRF全故障业务逃生(Bypass)

`WSFD-113013` · operations · required

## 所属目录

- [WSFD-114000](feature/UNC/20.15.2/WSFD-114000.md)

## 适用性

- 适用NF：AMF、SMF
- 代际支持矩阵：`SGSN(2.5&3G)=-; GGSN-C(2.5&3G)=-; MME(4G)=-; S/PGW-C(4G)=-; NB-IoT(4G)=-; 5G MME(5G NSA)=-; 5G S/PGW-C(5G NSA)=M; AMF(5G SA)=M; SMF(5G SA)=M; NRF(5G SA)=-`

## 定义

NRF全故障业务逃生是指NF将首次通过NRF选择的符合条件的NF/NFS进行本地缓存，待下次进行相同NF/NFS请求时，NF可以直接使用缓存的NF/NFS信息，以减少访问NRF的次数。

## 客户价值

| **受益方** | **受益描述** |
| --- | --- |
| 运营商 | - 提升NF发现的性能。<br>- 降低服务发现的时延，相比从NRF发现更快。 |
| 用户 | 用户不感知该特性。 |

## 应用场景

5G网络中的NF数量较多，业务对应的网元会进行频繁的选择，对NRF的冲击和NF的信令消耗都会比较大，此时可以使用NF缓存，减少访问NRF的次数。

## 可获得性

**涉及NF**

| **涉及NF** | **支持版本** | **功能说明** |
| --- | --- | --- |
| AMF | UNC 20.0.0及后续版本 | 支持向NRF发起的NF状态订阅和服务发现流程。 |
| SMF | UNC 20.0.0及后续版本 | 支持向NRF发起的NF状态订阅和服务发现流程。 |
| NSSF | UNC 20.0.0及后续版本 | 支持向NRF发起的NF状态订阅和服务发现流程。 |
| SMSF | UNC 20.0.0及后续版本 | 支持向NRF发起的NF状态订阅和服务发现流程。 |

**License** **支持**

本特性无需获得License许可即可获得该特性的服务。

## 与其他特性的交互（原文）

本特性不涉及与其他特性的交互。

## 对系统的影响

本特性对系统无影响。

## 应用限制

本特性无应用限制。

## 原理概述

NRF全故障业务逃生包含缓存功能使用、缓存数据刷新和缓存数据老化三个功能点：

- 缓存功能使用
  缓存功能有开关控制。
- 缓存数据刷新
  请求方NF需要订阅缓存的目标NF的属性变化，当目标NF属性变化通知到请求方NF时，NF缓存中的数据会对应刷新成最新。
- 缓存数据老化
  缓存记录具有一定的生命周期，生命周期结束后，缓存中数据将被删除，后续相同的NF/NFS发现需要再次向NRF发起服务发现请求。但是，如果NF侧识别到NRF故障后，会停止所有缓存数据老化，直到NRF业务恢复。系统可以通过周期性更新和老化前的提前更新配合，对缓存中的数据进行扫描，扫描后自动到NRF上获取此NF的数据刷新到缓存中，避免缓存中的NF老化被删除。

## 特性规格

本特性无特殊规格。

## 遵循标准

| **标准类别** | **标准编号** | **标准名称** |
| --- | --- | --- |
| 3GPP | 29.510 | Technical Specification Group Core Network and Terminals;5G System;Network Function Repository Services; |

## 发布历史

| **特性版本** | **发布版本** | **发布说明** |
| --- | --- | --- |
| 01 | 20.10.0 | 首次发布。 |

## 证据

- 原始手册：`evidence/UNC/20.15.2/WSFD-113013-NRF全故障业务逃生特性概述_87391658.md`
- 原始手册：`evidence/UNC/20.15.2/激活NRF全故障业务逃生_38597581.md`
- 原始手册：`evidence/UNC/20.15.2/调测NRF全故障业务逃生_88157592.md`
