---
id: UDG@20.15.2@Feature@IPFD-012002
type: Feature
name: ACL
nf: UDG
version: 20.15.2
feature_code: IPFD-012002
is_directory: false
catalog_section: IP基本特性
parent_feature_code: IPFD-012000
feature_category: base
config_relevance: required
first_release_version: 20.0.0
status: active
---

# ACL

`IPFD-012002` · base · required

## 所属目录

- [IPFD-012000](feature/UDG/20.15.2/IPFD-012000.md)

## 适用性

- 代际支持矩阵：`GGSN(2G&3G)=M; S/PGW-U(4G)=M; S/PGW-U(5G NSA)=M; UPF(5G)=M; NB-IoT=M`

## 定义

ACL（Access Control List，访问控制列表）是一系列有顺序的包含“允许”或者“拒绝”规则的集合，这些规则通过报文的源地址、目的地址、端口号等特征来描述，可以实现对报文的分类。

> **说明**
> 对于ACL中同时支持IPv4和IPv6的特性，正文中如果不做特殊说明，则表示二者实现无差异。

## 客户价值

| 受益方 | 受益描述 |
| --- | --- |
| 运营商 | ACL通过规则对报文进行分类，这些规则应用到设备上，设备根据这些规则判断哪些报文可以接收，哪些报文需要拒绝，从而极大地提升了网络的安全性。 |
| 用户 | ACL提升了网络的安全性，可以给用户带来更好的用网体验。 |

## 应用场景

网络中的设备相互通信时，需要保障网络传输的安全可靠和性能稳定。例如：

- 在设备管理的应用中，为了提升其安全性，防止遭受恶意攻击，网络管理员需要限制只允许满足符合一定条件的客户访问设备。
- 在策略路由的应用中，网络管理员需要对符合条件的数据报文执行相应的策略路由动作，比如设置报文的转发优先级、出接口、下一跳和VPN实例，使得指定的数据报文可以灵活地按照策略路由转发，从而实现提供有差别的服务的目的。
- 在路由策略的应用中，网络管理员有时候需要设置某种控制方式来改变路由产生、发布和选择的结果，从而达到网络流量所经过的路径的目的。
- 在QoS的应用中，网络管理员需要适时限制网络流量和提高网络性能，例如限定网络上行、下行流量的带宽，对用户申请的带宽进行收费，保证高带宽网络资源的充分利用。

针对上述场景，可以通过配置ACL，定义一组匹配规则，实现：

- 允许满足匹配规则的用户访问设备，不满足规则的用户拒绝其访问设备。
- 对满足符合匹配规则的数据报文进行相应的策略路由动作，按照灵活的策略路由转发，对不满足规则的数据报文单纯依照目的地址查找路由表进行转发。
- 允许设备只接收或发布满足匹配规则的路由信息，不满足规则的路由信息将不被接收或者发布。
- 对满足匹配规则的流量进行流量监管、流量整形、流量分类，对不满足规则的流量不作处理。

## 可获得性

**版本支持**

| 产品 | 支持版本 |
| --- | --- |
| UDG | 20.0.0<br>及后续版本 |

**License支持**

本特性无需获得License许可即可获得该特性的服务。

## 与其他特性的交互（原文）

| 相关特性 | 交互关系 |
| --- | --- |
| 设备管理 | ACL本身只是一组规则的集合，它只是通过规则对报文进行了分类。因此ACL需要与具体业务特性配合使用，才能实现过滤报文的功能。 |
| 策略路由 | ACL本身只是一组规则的集合，它只是通过规则对报文进行了分类。因此ACL需要与具体业务特性配合使用，才能实现过滤报文的功能。 |
| 路由策略 | ACL本身只是一组规则的集合，它只是通过规则对报文进行了分类。因此ACL需要与具体业务特性配合使用，才能实现过滤报文的功能。 |
| QoS | ACL本身只是一组规则的集合，它只是通过规则对报文进行了分类。因此ACL需要与具体业务特性配合使用，才能实现过滤报文的功能。 |

## 对系统的影响

当一个ACL中规则非常多时，寻找到相应匹配规则的时间耗时变长，导致转发性能下降。

## 应用限制

本特性本身不涉及应用限制，但是需要与具体特性配合之后才能生效。

## 原理概述

ACL实现的核心在于规则的制定和规则匹配的实现。用户在定义规则时，可以根据报文的源或目的地址、协议类型、源或目的端口号等属性描述规则，同时可以指定拒绝或接收报文。将这些规则应用到设备上，设备将根据规则对到达本设备的报文进行分类，并对符合条件的报文进行相应的处理。

如需详细了解ACL特性，请按顺序阅读如下章节：

- [ACL分类](实现原理/ACL分类_61317395.md)
- [ACL规则匹配过程](实现原理/ACL规则匹配过程_61317303.md)

## 计费与话单

本特性不涉及计费与话单。

## 特性规格

| 规格名称 | 规格指标 |
| --- | --- |
| 每个ACL支持的规则的规格 | 2K |

## 遵循标准

| 标准类别 | 标准名称 |
| --- | --- |
| RFC 4314 | IMAP4 Access Control List (ACL) Extension |

## 发布历史

| 特性版本 | 发布版本 | 发布说明 |
| --- | --- | --- |
| 01 | 20.0.0 | 首次发布。 |

## 特性关系

**交互（interacts_with）**

- [IPFD-012001](feature/UDG/20.15.2/IPFD-012001.md)：ACL防火墙功能：当用户认为某类报文不可信时，可以通过QoS引用ACL将这类报文与其他报文区别出来并进行丢弃；同样的，当用户认为某类报文可信时，也可以通过QoS引用ACL将这类报文与其他报文区别出来并允许通过。
- [IPFD-014005](feature/UDG/20.15.2/IPFD-014005.md)：ACL本身只是一组规则的集合，它只是通过规则对报文进行了分类。因此ACL需要与具体业务特性配合使用，才能实现过滤报文的功能。
- [IPFD-014006](feature/UDG/20.15.2/IPFD-014006.md)：ACL本身只是一组规则的集合，它只是通过规则对报文进行了分类。因此ACL需要与具体业务特性配合使用，才能实现过滤报文的功能。
- [NPFD-010004](feature/UDG/20.15.2/NPFD-010004.md)：ACL本身只是一组规则的集合，它只是通过规则对报文进行了分类。因此ACL需要与具体业务特性配合使用，才能实现过滤报文的功能。

## 证据

- 原始手册：`evidence/UDG/20.15.2/IPFD-012002-ACL特性概述_61317258.md`
- 原始手册：`evidence/UDG/20.15.2/ACL分类_61317395.md`
- 原始手册：`evidence/UDG/20.15.2/ACL规则匹配过程_61317303.md`
- 原始手册：`evidence/UDG/20.15.2/去激活支持ACL6_06381765.md`
- 原始手册：`evidence/UDG/20.15.2/去激活支持ACL_06381762.md`
- 原始手册：`evidence/UDG/20.15.2/激活支持ACL6_06381763.md`
- 原始手册：`evidence/UDG/20.15.2/激活支持ACL_06381760.md`
- 原始手册：`evidence/UDG/20.15.2/调测ACL6_06381764.md`
- 原始手册：`evidence/UDG/20.15.2/调测ACL_06381761.md`
