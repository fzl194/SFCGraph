# 查询NF实例路由（LST NRFINSTRT）

- [命令功能](#ZH-CN_MMLREF_0209651348__1.3.1)
- [注意事项](#ZH-CN_MMLREF_0209651348__1.3.2)
- [参数说明](#ZH-CN_MMLREF_0209651348__1.3.4)
- [使用实例](#ZH-CN_MMLREF_0209651348__1.3.5)
- [输出结果说明](#ZH-CN_MMLREF_0209651348__1.3.6)

## [命令功能](#ZH-CN_MMLREF_0209651348)

**适用NF：NRF**

该命令用于查询已配置的NF实例标识路由信息。

若要查询全部NF实例标识的路由信息，请不要输入任何参数。

若要查询某个NF实例标识的路由信息，请输入“NF实例标识匹配值”和其他参数的组合。

## [注意事项](#ZH-CN_MMLREF_0209651348)

无

#### [操作用户权限](#ZH-CN_MMLREF_0209651348)

G_1，管理员级别命令组；G_2，操作员级别命令组；G_3，用户级别命令组

## [参数说明](#ZH-CN_MMLREF_0209651348)

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| START | 条件匹配起始位置 | 可选必选说明：可选参数<br>参数含义：该参数表示条件匹配的NF实例标识起始位置。<br>数据来源：全网规划<br>取值范围：整数类型，取值范围是0~39。<br>默认值：无<br>配置原则：无 |
| LENGTH | 条件匹配长度 | 可选必选说明：可选参数<br>参数含义：该参数表示NF实例标识匹配值的长度。<br>数据来源：全网规划<br>取值范围：整数类型，取值范围是0~40。<br>默认值：无<br>配置原则：无 |
| NFINSTIDMATCHINGVALUE | NF实例标识匹配值 | 可选必选说明：可选参数<br>参数含义：该参数用于表示NF实例标识匹配值。<br>数据来源：全网规划<br>取值范围：字符串类型，输入长度范围是1~40。该参数只能由字母（A-Z或者a-z）、数字（0-9）、中划线（-）组成，不区分大小写。<br>默认值：无<br>配置原则：<br>“条件匹配长度”为非0时，此匹配值的字符串长度等于“条件匹配长度”的值。 |
| NEXTNRFGRPNAME | 归属NRF组名称 | 可选必选说明：可选参数<br>参数含义：该参数用于表示当前NRF基于NF实例路由寻址NF时的下一跳NRF实例组名称，被寻址的NF归属于该NRF实例组。<br>数据来源：全网规划<br>取值范围：字符串类型，输入长度范围是0~64。<br>默认值：无<br>配置原则：<br>该参数已通过ADD NRFGROUP配置，可通过LST NRFGROUP命令查询获取。 |

## [使用实例](#ZH-CN_MMLREF_0209651348)

- 查询全部NF实例标识的路由信息：
  ```
  LST NRFINSTRT:;
  %%LST NRFINSTRT:;%%
  RETCODE = 0  操作成功

  结果如下
  ------------------------
  条件匹配起始位置  条件匹配长度  NF实例标识匹配值                       归属NRF组名称

  0                 0             123e4567-e89b-12d3-a456-426655440000   L-NRF1
  0                 8             123e4567                               L-NRF1
  3                 5             e4567                                  L-NRF1
  (结果个数 = 3)
  ```
- 查询条件匹配起始位置为0，条件匹配长度为8，NF实例标识匹配值为123e4567的NF实例标识的路由信息：
  ```
  LST NRFINSTRT:START=0,LENGTH=8,NFINSTIDMATCHINGVALUE="123e4567";
  %%LST NRFINSTRT:START=0,LENGTH=8,NFINSTIDMATCHINGVALUE="123e4567";%%
  RETCODE = 0  操作成功

  结果如下
  ------------------------
  条件匹配起始位置 =  0
      条件匹配长度 =  8
  NF实例标识匹配值 =  123e4567
     归属NRF组名称 =  L-NRF1
  (结果个数 = 1)
  ```

## [输出结果说明](#ZH-CN_MMLREF_0209651348)

| 输出项名称 | 输出项解释 |
| --- | --- |
| 条件匹配起始位置 | 该参数表示条件匹配的NF实例标识起始位置。 |
| 条件匹配长度 | 该参数表示NF实例标识匹配值的长度。 |
| NF实例标识匹配值 | 该参数用于表示NF实例标识匹配值。 |
| 归属NRF组名称 | 该参数用于表示当前NRF基于NF实例路由寻址NF时的下一跳NRF实例组名称，被寻址的NF归属于该NRF实例组。 |
