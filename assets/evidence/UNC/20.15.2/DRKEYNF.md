# 设置监控的关键部件信息（SET DRKEYNF）

- [命令功能](#ZH-CN_MMLREF_0000001374835157__1.3.1)
- [注意事项](#ZH-CN_MMLREF_0000001374835157__1.3.2)
- [参数说明](#ZH-CN_MMLREF_0000001374835157__1.3.4)
- [使用实例](#ZH-CN_MMLREF_0000001374835157__1.3.5)

## [命令功能](#ZH-CN_MMLREF_0000001374835157)

该命令用于设置监控的关键部件信息。

## [注意事项](#ZH-CN_MMLREF_0000001374835157)

- 该命令执行后立即生效。

- 该命令只用于在UEG-L/UEN网元采用主备（冷备）容灾模式下执行。
- 该命令只用于在UEG-M/UEG-L/UEG采用负荷分担容灾模式下执行。
- 该命令只用于在UEG-M/UEG网元采用主备（热备）容灾模式下执行。

- 系统部署完成后，已经存在初始记录，参数的初始记录值如下表：

| MME | AMF | HSS | UDM | SMF | GWC | PCF | PCRF | UPF | UDR |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| NO | NO | NO | NO | NO | NO | NO | NO | NO | NO |

#### [操作用户权限](#ZH-CN_MMLREF_0000001374835157)

G_1，管理员级别命令组；G_2，操作员级别命令组

## [参数说明](#ZH-CN_MMLREF_0000001374835157)

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| MME | 是否监控MME服务 | 可选必选说明：可选参数<br>参数含义：设置是否监控MME服务。<br>数据来源：本端规划<br>取值范围：<br>- YES（是）<br>- NO（否）<br>默认值：NO。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRKEYNF查询当前参数配置值。<br>配置原则：无 |
| AMF | 是否监控AMF服务 | 可选必选说明：可选参数<br>参数含义：设置是否监控AMF服务。<br>数据来源：本端规划<br>取值范围：<br>- YES（是）<br>- NO（否）<br>默认值：NO。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRKEYNF查询当前参数配置值。<br>配置原则：无 |
| HSS | 是否监控HSS服务 | 可选必选说明：可选参数<br>参数含义：设置是否监控HSS服务。<br>数据来源：本端规划<br>取值范围：<br>- YES（是）<br>- NO（否）<br>默认值：NO。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRKEYNF查询当前参数配置值。<br>配置原则：无 |
| UDM | 是否监控UDM服务 | 可选必选说明：可选参数<br>参数含义：设置是否监控UDM服务。<br>数据来源：本端规划<br>取值范围：<br>- YES（是）<br>- NO（否）<br>默认值：NO。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRKEYNF查询当前参数配置值。<br>配置原则：无 |
| SMF | 是否监控SMF服务 | 可选必选说明：可选参数<br>参数含义：设置是否监控SMF服务。<br>数据来源：本端规划<br>取值范围：<br>- YES（是）<br>- NO（否）<br>默认值：NO。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRKEYNF查询当前参数配置值。<br>配置原则：无 |
| GWC | 是否监控GW-C服务 | 可选必选说明：可选参数<br>参数含义：设置是否监控GW-C服务。<br>数据来源：本端规划<br>取值范围：<br>- YES（是）<br>- NO（否）<br>默认值：NO。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRKEYNF查询当前参数配置值。<br>配置原则：无 |
| PCF | 是否监控PCF服务 | 可选必选说明：可选参数<br>参数含义：设置是否监控PCF服务。<br>数据来源：本端规划<br>取值范围：<br>- YES（是）<br>- NO（否）<br>默认值：NO。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRKEYNF查询当前参数配置值。<br>配置原则：无 |
| PCRF | 是否监控PCRF服务 | 可选必选说明：可选参数<br>参数含义：设置是否监控PCRF服务。<br>数据来源：本端规划<br>取值范围：<br>- YES（是）<br>- NO（否）<br>默认值：NO。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRKEYNF查询当前参数配置值。<br>配置原则：无 |
| UPF | 是否监控UPF服务 | 可选必选说明：可选参数<br>参数含义：设置是否监控UPF服务。<br>数据来源：本端规划<br>取值范围：<br>- YES（是）<br>- NO（否）<br>默认值：NO。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRKEYNF查询当前参数配置值。<br>配置原则：无 |
| UDR | 是否监控UDR服务 | 可选必选说明：可选参数<br>参数含义：设置是否监控UDR服务。<br>数据来源：本端规划<br>取值范围：<br>- YES（是）<br>- NO（否）<br>默认值：NO。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRKEYNF查询当前参数配置值。<br>配置原则：无 |

## [使用实例](#ZH-CN_MMLREF_0000001374835157)

设置监控MME服务：

```
%%SET DRKEYNF: MME=YES, AMF=NO, HSS=NO, UDM=NO, SMF=NO, GWC=NO, PCF=NO, PCRF=NO, UPF=NO, UDR=NO;%%
RETCODE = 0  操作成功

---    END
```
