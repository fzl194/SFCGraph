# 查询存储检测参数（DSP DISKCHKPARA）

- [命令功能](#ZH-CN_TOPIC_0280543422__1.3.1.1)
- [注意事项](#ZH-CN_TOPIC_0280543422__1.3.3.1)
- [参数说明](#ZH-CN_TOPIC_0280543422__1.3.4.1)
- [使用实例](#ZH-CN_TOPIC_0280543422__1.3.5.1)
- [输出结果说明](#ZH-CN_TOPIC_0280543422__1.3.6.1)

#### [命令功能](#ZH-CN_TOPIC_0280543422)

本命令用于查询节点当前的磁盘检测参数信息。

- 若需要查询所有节点的磁盘检测参数，需要输入“网元ID”并选择“查询存储检测类型”。
- 若需要查询指定节点的磁盘检测参数，需要输入“网元ID”和“节点名称”，并选择“查询存储检测类型”。

> **说明**
> 该命令仅在Full-stack虚机场景下支持。

#### [注意事项](#ZH-CN_TOPIC_0280543422)

无。

#### [参数说明](#ZH-CN_TOPIC_0280543422)

| **参数标识** | **参数名称** | **参数说明** |
| --- | --- | --- |
| APPID | 网元ID | 可选必选说明：可选参数<br>参数含义：用于指示系统需要查询的网元ID。<br>取值范围：0～65535。<br>默认值：无。<br>配置原则：<br>“网元ID”<br>获取方式为：点击<br>“首页”<br>，查看<br>“应用ID”<br>即可。 |
| TYPE | 查询存储检测类型 | 可选必选说明：必选参数<br>参数含义：用于指示系统需要查询指定网元ID下的节点存储检测参数类型。<br>取值范围：<br>- “DiskFault(存储故障)”：节点存储故障的检测参数信息。<br>- “DiskSubHealth(存储亚健康)”：节点存储亚健康的检测参数信息。<br>默认值：无。<br>配置原则：无。 |
| NODENAME | 节点名称 | 可选必选说明：可选参数<br>参数含义：用于指示系统需要查询哪个节点的存储检测参数。<br>取值范围：不超过128位字符串。<br>默认值：无。<br>配置原则：<br>- 若不输入，则表示查询所有节点的信息。<br>- 用户可以通过[**DSP NODE**](节点查询（DSP NODE）_71678755.md)命令查看相应的节点名称。 |

#### [使用实例](#ZH-CN_TOPIC_0280543422)

1. 查询“网元ID”为“0”的所有节点“存储故障检测”参数信息：
  ```
  %%DSP DISKCHKPARA: TYPE=DiskFault, APPID=0;%%
  RETCODE = 0  操作成功

  操作结果如下
  ------------
  网元ID  节点IP    节点名称   查询类型  检测开关 存储异常检测周期(个) 存储异常检测阈值（%） IO时延（ms）  dd读写速率（KB/s）  
  0       10.0.0.1  10.0.0.1   存储故障   开启      19                    20                   200            20              
  0       10.0.0.2  10.0.0.2   存储故障   开启      19                    20                   200            20
  0       10.0.0.3  10.0.0.3   存储故障   开启      19                    20                   200            20
                 
  (结果个数 = 3)

  ---    END
  ```
2. 查询“网元ID”为“0”，“节点名称”为“10.0.0.1”的“存储亚健康检测”参数信息：
  ```
  %%DSP DISKCHKPARA: TYPE=DiskSubHealth, APPID=0, NODENAME="10.0.0.1";%%
  RETCODE = 0  操作成功

  操作结果如下
  ------------
                   网元ID  =  0
                   节点IP  =  10.0.0.1
                 节点名称  =  10.0.0.1
                 查询类型  =  存储亚健康
                 检测开关  =  关闭
     存储异常检测周期(个)  =  17
    存储异常检测阈值（%）  =  50
             IO时延（ms）  =  200
       dd读写速率（KB/s）  =  20
  (结果个数 = 1)

  ---    END
  ```

#### [输出结果说明](#ZH-CN_TOPIC_0280543422)

命令执行正常，会返回命令执行成功的提示信息。输出结果说明如 [表1](#ZH-CN_TOPIC_0280543422__table819976173217) 所示。

命令执行异常，会返回提示对应的失败信息。错误码列表如 [表2](#ZH-CN_TOPIC_0280543422__table698716490471) 所示。

*表1 输出项说明*

| 输出项名称 | 输出项解释 |
| --- | --- |
| 网元ID | 用于返回查询网元的ID。 |
| 节点IP | 用于返回查询节点的IP地址。 |
| 节点名称 | 用于返回查询的节点名称。 |
| 查询类型 | 用于返回本节点查询的存储检测类型。<br>取值范围：<br>- “存储故障”：对节点进行存储故障检测。<br>- “存储亚健康”：对节点进行存储亚健康检测。 |
| 检测开关 | 用于返回查询的节点的存储检测开关状态。<br>取值范围：<br>- “开启”：用于返回当前查询的节点的存储检测开关状态为打开状态。<br>- “关闭”：用于返回当前查询的节点的存储检测开关状态为关闭状态。 |
| 存储异常检测周期（个） | 用于返回查询的节点的存储检测周期。 |
| 存储异常检测阈值（%） | 用于返回查询的节点的存储检测阈值。 |
| IO时延（ms） | 用于返回查询的节点的存储检测IO时延的阈值。 |
| dd读写速率（KB/s） | 用于返回查询的节点的存储检测的dd读写速率的阈值。 |

*表2 错误码列表*

| 错误码 | 错误码解释 | 原因分析 | 处理建议 |
| --- | --- | --- | --- |
| 130001 | 没有符合条件的节点 | 所要查询的节点或者网元ID不存在，无相应存储检测参数信息。 | 按照<br>[**DSP NODE**](节点查询（DSP NODE）_71678755.md)<br>命令查询的所有节点信息，选择确实存在的节点，查询其相应的存储检测类型对应的检测参数状态。 |
| 130002 | 操作失败 | 内部错误。 | 请联系<br>华为<br>技术支持。 |
| 130004 | 部分成功 | 部分资源信息查询失败。 | 请联系<br>华为<br>技术支持。 |
| 130009 | 输入参数不合法 | 设置的参数值不在规定范围之内。 | 按照规定的范围进行参数值的设置。 |
| 130060 | 访问ETCD失败 | ETCD故障导致查询数据库失败。 | 请联系<br>华为<br>技术支持。 |
