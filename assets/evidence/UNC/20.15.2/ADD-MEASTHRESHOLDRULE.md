# 添加话统阈值规则(ADD MEASTHRESHOLDRULE)

- [命令功能](#ZH-CN_MMLREF_0000001875942549__1.3.1)
- [注意事项](#ZH-CN_MMLREF_0000001875942549__1.3.2)
- [参数说明](#ZH-CN_MMLREF_0000001875942549__1.3.3)
- [使用实例](#ZH-CN_MMLREF_0000001875942549__1.3.4)
- [输出结果说明](#ZH-CN_MMLREF_0000001875942549__1.3.5)

## [命令功能](#ZH-CN_MMLREF_0000001875942549)

该命令用于添加话统阈值规则，且支持配置导出。

## [注意事项](#ZH-CN_MMLREF_0000001875942549)

1. 当“对象实例ID”和“测量指标ID”不存在相应周期的实时监控任务时，操作成功后会返回“ 操作成功，监控任务未开启 ”。
2. 整系统最多支持配置1000条话统阈值规则。
3. 至少需要填写一组告警阈值和告警偏移信息。
4. 当网元类型变更时，会清除当前未激活的“测量指标ID”对应的话统阈值规则。
5. 满足阈值规则条件会上报“ALM-136802 实时统计紧急阈值超限”、“ALM-136803 实时统计重要阈值超限”、“ALM-136804 实时统计次要阈值超限”或“ALM-136805 实时统计提示阈值超限”告警。
6. 英文环境设置阈值规则名称为中文时，在网管上显示的对应告警会出现乱码，在OM Portal界面显示正常。

## [参数说明](#ZH-CN_MMLREF_0000001875942549)

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| MEID | 网元ID | 可选必选说明：必选参数。<br>参数含义：标识网元ID，可以通过<br>[**LST ME**](../../系统管理/版本信息/查询网元配置信息（LST ME）_47084797.md)<br>命令查询获取。<br>取值范围：0~65535。<br>默认值：无。<br>配置原则：无。 |
| TRN | 阈值规则名称 | 可选必选说明：必选参数。<br>参数含义：话统阈值规则名称。<br>取值范围：字符串类型，长度不超过160个字符。<br>默认值：无。<br>配置原则：仅支持“A-Z”、“a-z”、“0-9”、“-_”、“\u4e00-\u9fa5”中文字符组合。 |
| TRT | 阈值规则类型 | 可选必选说明：必选参数。<br>参数含义：话统阈值规则类型。<br>取值范围：SIMPLE(简单阈值)<br>默认值：无。<br>配置原则：无。 |
| PRD | 测量周期 | 可选必选说明：必选参数。<br>参数含义：标识指标数据的周期。<br>取值范围：<br>- FIVE_SECOND(5秒)<br>- ONE_MINUTE(1分钟)<br>默认值：无。<br>配置原则：无。 |
| SDT | 激活开始时间 | 可选必选说明：可选参数。<br>参数含义：每天话统阈值校验的开始时间。<br>取值范围：时分，规则为HH:MM。<br>默认值：客户端当前时间。<br>配置原则：若不输入，则把00:00当做激活开始时间。 |
| EDT | 激活结束时间 | 可选必选说明：可选参数。<br>参数含义：每天话统阈值校验的结束时间。<br>取值范围：时分，规则为HH:MM。<br>默认值：客户端当前时间。<br>配置原则：若不输入，则把23:59当做激活结束时间。 |
| MOIID | 对象实例ID | 可选必选说明：必选参数。<br>参数含义：对象实例ID，可以通过<br>[**DSP MEASOBJ**](查询话统测量对象实例(DSP MEASOBJ)_01094728.md)<br>命令查询获取。<br>取值范围：字符串类型，长度不超过256个字符。<br>默认值：无。<br>配置原则：无。 |
| METRICID | 测量指标ID | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：测量指标ID，可以通过<br>[**LST MEASUNIT**](查询测量指标模型(LST MEASUNIT)_47814449.md)<br>命令查询获取。<br>取值范围：0~4294967294的整数。<br>默认值：无。<br>配置原则：无。 |
| DIR | 趋势 | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：标识阈值检测的指标值趋势。<br>取值范围：<br>- UP(上升)<br>- DOWN(下降)<br>默认值：无。<br>配置原则：无。 |
| CTC | 连续触发次数 | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：标识告警上报的阈值连续触发次数。<br>取值范围：1~10的整数。<br>默认值：无。<br>配置原则：无。 |
| CRC | 连续恢复次数 | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：标识告警清除的阈值连续恢复次数。<br>取值范围：1~10的整数。<br>默认值：无。<br>配置原则：无。 |
| CV | 紧急告警阈值 | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：紧急告警阈值。<br>取值范围：字符串类型，长度不超过256个字节。<br>默认值：无。<br>配置原则：请填写浮点数，配置值过长会有精度损失。 |
| CO | 紧急告警偏移 | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：紧急告警偏移。<br>当<br>“趋势”<br>为上升时，告警触发条件为指标值大于等于阈值+偏移，告警恢复条件为指标值小于阈值-偏移，<br>当<br>“趋势”<br>为下降时，告警触发条件为指标值小于等于阈值- 偏移，告警恢复条件为指标值大于阈值+偏移。<br>取值范围：字符串类型，长度不超过256个字符。<br>默认值：无。<br>配置原则：请填写浮点数，配置值过长会有精度损失，不支持负数。 |
| MAV | 重要告警阈值 | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：重要告警阈值。<br>取值范围：字符串类型，长度不超过256个字符。<br>默认值：无。<br>配置原则：请填写浮点数，配置值过长会有精度损失。 |
| MAO | 重要告警偏移 | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：重要告警偏移。<br>当<br>“趋势”<br>为上升时，告警触发条件为指标值大于等于阈值+偏移，告警恢复条件为指标值小于阈值-偏移，<br>当<br>“趋势”<br>为下降时，告警触发条件为指标值小于等于阈值- 偏移，告警恢复条件为指标值大于阈值+偏移。<br>取值范围：字符串类型，长度不超过256个字符。<br>默认值：无。<br>配置原则：请填写浮点数，配置值过长会有精度损失，不支持负数。 |
| MIV | 次要告警阈值 | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：次要告警阈值。<br>取值范围：字符串类型，长度不超过256个字符。<br>默认值：无。<br>配置原则：请填写浮点数，配置值过长会有精度损失。 |
| MIO | 次要告警偏移 | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：次要告警偏移。<br>当<br>“趋势”<br>为上升时，告警触发条件为指标值大于等于阈值+偏移，告警恢复条件为指标值小于阈值-偏移，<br>当<br>“趋势”<br>为下降时，告警触发条件为指标值小于等于阈值- 偏移，告警恢复条件为指标值大于阈值+偏移。<br>取值范围：字符串类型，长度不超过256个字符。<br>默认值：无。<br>配置原则：请填写浮点数，配置值过长会有精度损失，不支持负数。 |
| WV | 提示告警阈值 | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：提示告警阈值。<br>取值范围：字符串类型，长度不超过256个字符。<br>默认值：无。<br>配置原则：请填写浮点数，配置值过长会有精度损失。 |
| WO | 提示告警偏移 | 可选必选说明：当<br>“TRT”<br>类型为<br>“SIMPLE(简单阈值)”<br>时为条件必选参数。<br>参数含义：提示告警偏移。<br>当<br>“趋势”<br>为上升时，告警触发条件为指标值大于等于阈值+偏移，告警恢复条件为指标值小于阈值-偏移，<br>当<br>“趋势”<br>为下降时，告警触发条件为指标值小于等于阈值- 偏移，告警恢复条件为指标值大于阈值+偏移。<br>取值范围：字符串类型，长度不超过256个字符。<br>默认值：无。<br>配置原则：请填写浮点数，配置值过长会有精度损失，不支持负数。 |

## [使用实例](#ZH-CN_MMLREF_0000001875942549)

添加话统阈值规则：

```
%%ADD MEASTHRESHOLDRULE: MEID=20, TRN="ttt", TRT=SIMPLE, PRD=ONE_MINUTE, MOIID="moc122", METRICID=24100599, DIR=UP, CTC=1, CRC=1, CV="60";%%
RETCODE = 0  操作成功 
---    END
```

## [输出结果说明](#ZH-CN_MMLREF_0000001875942549)

命令执行正常，会返回命令执行成功的提示信息，无输出项。

命令执行异常，请联系华为技术支持处理。输出错误码说明如 [表1](#ZH-CN_MMLREF_0000001875942549__table16741512233) 所示。

*表1 错误码列表*

| 错误码 | 错误码解释 | 原因分析 | 处理建议 |
| --- | --- | --- | --- |
| 100047 | 参数的值超过上限 | 填写的参数超过MML长度的限制。 | 更正后重试。 |
| 650007 | 请求消息错误 | 请求消息错误。 | 核对更正参数后重试。 |
| 650115 | 服务初始化中 | 服务初始化中。 | 服务初始化中，请稍后重试。 |
| 650121 | 内部处理错误 | 未预期的内部处理错误。 | 请联系华为技术支持。 |
| 650125 | 网元不存在 | 网元ID输入非法，与实际环境不一致。 | 请通过<br>[**LST ME**](../../系统管理/版本信息/查询网元配置信息（LST ME）_47084797.md)<br>命令查询获取已存在的网元ID。 |
| 650127 | 测量指标不存在 | 测量指标ID输入非法，与当前模型不一致。 | 更正后重试。 |
| 650139 | 阈值规则已存在 | 当前阈值规则已经存在。 | 更换阈值规则名称后重试。 |
| 650141 | 触发或恢复次数与检测时间不匹配 | 检测阈值告警的时间不满足触发或恢复次数的时长要求。 | 更正后重试。 |
| 650142 | 告警偏移不能为负数 | 告警偏移填写为负数。 | 更正后重试。 |
| 650143 | 某级告警阈值信息只填写了告警偏移，未填写告警阈值 | 某级告警阈值信息只填写了告警偏移，未填写告警阈值。 | 更正后重试。 |
| 650144 | 请至少填写一组告警阈值信息 | 没有填写任何告警阈值信息。 | 更正后重试。 |
| 650145 | 告警阈值区间存在重叠或顺序与趋势不符 | 各级告警阈值和告警偏移组成的区间存在重叠或与填写的检测趋势不符。 | 更正后重试。 |
| 650146 | 整系统话统阈值规则个数超过上限 | 阈值规则定义超过整系统规格上限1000个。 | 使用<br>[**RMV MEASTHRESHOLDRULE**](删除话统阈值规则(RMV MEASTHRESHOLDRULE)_29103316.md)<br>按需删除部分话统阈值规则后，再次重试。 |
