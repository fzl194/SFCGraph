# POD复位（RST POD）

- [名词解释](#ZH-CN_TOPIC_0269830278__1.3.1.1)
- [命令功能](#ZH-CN_TOPIC_0269830278__1.3.2.1)
- [注意事项](#ZH-CN_TOPIC_0269830278__1.3.3.1)
- [参数说明](#ZH-CN_TOPIC_0269830278__1.3.4.1)
- [使用实例](#ZH-CN_TOPIC_0269830278__1.3.5.1)
- [输出结果说明](#ZH-CN_TOPIC_0269830278__1.3.6.1)

#### [名词解释](#ZH-CN_TOPIC_0269830278)

Mini Pod: Super Pod中的Pod。

Super Pod: 一种资源调度对象，在同一个Super Pod中的Pod可以共享其中的资源(同ResourceBox)。

![](POD复位（RST POD）_69830278.assets/zh-cn_image_0000001222945552_2.png)

#### [命令功能](#ZH-CN_TOPIC_0269830278)

![](POD复位（RST POD）_69830278.assets/notice_3.0-zh-cn_2.png)

- Pod复位命令会删除并重建Pod，可能会导致业务受损，请确认是否继续操作。
- 下文所有的“复位”，表示对Pod进行重建。
- 按照Pod名称进行非强制复位时，建议同类型下的Pod需要间隔一分钟再执行，否则上次执行创建的POD未Running导致本次可能不生效。
- 在5G中心裸机场景，按照Pod名称进行Super Pod复位时，该Super Pod下的所有Mini Pod都会被复位。
- 在5G中心裸机场景，按照Pod类型进行强制复位时，当类型是"vnode"时，该网元下属于vnode类型的Super Pod都会被复位，即该网元下的所有Mini Pod都会被复位。

本命令用于对指定的Pod进行复位。

#### [注意事项](#ZH-CN_TOPIC_0269830278)

- 根据Pod类型进行复位操作时，只支持强制复位；复位向此微服务注册的Pod（此类型Pod复位时需要先回调Pod注册的URL）时无此限制。
- RST POD是异步操作，实际复位结果可能存在一定时间的延迟，延迟时间小于2分钟。
- 执行RST POD命令后，等待不超过5分钟，再执行DSP POD命令，可以查看Pod状态，当“Pod状态”为“Running”时，表示POD复位成功。

#### [参数说明](#ZH-CN_TOPIC_0269830278)

| **参数标识** | **参数名称** | **参数说明** |
| --- | --- | --- |
| MEID | 网元ID | 可选必选说明：必选参数<br>参数含义：复位Pod服务所在的网元ID，即应用ID。<br>取值范围：长度不超过40的字符串。<br>默认值：无。<br>配置原则：可以通过MML命令“LST ME”获取。 |
| TYPE | Pod复位类型 | 可选必选说明：必选参数<br>参数含义：复位Pod的方式。<br>取值范围：<br>- byId(Pod名称)<br>- byType(Pod类型)<br>默认值：无。<br>配置原则：无。 |
| PODNAME | Pod名称 | 可选必选说明：该参数在<br>“Pod复位类型”<br>取值为<br>“Pod名称”<br>时为必选参数。<br>参数含义：Pod名称。<br>取值范围：字符串类型，字符串长度范围为3~100个字符。<br>默认值：无。<br>配置原则：<br>- 服务实例所在Pod名称，该参数仅在“Pod复位类型”取值为“Pod名称”时有效。<br>- 可以通过执行MML命令[**DSP POD**](POD查询（DSP POD）_69830277.md)时不输入“网元ID”且不选择“Pod查询类型”获取所有的“Pod名称”列表。<br>- 该参数不支持在“Pod名称”列表中模糊匹配复位。 |
| PODTYPE | Pod类型 | 可选必选说明：该参数在<br>“Pod复位类型”<br>取值为<br>“Pod类型”<br>时为必选参数。<br>参数含义：Pod类型。<br>取值范围：字符串类型，字符串长度范围为3~100个字符。<br>默认值：无。<br>配置原则：<br>- 该参数仅在“Pod复位类型”取值为“Pod类型”时有效。<br>- 可以通过执行MML命令[**DSP POD**](POD查询（DSP POD）_69830277.md)时不输入“网元ID”且不选择“Pod查询类型”获取所有的“Pod类型”列表。<br>- 该参数不支持在“Pod类型”列表中模糊匹配复位。 |
| FORCEFLAG | 强制复位 | 可选必选说明：必选参数<br>参数含义：强制复位标识。<br>取值范围：<br>- false(非强制复位)<br>- true(强制复位)<br>默认值：无。<br>配置原则：<br>- 强制复位：适合于任何场景，可能会影响到业务。<br>- 非强制复位：- 不支持根据pod类型复位<br>- 根据pod名称的非强制复位，会有提示信息说明。防呆分为3种情况，解释如下：<br>（1）StatefulSet：需要保证复位完后该类型pod有一半以上处于Running状态。<br>（2）Deployment：需要保证复位完后该类型pod至少有一个处于Running状态。<br>（3）DaemonSet：该类型pod不支持非强制复位。 |

#### [使用实例](#ZH-CN_TOPIC_0269830278)

1. 根据Pod名称复位Pod。
  ```
  %%RST POD:MEID="0", TYPE=byId, PODNAME="runlog-676d4f665c-qrmz6", FORCEFLAG=true;%% 
  RETCODE = 0 操作成功，RST POD是异步操作，实际复位结果可能存在一定时间的延迟，延迟时间小于2分钟。
  ---    END
  ```
2. 根据Pod类型复位Pod。
  ```
  %%RST POD:MEID="0", TYPE=byType, PODTYPE="runlog", FORCEFLAG=true;%% 
  RETCODE = 0 操作成功，RST POD是异步操作，实际复位结果可能存在一定时间的延迟，延迟时间小于2分钟。
  ---    END
  ```

#### [输出结果说明](#ZH-CN_TOPIC_0269830278)

命令执行正常，会返回命令执行成功的提示信息。

命令执行失败则返回相应的错误，参见 [表1 错误码列表](#ZH-CN_TOPIC_0269830278__table8662117193114) ；命令执行异常，请联系技术支持处理。

*表1 错误码列表*

| 错误码 | 错误码解释 | 原因分析 | 处理建议 |
| --- | --- | --- | --- |
| 140001 | MML命令请求参数为空。 | MML下发命令必输参数为空。 | MML下发命令时请输入全部必输字段。 |
| 140002 | MML命令不存在。 | 下发MML操作命令不存在。 | 确认下发的MML命令存在。 |
| 140010 | Pod复位参数Pod类型不合法。 | Pod复位参数Pod类型不合法。 | 确认Pod复位参数Pod类型合法。 |
| 140011 | Pod复位参数强制复位不合法。 | Pod复位参数强制复位不合法。 | 确认Pod复位参数强制复位合法。 |
| 140012 | 根据Pod名称复位Pod时，Pod名称不能为空。 | 根据Pod名称复位Pod时，Pod名称不能为空。 | 确认Pod名称参数不能为空。 |
| 140013 | 根据Pod类型复位Pod时，Pod类型不能为空。 | 根据Pod类型复位Pod时，Pod类型不能为空。 | 确认Pod类型参数不能为空。 |
| 140014 | Pod名称和Pod类型只能有一个有值。 | Pod名称和Pod类型只能有一个有值。 | 确认Pod名称和Pod类型不能同时有值。 |
| 140015 | Pod复位失败，Pod类型校验失败。 | Pod复位失败，Pod类型校验失败。 | 确认Pod类型正确。 |
| 140017 | Pod复位失败。 | 回调Pod注册URL失败。 | 确认回调返回失败的原因。 |
| 140018 | Pod复位失败，请联系华为技术人员。 | MML复位结果异常。 | 联系技术支持处理。 |
| 140019 | 其他错误。 | MML复位命令异常。 | 联系技术支持处理。 |
| 140020 | Pod复位失败，Pod复位参数长度必须在3到100之间。 | Pod复位参数长度必须在3到100之间。 | Pod复位参数长度确保在3到100之间。 |
| 140021 | 复位Pod不存在。 | 复位Pod信息不存在。 | 通过执行MML命令“DSP POD”（参数<br>“网元ID”<br>输入当前命令执行的<br>“网元ID”<br>）查询该网元所有的Pod，确认Pod信息是否存在。 |
| 140022 | Pod复位参数网元ID校验异常。 | Pod复位参数网元ID校验异常。 | 确认存在有效的网元ID，进入“首页”，查看输入的网元ID（页面显示为应用ID）下的服务数是否为0。 |
| 140024 | Pod复位参数网元ID不存在。 | Pod复位参数网元ID不存在。 | 确认输入参数网元ID为存在网元ID。 |
| 140029 | Pod复位失败，根据Pod类型复位只支持强制复位。 | Pod复位失败，根据Pod类型复位只支持强制复位。 | 根据Pod类型复位场景下，只能选强制复位。 |
| 140030 | Pod复位失败，按照Pod名称非强制复位时，Deployment类型的Pod要求同类型下需要有其他Running的Pod。 | 按照Pod名称非强制复位时，Deployment类型的Pod要求同类型下需要有其他Running的Pod。 | 保证该Deployment类型下有其他Pod处于Running状态后，再执行该操作。 |
| 140031 | Pod复位失败，DaemonSet类型的Pod不允许按照Pod名称非强制复位。 | DaemonSet类型的Pod不允许按照Pod名称非强制复位。 | DaemonSet类型的Pod不允许按照Pod名称非强制复位。 |
| 140032 | Pod复位失败，按照Pod名称非强制复位时，StatefulSet类型的Pod要求同类型下要有一半以上的Pod处于Running状态。 | 按照Pod名称非强制复位时，StatefulSet类型的Pod要求同类型下要有一半以上的Pod处于Running状态。 | 按照Pod名称非强制复位时，对于StatefulSet类型的Pod，保证同类型下要有一半以上的Pod处于Running状态。 |
| 140033 | Pod复位失败，该Pod类型不允许按照Pod名称非强制复位。 | 该Pod类型不允许按照Pod名称非强制复位。 | 该Pod类型不允许按照Pod名称非强制复位。 |
