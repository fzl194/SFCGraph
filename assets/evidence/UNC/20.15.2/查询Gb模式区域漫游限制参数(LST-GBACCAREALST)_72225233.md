# 查询Gb模式区域漫游限制参数(LST GBACCAREALST)

- [命令功能](#ZH-CN_MMLREF_0000001172225233__1.3.1.1)
- [注意事项](#ZH-CN_MMLREF_0000001172225233__1.3.2.1)
- [本地用户权限](#ZH-CN_MMLREF_0000001172225233__1.3.3.1)
- [网管用户权限](#ZH-CN_MMLREF_0000001172225233__1.3.4.1)
- [参数说明](#ZH-CN_MMLREF_0000001172225233__1.3.5.1)
- [使用实例](#ZH-CN_MMLREF_0000001172225233__1.3.6.1)
- [输出结果说明](#ZH-CN_MMLREF_0000001172225233__1.3.7.1)

#### [命令功能](#ZH-CN_MMLREF_0000001172225233)

**适用网元：SGSN**

此命令用于查询Gb模式区域漫游限制参数。

#### [注意事项](#ZH-CN_MMLREF_0000001172225233)

此命令执行后立即生效。

#### [本地用户权限](#ZH-CN_MMLREF_0000001172225233)

manage-ug；system-ug；monitor-ug

#### [网管用户权限](#ZH-CN_MMLREF_0000001172225233)

G_1，管理员级别命令组；G_2，操作员级别命令组；G_3，用户级别命令组

#### [参数说明](#ZH-CN_MMLREF_0000001172225233)

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| AREA | 区域范围 | 可选必选说明：可选参数<br>参数含义：待查询的区域范围。<br>取值范围：<br>- “AG(区域组)”：表示该区域标识类型为区域组<br>- “LA(指定位置区)”：表示该区域标识类型为指定位置区。<br>- “RA(指定路由区)”：表示该区域标识类型为指定路由区。<br>- “OTHER(其他)”：表示该区域标识类型为其他<br>默认值：无<br>说明：- 区域范围表示进行接入控制所适用的位置区域。<br>- 区域范围按照粒度从粗到细分为以下几个级别： 区域组，指定位置区，指定路由区，其他。<br>- 相同粒度区域范围的各记录的区域范围不能交迭。<br>- 使用时先按照位置区域查找记录，然后按照用户的IMSI查找是否存在对应记录，查找不到再使用较粗的区域粒度进行查找。<br>- 未配置在该表区域范围中的用户缺省允许接入。 |
| MCC | 移动国家码 | 可选必选说明：条件可选参数<br>参数含义：待查询的移动国家代码。<br>前提条件：该参数在当<br>“AREA(区域范围)”<br>为指定<br>“LA(位置区)”<br>或<br>“RA(路由区)”<br>时生效。<br>取值范围：3位的十进制数字<br>默认值：无 |
| MNC | 移动网号 | 可选必选说明：条件可选参数<br>参数含义：待查询的移动网号。<br>前提条件：该参数在当<br>“AREA(区域范围)”<br>为指定<br>“LA(位置区)”<br>或<br>“RA(路由区)”<br>时生效。<br>取值范围：2～3位的十进制数字<br>默认值：无 |
| LAC | 位置区域码 | 可选必选说明：条件可选参数<br>参数含义：待查询的位置区域码。<br>前提条件：该参数在当<br>“AREA(区域范围)”<br>为指定<br>“LA(位置区)”<br>或<br>“RA(路由区)”<br>时生效。<br>取值范围：0x0000～0xFFFF<br>默认值：无<br>说明：- 各记录的LAC区段不能交叉，但是同一个LAC区段可以配置多种IMSI记录。<br>- 各记录的IMSI不能重复，支持最长匹配的查询方式。 |
| RAC | 路由区域码 | 可选必选说明：条件可选参数<br>参数含义：待查询的路由区域码。<br>前提条件：该参数在当<br>“AREA(区域范围)”<br>为指定<br>“RA(路由区)”<br>时生效。<br>取值范围：0x00～0xFF<br>默认值：无<br>说明：- 各记录的RAC区段不能交叉，但是同一个RAC区段可以配置多种IMSI记录。<br>- 各记录的IMSI不能重复，支持最长匹配的查询方式。 |
| AREAID | 区域群标识 | 可选必选说明：条件可选参数<br>参数含义：待查询的区域群标识。<br>前提条件：该参数在<br>“AREA(区域范围)”<br>设置为<br>“AG(区域组)”<br>时生效。<br>取值范围：1～50<br>默认值：无<br>说明：该参数需要在<br>[**ADD AREAGP**](../区域群管理/增加区域群(ADD AREAGP)_26145542.md)<br>命令中事先配置过，可执行<br>[**LST AREAGP**](../区域群管理/查询区域群(LST AREAGP)_72345141.md)<br>进行查看。 |
| SUBRANGE | 用户范围 | 可选必选说明：可选参数<br>参数含义：待查询的用户范围。<br>取值范围：<br>- “ALL_USER(所有用户)”：表示用户范围为所有用户。<br>- “SPECIAL_IMSIPRE(指定IMSI前缀)”：表示该用户范围为指定IMSI前缀。<br>- “USER_GROUP(用户群)”：表示用户范围为用户群。<br>- “SPECIAL_IMSI_RANGE(指定IMSI范围)”：表示该用户范围为指定IMSI范围。<br>- “UE_TYPE(终端类型)”：保留，暂未实现。<br>默认值：无 |
| IMSIPRE | IMSI前缀 | 可选必选说明：条件可选参数<br>参数含义：待查询的IMSI前缀。使用时按照IMSI最长匹配进行查询。<br>前提条件：该参数在<br>“SUBRANGE(用户范围)”<br>设置为<br>“SPECIAL_IMSIPRE(指定IMSI前缀)”<br>时生效。<br>取值范围：1～15位的十进制数字<br>默认值：无<br>说明：支持IMSI最长匹配的查询方式。 |
| IMSI | IMSI | 可选必选说明：条件可选参数<br>参数含义：待查询的IMSI。<br>前提条件：该参数在<br>“SUBRANGE(用户范围)”<br>设置为<br>“SPECIAL_IMSI_RANGE(指定IMSI范围)”<br>时生效。<br>取值范围：1～15位的十进制数字<br>默认值：无 |
| SUBID | 用户群标识 | 可选必选说明：条件可选参数<br>参数含义：待查询的用户群标识。<br>前提条件：该参数在<br>“SUBRANGE(用户范围)”<br>设置<br>“USER_GROUP(用户群)”<br>时生效。<br>取值范围：1～100<br>默认值：无 |

#### [使用实例](#ZH-CN_MMLREF_0000001172225233)

查询Gb模式区域漫游限制参数记录：

LST GBACCAREALST:;

```
%%LST GBACCAREALST:;%%
RETCODE = 0  操作成功。

输出结果如下
--------------
      区域范围  =  指定位置区
    移动国家码  =  123
      移动网号  =  123
    位置区域码  =  0x0456
位置区域码范围  =  0x0789
    路由区域码  =  NULL
路由区域码范围  =  NULL
    区域群标识  =  NULL
      用户范围  =  指定IMSI范围
      起始IMSI  =  123000000000001
      终止IMSI  =  123000000000009
    用户群标识  =  NULL
      控制类型  =  拒绝
        原因值  =  GPRS服务被禁止
  自定义原因值  =  NULL
(结果个数 = 1)

---    END
```

#### [输出结果说明](#ZH-CN_MMLREF_0000001172225233)

参见 [**ADD GBACCAREALST**](增加Gb模式区域漫游限制参数（ADD GBACCAREALST）_72345149.md) 的参数说明。
