# 加载扩展包（LOD COMPKG）

- [命令功能](#ZH-CN_TOPIC_0242742473__1.3.1.1)
- [注意事项](#ZH-CN_TOPIC_0242742473__1.3.2.1)
- [参数说明](#ZH-CN_TOPIC_0242742473__1.3.3.1)
- [使用实例](#ZH-CN_TOPIC_0242742473__1.3.4.1)
- [输出结果说明](#ZH-CN_TOPIC_0242742473__1.3.6.1)

#### [命令功能](#ZH-CN_TOPIC_0242742473)

![](加载扩展包（LOD COMPKG）_42742473.assets/notice_3.0-zh-cn_2.png)

- 此命令为高危命令，请谨慎使用并联系华为技术支持协助操作。
- 部署了CGPLite服务的系统，如果涉及组件包的加载，组件包加载成功后CGPLite服务会自动重启，正常情况下复位需要一分钟左右的时间，重启期间CGPLite相关业务不可用。

网元采用扩容方式上线扩展域业务，需要执行此命令完成扩展域适配包、组件包的加载。

#### [注意事项](#ZH-CN_TOPIC_0242742473)

无。

#### [参数说明](#ZH-CN_TOPIC_0242742473)

| **参数标识** | **参数名称** | **参数说明** |
| --- | --- | --- |
| MEID | 网元ID | 可选必选说明：必选参数。<br>参数含义：网元ID，可以通过<br>[**LST ME**](../../系统管理/版本信息/查询网元配置信息（LST ME）_47084797.md)<br>获取。<br>取值范围：0～65535<br>默认值：无。<br>配置原则：无。 |
| PKGNAME | 网元包名称 | 可选必选说明：可选参数。<br>参数含义：上线扩展域网元业务时所需的网元包名称，网元包中包含扩展域适配包、组件包。通过VNFM上传后，该网元包将存储在VNFM系统后台，路径如下：/export/home/catalog/softpkg/HUAWEI/{APPTYPE}/{VERSION}/{PKGNAME}。<br>取值范围：长度不超过50的字符串。<br>默认值：无。<br>配置原则：网元包名称和适配包名称不能同时为空。 |
| PATCHPKGNAME | 网元补丁包名称 | 可选必选说明：可选参数。<br>参数含义：基于补丁版本上线扩展域网元业务时所需的网元补丁包名称，网元补丁包中包含扩展域适配包、组件包。通过VNFM上传后，该网元包将存储在VNFM系统后台，路径如下：/export/home/catalog/softpkg/HUAWEI/{APPTYPE}/{PATCHVERSION}/{PATCHPKGNAME}。<br>取值范围：长度不超过50的字符串。<br>默认值：无。<br>配置原则：输入网元补丁包名称时必须输入网元包名称。 |
| OMANAME | 适配包名称 | 可选必选说明：可选参数。<br>参数含义：上线扩展域网元业务时所需的适配包名称。适配包中包含扩展域的适配包，通过VNFM上传后，该适配包将存储在VNFM系统后台，路径如下：<br>/export/home/catalog/softpkg/HUAWEI/{APPTYPE}/{VERSION}/{OMANAME}。<br>取值范围：长度不超过50的字符串。<br>默认值：无。<br>配置原则：<br>- 网元包名称和适配包名称不能同时为空。<br>- 对于软件包解耦场景，扩展域组件包和扩展域适配包不在同一个大包下，需要将扩展域组件包所在的软件包名称输入到网元包名称参数中，扩展域适配包所在的软件包的包名称输入到适配包名称中。<br>- 对于上线的扩展域只有适配包需要加载场景，只用输入适配包名称即可。 |
| PATCHOMANAME | 补丁适配包名称 | 可选必选说明：可选参数。网元包名称和适配包名称不能同时为空<br>参数含义：基于补丁版本上线扩展域网元业务时所需的补丁适配包名称，补丁适配包中包含扩展域适配包。通过VNFM上传后，该补丁适配包将存储在VNFM系统后台，路径如下：/export/home/catalog/softpkg/HUAWEI/{APPTYPE}/{PATCHVERSION}/{PATCHOMANAME}。<br>取值范围：长度不超过50的字符串。<br>默认值：无。<br>配置原则：输入补丁适配包名称时必须输入适配包名称。 |

#### [使用实例](#ZH-CN_TOPIC_0242742473)

加载网元ID为219，网元包名称为APP_20.X的软件包。

```
%%LOD COMPKG: MEID=219, PKGNAME="APP_20.X";%%
RETCODE = 0  正在下载适配包

进度报告
--------
已完成 = 20%
(结果个数 = 1)

---    END

%%LOD COMPKG: MEID=219, PKGNAME="APP_20.X";%%
RETCODE = 0  正在推送适配包

进度报告
--------
已完成 = 50%
(结果个数 = 1)

---    END

%%LOD COMPKG: MEID=219, PKGNAME="APP_20.X";%%
RETCODE = 0  操作成功

---    END
```

#### [输出结果说明](#ZH-CN_TOPIC_0242742473)

命令执行正常，会定时返回命令执行时的进度信息及执行完成后的成功提示信息。

该命令执行异常，会返回对应的错误码。常见错误码如 [表1](#ZH-CN_TOPIC_0242742473__table14686130124019) 所示。

*表1 错误码列表*

| 错误码 | 错误码解释 | 原因分析 | 处理建议 |
| --- | --- | --- | --- |
| 150001 | 查询网元信息失败 | 输入了错误的网元ID值。 | 检查并修改为正确的网元ID。 |
| 150002 | MML命令参数校验失败 | 输入了错误的输入参数。 | 按照参数说明检查修改输入参数。 |
| 150007 | 下载适配包失败 | 输入了错误网元包名称。 | 重新输入正确的网元包名称。 |
| 150008 | 验证适配包签名失败 | 验证适配包签名失败，适配包签名被篡改。 | 请联系华为技术支持。 |
| 150009 | 推送适配包失败 | 给扩展域MK服务推送扩展域适配包失败。 | 请联系华为技术支持。 |
| 150010 | 下发消息到CGPBroker失败 | 给CGPBroker发送处理扩展域包消息失败。 | 请联系华为技术支持。 |
| 150011 | 查询CGPBroker状态失败 | 查询CGPBroker处理扩展域包的执行结果失败。 | 请联系华为技术支持。 |
| 150012 | 添加扩展域MK应用信息失败 | 给扩展域MK服务推送扩展域应用信息失败（可能原因是扩展域的堆栈没上线）。 | 登录VNF LCM，选择“VNF生命周期管理->扩缩容管理->扩缩容任务”查看VNF对应的扩展域堆栈上线任务是否执行成功，确保扩展域堆栈任务执行成功，否则联系华为技术支持。 |
| 150016 | 与CGPBroker通信异常 | 与CGPBroker通信查询信息异常。 | 请联系华为技术支持。 |
| 150021 | 当前模式不支持此命令 | 无需部署CGPBroker服务的模式不支持此命令。 | 无需处理。 |
| 150022 | 已加载组件包回滚失败 | LOD COMPKG命令执行失败后，执行自动回滚失败 | 请联系华为技术支持。 |
| 150024 | 连接sftp失败。可能的原因：（1）sftp用户密码错误；（2）网络异常 | sftp用户密码错误；网络异常。 | 请联系华为技术支持。 |
| 150025 | 该组件包已经被加载 | 组件包已经加载过，无需重复加载。 | 无需处理。 |
| 150026 | CSP扩展域信息不存在 | CSP扩展域信息不存在。 | 部署CSP扩展域。 |
| 150027 | 网元包名称和适配包名称不能同时为空 | 网元包名称和适配包名称不能同时为空 | 检查并修改请求命令参数 |
| 150028 | 存在网元补丁包名称必须有网元包名称，或者存在补丁适配包名称必须有适配包名称 | 输入参数错误 | 按照参数说明检查修改输入参数。 |
| 150031 | 存在相同任务正在进行中，请稍后。 | 存在相同任务正在进行中，请稍后。 | 待上一次任务执行成功后再执行。 |
