# 查询逻辑口报文统计信息（DSP INTERFACEPERF）

- [命令功能](#ZH-CN_CONCEPT_0186526363__1.3.1.1)
- [注意事项](#ZH-CN_CONCEPT_0186526363__1.3.2.1)
- [操作用户权限](#ZH-CN_CONCEPT_0186526363__1.3.3.1)
- [参数说明](#ZH-CN_CONCEPT_0186526363__1.3.4.1)
- [使用实例](#ZH-CN_CONCEPT_0186526363__1.3.5.1)
- [输出结果说明](#ZH-CN_CONCEPT_0186526363__1.3.6.1)

#### [命令功能](#ZH-CN_CONCEPT_0186526363)

**适用NF：SGW-U、PGW-U、UPF**

该命令用来查看接口当前运行状态和接口统计信息。在监控接口的状态或检查接口的故障原因时，可执行该命令获取接口的状态信息和统计信息。用户可以根据这些信息进行流量统计和接口的故障诊断等。

#### [注意事项](#ZH-CN_CONCEPT_0186526363)

无。

#### [操作用户权限](#ZH-CN_CONCEPT_0186526363)

G_1，管理员级别命令组；G_2，操作员级别命令组；G_3，用户级别命令组

#### [参数说明](#ZH-CN_CONCEPT_0186526363)

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| INTERFACENAME | 逻辑接口名称 | 可选必选说明：可选参数<br>参数含义：该参数用于指定要查询的逻辑接口名称。<br>数据来源：本端规划<br>取值范围：字符串类型，输入长度范围为1～63。<br>默认值：无<br>配置原则：无 |

#### [使用实例](#ZH-CN_CONCEPT_0186526363)

以paif接口为例，查看paif1/0/0接口的状态信息和统计信息：

```
DSP INTERFACEPERF: INTERFACENAME="paif1/0/0";
```

```

RETCODE = 0  操作成功。

结果如下
--------
                                逻辑接口名称  =  paif1/0/0
                                    当前状态  =  正常
                      接收的总报文个数（包）  =  102
                      发送的总报文个数（包）  =  102
                      接收的总字节数（字节）  =  7040
                      发送的总字节数（字节）  =  7040
        前5秒内通过接口接收的包速率（包/秒）  =  2
        前5秒内通过接口发送的包速率（包/秒）  =  2
    前5秒内通过接口接收的字节速率（比特/秒）  =  1120
    前5秒内通过接口发送的字节速率（比特/秒）  =  1120
                  接收的Ipv6总报文个数（包）  =  46
                  发送的Ipv6总报文个数（包）  =  46
                  接收的Ipv6总字节数（字节）  =  3680
                  发送的Ipv6总字节数（字节）  =  3680
    前5秒内通过接口接收的Ipv6包速率（包/秒）  =  1
    前5秒内通过接口发送的Ipv6包速率（包/秒）  =  1
前5秒内通过接口接收的Ipv6字节速率（比特/秒）  =  640
前5秒内通过接口发送的Ipv6字节速率（比特/秒）  =  640
(结果个数 = 1)
---    END
```

#### [输出结果说明](#ZH-CN_CONCEPT_0186526363)

| 输出项名称 | 输出项解释 |
| --- | --- |
| 逻辑接口名称 | 用于指定要查询的逻辑接口名称。 |
| 当前状态 | 用于显示接口的协议状态。UP：接口的链路协议处于正常的启动状态。DOWN：接口的链路协议层出现故障，或者没有在此接口配置IP地址，或者逻辑口所在的ISU组状态不正常。 |
| 接收的总报文个数（包） | 用于显示接口接收的报文统计总个数。 |
| 发送的总报文个数（包） | 用于显示接口发送的报文统计总个数。 |
| 接收的总字节数（字节） | 用于显示接口接收的报文统计总字节数。 |
| 发送的总字节数（字节） | 用于显示接口发送的报文统计总字节数。 |
| 前5秒内通过接口接收的包速率（包/秒） | 用于显示在前5秒钟内通过接口接收的报文速率。每次执行本命令时此字段都会重新统计，即显示本命令执行时间前5秒内通过接口接收的报文速率，统计方式是报文数/5秒。 |
| 前5秒内通过接口发送的包速率（包/秒） | 用于显示在前5秒钟内通过接口发送的报文速率。每次执行本命令时此字段都会重新统计，即显示本命令执行时间前5秒内通过接口发送的报文速率，统计方式是报文数/5秒。 |
| 前5秒内通过接口接收的字节速率（比特/秒） | 用于显示在前5秒钟内通过接口接收的比特速率。每次执行本命令时此字段都会重新统计，即显示本命令执行时间前5秒内通过接口接收的比特速率，统计方式是比特数/5秒。 |
| 前5秒内通过接口发送的字节速率（比特/秒） | 用于显示在前5秒钟内通过接口发送的比特速率。每次执行本命令时此字段都会重新统计，即显示本命令执行时间前5秒内通过接口发送的比特速率，统计方式是比特数/5秒。 |
| 前5秒内通过接口接收的Ipv6包速率（包/秒） | 用于显示在前5秒钟内通过接口接收的Ipv6报文速率。每次执行本命令时此字段都会重新统计，即显示本命令执行时间前5秒内通过接口接收的Ipv6报文速率，统计方式是报文数/5秒。 |
| 前5秒内通过接口发送的Ipv6字节速率（比特/秒） | 用于显示在前5秒钟内通过接口发送的Ipv6比特速率。每次执行本命令时此字段都会重新统计，即显示本命令执行时间前5秒内通过接口发送的Ipv6比特速率，统计方式是比特数/5秒。 |
| 接收的Ipv6总字节数（字节） | 用于显示接口接收的Ipv6报文统计总字节数。 |
| 接收的Ipv6总报文个数（包） | 用于显示接口接收的Ipv6报文统计总个数。 |
| 前5秒内通过接口接收的Ipv6字节速率（比特/秒） | 用于显示在前5秒钟内通过接口接收的Ipv6比特速率。每次执行本命令时此字段都会重新统计，即显示本命令执行时间前5秒内通过接口接收的Ipv6比特速率，统计方式是比特数/5秒。 |
| 发送的Ipv6总字节数（字节） | 用于显示接口发送的Ipv6报文统计总字节数。 |
| 前5秒内通过接口发送的Ipv6包速率（包/秒） | 用于显示在前5秒钟内通过接口发送的Ipv6报文速率。每次执行本命令时此字段都会重新统计，即显示本命令执行时间前5秒内通过接口发送的Ipv6报文速率，统计方式是报文数/5秒。 |
| 发送的Ipv6总报文个数（包） | 用于显示接口发送的Ipv6报文统计总个数。 |
