# POD停止（STP POD）

- [命令功能](#ZH-CN_MMLREF_0000001557196802__1.3.1)
- [注意事项](#ZH-CN_MMLREF_0000001557196802__1.3.2)
- [参数说明](#ZH-CN_MMLREF_0000001557196802__1.3.3)
- [使用实例](#ZH-CN_MMLREF_0000001557196802__1.3.4)
- [输出结果说明](#ZH-CN_MMLREF_0000001557196802__1.3.5)

## [命令功能](#ZH-CN_MMLREF_0000001557196802)

![](POD停止（STP POD）_57196802.assets/notice_3.0-zh-cn.png)

Pod停止运行可能会导致业务受损，请谨慎使用该命令。

该命令用于停止Pod运行。

> **说明**
> - 在第三方场景下不支持该命令。
> - 该命令功能依赖NFV_FusionStage能力，CSP配套NFV_FusionStage 22.1.0及之后的版本，支持该命令，配套其它版本执行该命令不生效或执行失败。

> **说明**
> 无。

## [参数说明](#ZH-CN_MMLREF_0000001557196802)

| **参数标识** | **参数名称** | **参数说明** |
| --- | --- | --- |
| MEID | 网元ID | 可选必选说明：必选参数。<br>参数含义：当前Pod服务所在的网元ID，即应用ID。<br>取值范围：长度不超过40的字符串。<br>默认值：无。<br>配置原则：可以通过MML命令<br>[**LST ME**](../../系统管理/版本信息/查询网元配置信息（LST ME）_47084797.md)<br>获取。 |
| PODNAME | Pod名称 | 可选必选说明：必选参数。<br>参数含义：Pod名称。<br>取值范围：字符串类型，字符串长度范围为3~100个字符。<br>默认值：无。<br>配置原则：服务实例所在Pod名称。 |
| FORCEFLAG | 强制停止 | 可选必选说明：必选参数。<br>参数含义：是否强制停止标志位。<br>取值范围：<br>- false(非强制停止)<br>- true(强制停止)<br>默认值：无。<br>配置原则：<br>- 强制停止Pod：只校验Pod是否存在，如果存在则下发Pod停止命令。<br>- 非强制停止Pod：有防呆处理，按照Pod类型分别执行下面操作：- StatefulSet类型：需要保证停止完后该类型Pod有一半以上处于Running状态。<br>- Deployment类型：需要保证停止完后该类型Pod至少有一个处于Running状态。<br>- DaemonSet类型：该类型Pod不支持非强制停止。 |

## [使用实例](#ZH-CN_MMLREF_0000001557196802)

1. 执行停止Pod成功，其中“网元ID”为“0”，“Pod名称”为“zookeeper-0”，非强制停止。
  STP POD: MEID="0", PODNAME="zookeeper-0", FORCEFLAG=false;

  ```
  %%STP POD: MEID="0", PODNAME="zookeeper-0", FORCEFLAG=false;%%
  RETCODE = 0  操作成功

  ---    END
  ```
2. 执行停止Pod失败，其中“网元ID”为“0”，“Pod名称”为“zookeeper-5”，非强制停止。
  STP POD: MEID="0", PODNAME="zookeeper-5", FORCEFLAG=false;

  ```
  %%STP POD: MEID="0", PODNAME="zookeeper-5", FORCEFLAG=false;%%
  RETCODE = 140041  启停Pod不存在。

  ---    END
  ```

## [输出结果说明](#ZH-CN_MMLREF_0000001557196802)

命令执行正常，会返回命令执行成功的提示信息。

命令执行失败则返回相应的错误，参见 [表1](#ZH-CN_MMLREF_0000001557196802__table8662117193114) ；命令执行异常，请联系华为技术支持处理。

*表1 错误码列表*

| 错误码 | 错误码解释 | 原因分析 | 处理建议 |
| --- | --- | --- | --- |
| 140001 | MML命令请求参数为空。 | MML下发命令必填参数为空。 | MML下发命令时请输入全部必填字段。 |
| 140002 | MML命令不存在。 | 下发MML操作命令不存在。 | 确认下发的MML命令存在。 |
| 140041 | 启停Pod不存在。 | 该Pod不存在。 | 确认Pod是否存在。 |
| 140042 | 该Pod不支持启停操作。 | 该Pod不支持启停操作。 | 请联系华为技术人员。 |
| 140043 | Pod启停参数网元ID不存在。 | Pod启停参数网元ID不存在。 | 检查网元ID是否存在。 |
| 140044 | Pod启停参数网元ID和Pod不对应。 | Pod启停参数网元ID和Pod不对应。 | 检查网元ID是否和Pod对应。 |
| 140045 | 该Pod不支持强制停止操作。 | 该Pod不支持强制停止操作。 | 请联系华为技术人员。 |
| 140046 | 环境不支持Pod启停操作。 | 当前环境不支持Pod启停操作。 | 请联系华为技术人员。 |
| 140047 | Pod启停结果查询失败。 | Pod启停结果查询失败，可能为命令执行超时。 | 等待两分钟后，执行<br>[**DSP POD**](POD查询（DSP POD）_69830277.md)<br>命令查询<br>“Pod状态”<br>，如果为<br>“Pending”<br>则执行成功，其余情况请联系华为技术人员。 |
| 140048 | Pod启停操作失败，请联系华为技术人员。 | Pod启停操作失败，请联系华为技术人员。 | 请联系华为技术人员。 |
| 140049 | Pod停止失败，按照Pod名称非强制停止时，Deployment类型的Pod要求同类型下需要有其他Running的Pod。 | Pod停止失败，按照Pod名称非强制停止时，Deployment类型的Pod要求同类型下需要有其他Running的Pod。 | 请联系华为技术人员。 |
| 140050 | Pod停止失败，DaemonSet类型的Pod不允许按照Pod名称非强制停止。 | Pod停止失败，DaemonSet类型的Pod不允许按照Pod名称非强制停止。 | 请联系华为技术人员。 |
| 140051 | Pod停止失败，按照Pod名称非强制停止时，StatefulSet类型的Pod要求同类型下要有一半以上的Pod处于Running状态。 | Pod停止失败，按照Pod名称非强制停止时，StatefulSet类型的Pod要求同类型下要有一半以上的Pod处于Running状态。 | 请联系华为技术人员。 |
| 140052 | Pod停止失败，该Pod类型不允许非强制停止。 | Pod停止失败，该Pod类型不允许非强制停止。 | 请联系华为技术人员。 |
| 140053 | Pod查询失败，请联系华为技术人员。 | Pod查询失败，请联系华为技术人员。 | 请联系华为技术人员。 |
| 140054 | Pod名称长度为3~100。 | Pod名称长度不符合取值范围。 | 确认Pod名称长度为3~100。 |
