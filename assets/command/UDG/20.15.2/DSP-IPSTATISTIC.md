---
id: UDG@20.15.2@MMLCommand@DSP IPSTATISTIC
type: MMLCommand
name: DSP IPSTATISTIC（查询IP报文统计）
nf: UDG
version: 20.15.2
verb: DSP
object_keyword: IPSTATISTIC
command_category: 查询类
effect_mode: ''
is_dangerous: false
category_path:
- 平台服务管理
- VNRS功能管理
- IP服务
- IP协议栈
- IP协议统计
- IP报文统计
status: active
---

# DSP IPSTATISTIC（查询IP报文统计）

## 功能

该命令用于查看IP报文统计信息。

通过该命令的显示信息可以查看包括接收报文（其中包括被丢弃的带源路由选项的报文）、发送报文、分片报文、重组报文的统计信息。

在传输报文的过程中，当源端对报文进行了分片，可以通过该命令查看分片成功的IP报文总数和发送的分片报文总数，然后再在目的端查看接收到的分片报文数是否正确。

通过DSP IPSTATISTIC命令可以用来查看IP流量统计详细信息，从而帮助用户定位到具体应用在报文收发过程中的丢包问题。

不指定参数时，查询所有接口的统计信息；当指定参数时，可以查询指定接口的统计信息。

## 注意事项

无。

## 权限

G_1，管理员级别命令组；G_2，操作员级别命令组；G_3，用户级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| IFNAME | 接口名称 | 可选必选说明：可选参数<br>参数含义：该参数表示IP报文统计的接口名称。<br>数据来源：本端规划<br>取值范围：字符串类型，输入长度范围为1～63。接口名称由接口类型+接口编号组成。<br>默认值：无<br>配置原则：请使用LST INTERFACE命令查看可用接口。 |

## 操作的配置对象

- [IP报文统计（IPSTATISTIC）](configobject/UDG/20.15.2/IPSTATISTIC.md)

## 使用实例

显示当前系统的IP报文统计信息：

```
DSP IPSTATISTIC:;
```

```

        RETCODE = 0  操作成功

        结果如下
        -------------------------
                                      收到的IP报文总数  =  1836588
                      收到的IP报文中本机处理的报文个数  =  700802
            收到的IP报文中使用了不正确协议号的报文个数  =  0
        收到的IP报文中携带了不可识别的IP选项的报文个数  =  0
        收到的IP报文中由于源路由选项而丢弃掉的报文个数  =  0
           收到的IP报文中由于TTL超时而丢弃掉的报文个数  =  0
                                      转发IP报文的个数  =  14159
                      发送IP报文时找不到路由的报文个数  =  0
                                    丢弃的分片报文个数  =  0
                                    重组的分片报文个数  =  106
                                    重组超时的报文个数  =  0
              收到的IP报文中因格式错误而丢弃的报文个数  =  0
            收到的IP报文中因校验码错误而丢弃的报文个数  =  82751
                                      发送的IP报文总数  =  571265
                        发送的IP报文中被本机丢弃的个数  =  0
                                发送的分片IP报文的个数  =  0
                              接收到的IP分片报文的个数  =  0
                          收到的禁止分片的IP报文的个数  =  0
                                  收到的分片IP报文个数  =  106
                        收到的IP报文中IP首部错误的报文  =  0
        (结果个数 = 1)
        ---   END
```

## 证据

- 原始手册：`evidence/UDG/20.15.2/查询IP报文统计（DSP-IPSTATISTIC）_50121210.md`
