---
id: UDG@20.15.2@MMLCommand@ADD URR
type: MMLCommand
name: ADD URR（增加URR）
nf: UDG
version: 20.15.2
verb: ADD
object_keyword: URR
command_category: 配置类
applicable_nf:
- PGW-U
- UPF
effect_mode: 立即生效
is_dangerous: false
max_records: 65000
category_path:
- 用户面服务管理
- 业务控制策略
- 计费控制
- 使用率上报规则
status: active
---

# ADD URR（增加URR）

## 功能

**适用NF：PGW-U、UPF**

该命令用于增加使用量上报规则信息，通过该命令可以针对不同应用类型的数据采取不同的上报规则。

## 注意事项

- 该命令执行后立即生效。
- 该命令最大记录数为65000。当配置记录数大于规格的90%时，会上报“ALM-135602215 配置数量超阈值”告警。当配置记录数小于等于配置规格90%时，恢复“ALM-135602215 配置数量超阈值”告警。阈值可以通过MOD CFGTHRESHOLD命令修改。
- 该命令支持配置在线计费URR、离线计费URR以及监控属性的URR。
- 该命令中的URRID字段要求在所有URR内唯一，建议从1000开始分配。
- 开启事件计费时, 对性能会产生一定影响。

## 权限

G_1，管理员级别命令组；G_2，操作员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| URRNAME | 使用量上报规则名称 | 可选必选说明：必选参数<br>参数含义：该参数用于设置URR名称。<br>数据来源：本端规划<br>取值范围：字符串类型，输入长度范围为1～31。不支持空格，不区分大小写。<br>默认值：无<br>配置原则：无 |
| URRID | URR标识 | 可选必选说明：必选参数<br>参数含义：该参数用于设置URR标识。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0～2147483646。<br>默认值：2147483647 |
| USAGERPTMODE | 使用量上报模式 | 可选必选说明：必选参数<br>参数含义：该参数用于设置使用量上报模式。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- OFFLINE：离线计费，表示该URR为离线计费应用的URR。<br>- ONLINE：在线计费，表示该URR为在线计费应用的URR。<br>- MONITORINGKEY：监控属性值，表示该URR为流量累计应用的Monitoring-Key。<br>- QOS：QoS，表示该URR为QoS保证。<br>默认值：无<br>配置原则：无 |
| DFTQUOTAVOLUME | 默认流量配额 | 可选必选说明：条件可选参数<br>前提条件：该参数在“USAGERPTMODE”配置为“ONLINE”时为可选参数。<br>参数含义：该参数用于设置默认流量配额大小，单位参考DFTQUOTAPARA参数。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0、1000～4294967295。配置为0时，表示该URR不使能默认配额流量功能，配置为4294967295时，表示默认流量配额继承全局配置。<br>默认值：无<br>配置原则：<br>- 如果运营商希望设定用户使用的默认配额流量，则配置该参数。<br>- 配置过大，导致免费放通的流量多；配置过小，申请配额期间用户体验差。建议按现网用户的速率和申请配额平均时间综合考虑此参数。 |
| DFTQUOTATIME | 默认时长配额（秒） | 可选必选说明：条件可选参数<br>前提条件：该参数在“USAGERPTMODE”配置为“ONLINE”时为可选参数。<br>参数含义：该参数用于设置默认时长配额。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0～3600、4294967295。配置为0时，表示该URR不使能默认配额时长功能，配置为4294967295时，表示默认时长配额继承全局配置。<br>默认值：无<br>配置原则：如果运营商希望设定用户使用的默认配额时长，则配置该参数。 |
| ONLMETERINGTYPE | 在线计费统计类型 | 可选必选说明：条件可选参数<br>前提条件：该参数在“USAGERPTMODE”配置为“ONLINE”时为可选参数。<br>参数含义：该参数用于设置在线计费统计类型。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- VOLUME：按流量计费。<br>- TIME：按时长计费。<br>- EVENT：按事件计费。<br>- FREE：免费。表示该URR有计费欺诈风险，需要做计费防欺诈判断。<br>- VOLUME_TIME：按流量和时长计费。<br>- EVENT_VOLUME：按事件和流量计费。<br>- EVENT_TIME：按事件和时长计费。<br>- EVENT_VOLUME_TIME：按事件、流量和时长计费。<br>默认值：VOLUME<br>配置原则：<br>- 如果运营商希望采取计费方式为在线计费，可以配置该参数。要实现流量免费，需要在UNC上一起配合做配置。<br>- 开启事件计费时, 对性能会产生一定影响，误配后影响系统性能，执行命令前请评估对性能的影响。 |
| OFFMETERINGTYPE | 离线计费统计类型 | 可选必选说明：条件可选参数<br>前提条件：该参数在“USAGERPTMODE”配置为“OFFLINE”时为可选参数。<br>参数含义：该参数用于设置离线计费统计类型。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- VOLUME：按流量计费。<br>- TIME：按时长计费。<br>- EVENT：按事件计费。<br>- FREE：免费。表示该URR有计费欺诈风险，需要做计费防欺诈判断。<br>- VOLUME_TIME：按流量和时长计费。<br>- EVENT_VOLUME：按事件和流量计费。<br>- EVENT_TIME：按事件和时长计费。<br>- EVENT_VOLUME_TIME：按事件、流量和时长计费。<br>默认值：VOLUME<br>配置原则：<br>- 如果运营商希望采取计费方式为离线计费，可以配置该参数。要实现流量免费，需要在UNC上一起配合做配置。<br>- 开启事件计费时, 对性能会产生一定影响，误配后影响系统性能，执行命令前请评估对性能的影响。 |
| DFTQUOTAPARA | 默认流量配额的单位 | 可选必选说明：条件可选参数<br>前提条件：该参数在“USAGERPTMODE”配置为“ONLINE”时为可选参数。<br>参数含义：默认流量配额的单位。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- Byte：默认配额单位是字节。<br>- KByte：默认配额单位是千字节。<br>默认值：Byte<br>配置原则：一般可以不配置或者配置Byte，如果需要的配额超过4G则需要配置Kbyte。 |
| CFGDOMAINNAME | 配置域名称 | 可选必选说明：可选参数<br>参数含义：该参数表示命令所属公共配置域的名称。<br>数据来源：本端规划<br>取值范围：字符串类型，输入长度范围为1～31。<br>默认值：无<br>配置原则：无 |
| RG | 计费组 | 可选必选说明：条件可选参数<br>前提条件：该参数在“USAGERPTMODE”配置为“OFFLINE” 或 “ONLINE”时为可选参数。<br>参数含义：该参数用于设置计费组。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0~4294967294。<br>默认值：无<br>配置原则：报表功能开启，软参bit1557开启，则需要配置此参数，同时参数值需要与SMF保持一致，否则报表上报的RG与实际计费的RG不一致。 |
| SID | 业务标识 | 可选必选说明：条件可选参数<br>前提条件：该参数在“USAGERPTMODE”配置为“OFFLINE” 或 “ONLINE”时为可选参数。<br>参数含义：该参数用于设置业务标识。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0~4294967294。<br>默认值：无<br>配置原则：报表功能开启，软参bit1557开启，则需要配置此参数，同时参数值需要与SMF保持一致，否则报表上报的SID与实际计费的SID不一致。 |

## 操作的配置对象

- [URR（URR）](configobject/UDG/20.15.2/URR.md)

## 关联任务

- [0-00001](task/UDG/20.15.2/0-00001.md)

## 使用实例

假如运营商需要增加一个使用量上报规则信息用于在线计费，设置在线计费方式为免费：

```
ADD URR: URRNAME="onlineURR", URRID=1000, USAGERPTMODE=ONLINE, ONLMETERINGTYPE=FREE;
```

## 证据

- 原始手册：`evidence/UDG/20.15.2/增加URR（ADD-URR）_82837629.md`
