---
id: UDG@20.15.2@MMLCommand@MOD FILTERIPV6
type: MMLCommand
name: MOD FILTERIPV6（修改IPv6过滤器）
nf: UDG
version: 20.15.2
verb: MOD
object_keyword: FILTERIPV6
command_category: 配置类
applicable_nf:
- PGW-U
- UPF
effect_mode: ''
is_dangerous: false
category_path:
- 用户面服务管理
- 业务匹配策略
- 业务过滤器管理
- 流过滤器管理
- 过滤器
status: active
---

# MOD FILTERIPV6（修改IPv6过滤器）

## 功能

**适用NF：PGW-U、UPF**

该命令用于修改过滤器，其中包含三、四层的协议、源端口号、目的端口号、源IPv6地址和目的IPv6地址等。数据报文通过时，系统会根据过滤器选择到对应的规则，然后执行选中规则对应的计费策略和动作策略。

## 注意事项

- 该命令执行后需要等待执行SET REFRESHSRV命令（REFRESHTYPE参数设置为USERPROFILE或ALL）刷新后生效。
- 如果引用了该过滤器的流过滤器记录存在，则不允许将其从IPv4型过滤规则直接修改为IPv6型过滤规则。
- 如果引用了该过滤器的访问控制列表记录存在，则不允许将其从IPv4型过滤规则直接修改为IPv6型过滤规则。
- 该命令设定后的数据，需要通过LST FILTER命令进行查看。
- 反掩码举例：网络的默认掩码255.255.255.0，其对应反掩码就是0.0.0.255，在这个例子中，IP地址的最后8位被用作主机地址。

## 权限

G_1，管理员级别命令组；G_2，操作员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| FILTERNAME | 过滤器名字 | 可选必选说明：必选参数<br>参数含义：该参数用于设置过滤器名字。<br>数据来源：本端规划<br>取值范围：字符串类型，输入长度范围为1～63。不支持空格，不区分大小写。<br>默认值：无<br>配置原则：无 |
| L34PROTTYPEV6 | 三四层IPv6协议输入类型 | 可选必选说明：必选参数<br>参数含义：该参数用于设置三四层协议输入类型。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- NUMBER：数字类型。<br>- STRING：字符串类型。<br>默认值：无<br>配置原则：无 |
| L34PROTNUM | 三四层协议数字类型 | 可选必选说明：条件必选参数<br>前提条件：该参数在“L34PROTTYPEV6”配置为“NUMBER”时为必选参数。<br>参数含义：该参数用于设置三四层协议类型数字。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0～255。<br>默认值：无<br>配置原则：<br>- ANY：ANY对应的协议号是0。<br>- ICMP：ICMP对应的协议号是1。<br>- TCP：TCP对应的协议号是6。<br>- UDP：UDP对应的协议号是17。<br>- GRE：GRE对应的协议号是47。 |
| L34PROTOCOL | 三四层协议类型 | 可选必选说明：条件必选参数<br>前提条件：该参数在“L34PROTTYPEV6”配置为“STRING”时为必选参数。<br>参数含义：该参数用于设置三四层协议类型字符串。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- ANY：任意三四层协议。<br>- ICMP：Internet控制报文协议。<br>- TCP：传输控制协议。<br>- UDP：用户数据报协议。<br>- GRE：通用路由封装协议。<br>默认值：无<br>配置原则：<br>- ANY：ANY对应的协议号是0。<br>- ICMP：ICMP对应的协议号是1。<br>- TCP：TCP对应的协议号是6。<br>- UDP：UDP对应的协议号是17。<br>- GRE：GRE对应的协议号是47。 |
| MSIPMODE | 手机IP地址配置模式 | 可选必选说明：可选参数<br>参数含义：该参数用于设置手机IP地址配置模式。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- IP：IP地址类型。<br>- IPLIST：IP地址列表类型。<br>默认值：无<br>配置原则：<br>- 如果运营商希望报文匹配到手机的单个IP地址，则配置该参数为MSIP。<br>- 如果运营商希望报文匹配到手机的多个IP地址，则配置该参数为MSIPLISTNAME。 |
| MSIPV6 | 手机IPv6地址 | 可选必选说明：条件必选参数<br>前提条件：该参数在“MSIPMODE”配置为“IP”时为必选参数。<br>参数含义：该参数用于设置手机IPv6地址。<br>数据来源：本端规划<br>取值范围：IPv6地址类型。<br>默认值：无<br>配置原则：无 |
| MSIPV6MASKTYPE | 手机IPv6地址掩码类型 | 可选必选说明：条件必选参数<br>前提条件：该参数在“MSIPMODE”配置为“IP”时为必选参数。<br>参数含义：该参数用于设置手机IPv6地址掩码类型。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- IPTYPE：反掩码地址类型。<br>- LENGTHTYPE：掩码长度类型。<br>默认值：无<br>配置原则：<br>- 如果运营商希望输入IP掩码类型为IPv6的反掩码，则配置该参数为IPTYPE。<br>- 如果运营商希望输入IP掩码类型为IPv6的掩码长度，则配置该参数为LENGTHTYPE。 |
| MSIPV6MASKLEN | 手机IPv6地址掩码长度 | 可选必选说明：条件必选参数<br>前提条件：该参数在“MSIPV6MASKTYPE”配置为“LENGTHTYPE”时为必选参数。<br>参数含义：该参数用于设置手机IPv6地址掩码长度。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0～128。<br>默认值：无<br>配置原则：无 |
| MSIPV6MASK | 手机IPv6地址反掩码 | 可选必选说明：条件必选参数<br>前提条件：该参数在“MSIPV6MASKTYPE”配置为“IPTYPE”时为必选参数。<br>参数含义：该参数用于设置手机IPv6地址反掩码。<br>数据来源：本端规划<br>取值范围：IPv6地址类型。<br>默认值：无<br>配置原则：反掩码不支持配置为全F，如果希望达到反掩码配置为全F的效果，可以不配置IP和反掩码，或者将掩码长度设置为0。 |
| MSIPLISTNAME | 手机IP列表名称 | 可选必选说明：条件必选参数<br>前提条件：该参数在“MSIPMODE”配置为“IPLIST”时为必选参数。<br>参数含义：该参数用于设置手机IP列表名称。<br>数据来源：本端规划<br>取值范围：字符串类型，输入长度范围为1～31。不支持空格，不区分大小写。<br>默认值：无<br>配置原则：该参数使用ADD IPLIST命令配置生成。 |
| SVRIPMODE | 服务器IP地址配置模式 | 可选必选说明：可选参数<br>参数含义：该参数用于设置服务器IP地址配置模式。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- IP：IP地址类型。<br>- HOSTNAME：Host类型。<br>- IPLIST：IP地址列表类型。<br>默认值：无<br>配置原则：<br>- 如果运营商希望报文匹配到服务器的单个IP地址，则配置该参数为IP。<br>- 如果运营商希望报文匹配到服务器的域名，则配置该参数为HOSTNAME。<br>- 如果运营商希望报文匹配到服务器的多个IP地址，则配置该参数为IPLIST。 |
| SVRIPV6 | 服务器IPv6地址 | 可选必选说明：条件必选参数<br>前提条件：该参数在“SVRIPMODE”配置为“IP”时为必选参数。<br>参数含义：该参数用于设置服务器IPv6地址。<br>数据来源：本端规划<br>取值范围：IPv6地址类型。<br>默认值：无<br>配置原则：无 |
| SVRIPV6MASKTYPE | 服务器IPv6地址掩码类型 | 可选必选说明：条件必选参数<br>前提条件：该参数在“SVRIPMODE”配置为“IP”时为必选参数。<br>参数含义：该参数用于设置服务器IPv6地址掩码类型。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- IPTYPE：反掩码地址类型。<br>- LENGTHTYPE：掩码长度类型。<br>默认值：无<br>配置原则：无 |
| SVRIPV6MASKLEN | 服务器IPv6地址掩码长度 | 可选必选说明：条件必选参数<br>前提条件：该参数在“SVRIPV6MASKTYPE”配置为“LENGTHTYPE”时为必选参数。<br>参数含义：该参数用于设置服务器IPv6地址掩码长度。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0～128。<br>默认值：无<br>配置原则：无 |
| SVRIPV6MASK | 服务器IPv6地址反掩码 | 可选必选说明：条件必选参数<br>前提条件：该参数在“SVRIPV6MASKTYPE”配置为“IPTYPE”时为必选参数。<br>参数含义：该参数用于设置服务器IPv6地址反掩码。<br>数据来源：本端规划<br>取值范围：IPv6地址类型。<br>默认值：无<br>配置原则：反掩码不支持配置为全F，如果希望达到反掩码配置为全F的效果，可以不配置IP和反掩码，或者将掩码长度设置为0。 |
| HOSTNAME | Host配置名称 | 可选必选说明：条件必选参数<br>前提条件：该参数在“SVRIPMODE”配置为“HOSTNAME”时为必选参数。<br>参数含义：该参数用于设置Host配置名称。<br>数据来源：本端规划<br>取值范围：字符串类型，输入长度范围为1～31。不支持空格，不区分大小写。<br>默认值：无<br>配置原则：该参数使用ADD HOST命令配置生成。 |
| SVRIPLISTNAME | 服务器IP列表名称 | 可选必选说明：条件必选参数<br>前提条件：该参数在“SVRIPMODE”配置为“IPLIST”时为必选参数。<br>参数含义：该参数用于设置服务器IP列表名称。<br>数据来源：本端规划<br>取值范围：字符串类型，输入长度范围为1～31。不支持空格，不区分大小写。<br>默认值：无<br>配置原则：该参数使用ADD IPLIST命令配置生成。 |
| MSSTARTPORT | 手机起始端口号 | 可选必选说明：可选参数<br>参数含义：该参数用于设置手机起始端口号。该参数不大于手机结束端口号。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0～65535。<br>默认值：无<br>配置原则：如果运营商希望定义手机侧的端口范围，通过配置该参数来设置起始端口号。 |
| MSENDPORT | 手机结束端口号 | 可选必选说明：可选参数<br>参数含义：该参数用于设置手机结束端口号。该参数不小于手机起始端口号。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0～65535。<br>默认值：无<br>配置原则：如果运营商希望定义手机侧的端口范围，通过配置该参数来设置结束端口号。 |
| SVRSTARTPORT | 服务器起始端口号 | 可选必选说明：可选参数<br>参数含义：该参数用于设置服务器起始端口号。该参数不大于服务器结束端口号。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0～65535。<br>默认值：无<br>配置原则：如果运营商希望定义服务器侧的端口范围，通过配置该参数来设置起始端口号。 |
| SVRENDPORT | 服务器结束端口号 | 可选必选说明：可选参数<br>参数含义：该参数用于设置服务器结束端口号。该参数不小于服务器起始端口号。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0～65535。<br>默认值：无<br>配置原则：如果运营商希望定义服务器侧的端口范围，通过配置该参数来设置结束端口号。 |
| TOSCFGTYPE | Tos配置类型 | 可选必选说明：可选参数<br>参数含义：该参数用于配置TOS类型的配置方式。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- CLASS：使用TOS类别名称配置。<br>- TOS_VALUE：使用TOS值配置。<br>默认值：无<br>配置原则：<br>- TOS配置仅适用于ACL的相关功能，不适用于计费策略和动作策略的相关功能。<br>- CLASS：如果运营商希望设置以TOS类别名称配置TOS类型，则配置该参数。<br>- TOS_VALUE：如果运营商希望设置以TOS类别值配置TOS类型，则配置该参数。 |
| TOSCLASSTYPE | Tos分类类型 | 可选必选说明：条件必选参数<br>前提条件：该参数在“TOSCFGTYPE”配置为“CLASS”时为必选参数。<br>参数含义：该参数用于配置TOS类型业务的类别名。<br>数据来源：本端规划<br>取值范围：枚举类型。<br>- BE：表示remark值为0（0x00）。<br>- CS1：表示remark值为8（0x08）。<br>- AF11：表示remark值为10（0x0a）。<br>- AF12：表示remark值为12（0x0c）。<br>- AF13：表示remark值为14（0x0e）。<br>- CS2：表示remark值为16（0x10）。<br>- AF21：表示remark值为18（0x12）。<br>- AF22：表示remark值为20（0x14）。<br>- AF23：表示remark值为22（0x16）。<br>- CS3：表示remark值为24（0x18）。<br>- AF31：表示remark值为26（0x1a）。<br>- AF32：表示remark值为28（0x1c）。<br>- AF33：表示remark值为30（0x1e）。<br>- CS4：表示remark值为32（0x20）。<br>- AF41：表示remark值为34（0x22）。<br>- AF42：表示remark值为36（0x24）。<br>- AF43：表示remark值为38（0x26）。<br>- CS5：表示remark值为40（0x28）。<br>- EF：表示remark值为46（0x2e）。<br>- CS6：表示remark值为48（0x30）。<br>- CS7：表示remark值为56（0x38）。<br>默认值：无<br>配置原则：无 |
| TOSVALUE | 服务类型 | 可选必选说明：条件必选参数<br>前提条件：该参数在“TOSCFGTYPE”配置为“TOS_VALUE”时为必选参数。<br>参数含义：该参数用于设置服务类型。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0～63。<br>默认值：无<br>配置原则：如果运营商希望报文中携带TOS服务类型的值，则配置该参数。 |
| CFGDOMAINNAME | 配置域名称 | 可选必选说明：可选参数<br>参数含义：该参数表示命令所属公共配置域的名称。<br>数据来源：本端规划<br>取值范围：字符串类型，输入长度范围为1～31。<br>默认值：无<br>配置原则：无 |

## 操作的配置对象

- [[configobject/UDG/20.15.2/FILTERIPV6]] · IPv6过滤器（FILTERIPV6）

## 使用实例

运营商需要修改一个过滤器的三四层协议类型为TCP，并修改手机IP为“2001:db8::1428:0”：

```
MOD FILTERIPV6:FILTERNAME="filter1",L34PROTTYPEV6=STRING,L34PROTOCOL=TCP,MSIPMODE=IP,MSIPV6="2001:db8::1428:0",MSIPV6MASKTYPE=LENGTHTYPE,MSIPV6MASKLEN=128;
```

## 证据

- 原始手册：`evidence/UDG/20.15.2/MOD-FILTERIPV6.md`
