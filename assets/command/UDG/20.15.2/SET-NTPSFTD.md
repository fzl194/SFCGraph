---
id: UDG@20.15.2@MMLCommand@SET NTPSFTD
type: MMLCommand
name: SET NTPSFTD（设置NTP软参）
nf: UDG
version: 20.15.2
verb: SET
object_keyword: NTPSFTD
command_category: 配置类
effect_mode: ''
is_dangerous: false
category_path:
- 平台服务管理
- 系统调测
- NTP软参管理
status: active
---

# SET NTPSFTD（设置NTP软参）

## 功能

![](设置NTP软参(SET NTPSFTD)_50379874.assets/notice_3.0-zh-cn.png)

NTP软参配置属于系统关键配置，修改可能会影响系统时间的准确性，请谨慎操作。

该命令仅在 Full-stack 虚机场景下支持。

该命令为专家维护命令，用于设置NTP软参值。

该命令仅适用于开局阶段调测或者维护的操作场景，当需要设置NTP软参配置信息时，操作员可以使用该命令实现这一目的。

## 注意事项

- 设置手工同步策略为优先从CSP侧外部时钟源同步时间，该设置会在48小时后自动恢复为优先从paas管理面外部时钟源同步时间。

- 当进行“**NTP同步策略**”软参设置（NM=4，CTRL=1）时：
    - 在设置PM0为1时，需要先登录paas管理面portal，确认不存在告警原因为“所配置的NTP服务器不可用或者key认证失败”的“ALM-135901 NTP连接状态异常”活动告警。
    - 在设置PM0为2时，需要先登录OM portal，确认不存在告警原因为“所配置的NTP服务器不可用或者key认证失败”的“ALM-135901 NTP连接状态异常”活动告警。
    - 在设置PM0为3时，需要先登录paas管理面portal，确认不存在告警原因为“所配置的NTP服务器不可用或者key认证失败”的“ALM-135901 NTP连接状态异常”活动告警。
- 该命令在进行“**NTP同步策略**”软参设置时，存在系统初始记录，参数的初始值如下：
  | 手工同步策略 | 自动同步策略 |
  | --- | --- |
  | priority_get_time_from_fst | auto_get_time_from_fst |

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| NM | NTP功能模块 | 可选必选说明：必选参数。<br>参数含义：用于指定需要设置软参功能的模块号。<br>取值范围：0～65535<br>默认值：无。<br>配置原则：无。 |
| CTRL | 控制字 | 可选必选说明：必选参数。<br>参数含义：用于指定本次操作设置的软参控制字。<br>取值范围：0～65535<br>默认值：无。<br>配置原则：无。 |
| PM0 | 参数0 | 可选必选说明：可选参数。<br>参数含义：用于指定软件调试命令下发的参数0。<br>取值范围：1～65535<br>默认值：无。<br>配置原则：该参数在“<br>**NTP同步策略**<br>”软参设置（NM=4，CTRL=1）时有效。 |
| PM1 | 参数1 | 可选必选说明：可选参数。<br>参数含义：用于指定软件调试命令下发的参数1。<br>取值范围：0～65535<br>默认值：无。<br>配置原则：无。 |
| PM2 | 参数2 | 可选必选说明：可选参数。<br>参数含义：用于指定软件调试命令下发的参数2。<br>取值范围：0～65535<br>默认值：无。<br>配置原则：无。 |
| PM3 | 参数3 | 可选必选说明：可选参数。<br>参数含义：用于指定软件调试命令下发的参数3。<br>取值范围：0～65535<br>默认值：无。<br>配置原则：无。 |
| PM4 | 参数4 | 可选必选说明：可选参数。<br>参数含义：用于指定软件调试命令下发的参数4。<br>取值范围：0～65535<br>默认值：无。<br>配置原则：无。 |
| PM5 | 参数5 | 可选必选说明：可选参数。<br>参数含义：用于指定软件调试命令下发的参数5。<br>取值范围：0～65535<br>默认值：无。<br>配置原则：无。 |
| PM6 | 参数6 | 可选必选说明：可选参数。<br>参数含义：用于指定软件调试命令下发的参数6。<br>取值范围：长度不超过127个英文字符，63个中文字符。<br>默认值：无。<br>配置原则：无。 |
| PM7 | 参数7 | 可选必选说明：可选参数。<br>参数含义：用于指定软件调试命令下发的参数7。<br>取值范围：长度不超过127个英文字符，63个中文字符。<br>默认值：无。<br>配置原则：无。 |

## 操作的配置对象

- [NTP软参（NTPSFTD）](configobject/UDG/20.15.2/NTPSFTD.md)

## 使用实例

1. 设置时间同步策略，设置手工同步策略为优先从paas管理面外部时钟源同步时间。
  ```
  %%SET NTPSFTD: NM=4, CTRL=1, PM0=1;%%
  RETCODE = 0  操作成功

  ---    END
  ```
2. 设置时间同步策略，设置手工同步策略为优先从CSP侧外部时钟源同步时间。
  ```
  %%SET NTPSFTD: NM=4, CTRL=1, PM0=2;%%
  RETCODE = 0  操作成功

  ---    END
  ```
3. 设置时间同步策略，设置手工同步策略强制从paas管理面外部时钟源同步时间。
  ```
  %%SET NTPSFTD: NM=4, CTRL=1, PM0=3;%%
  RETCODE = 0  操作成功

  ---    END
  ```

## 证据

- 原始手册：`evidence/UDG/20.15.2/设置NTP软参(SET-NTPSFTD)_50379874.md`
