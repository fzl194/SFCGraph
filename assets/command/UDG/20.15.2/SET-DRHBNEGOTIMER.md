---
id: UDG@20.15.2@MMLCommand@SET DRHBNEGOTIMER
type: MMLCommand
name: SET DRHBNEGOTIMER（设置容灾定时器）
nf: UDG
version: 20.15.2
verb: SET
object_keyword: DRHBNEGOTIMER
command_category: 配置类
effect_mode: ''
is_dangerous: false
category_path:
- 平台服务管理
- 可靠性管理
- 微服务可靠性管理
status: active
---

# SET DRHBNEGOTIMER（设置容灾定时器）

## 功能

该命令用于配置容灾组中网元间发送心跳、协商消息的间隔时间及次数。

> **说明**
> - 该命令执行后立即生效。
>
> - 该命令只用于在UEG-L/UEN网元采用主备（冷备）容灾模式下执行。
> - 该命令只用于在UEG-M/UEG网元采用主备（热备）容灾模式下执行。
> - 在容灾运行期间执行命令，需要参考容灾配置指导书，避免主备容灾协商失败。
> - 在容灾组中的两个网元中配置容灾定时器时，参数应保持一致。
> - 在冷备容灾模式下，心跳超时时间的最小时间建议设置为6s。
> - 使用[**LST DRHBNEGOTIMER**](查询容灾定时器（LST DRHBNEGOTIMER）_23235162.md)查询容灾定时器时，只有当前容灾组的容灾定时器会被显示。
>
> - 系统部署完成后，已经存在初始记录，参数的初始记录值如下表：
>
> | DRGROUPID | HELLOINTERVAL | NEGOINTERVAL | TIMES | OMHBTIMES | OMHBINTERVAL |
> | --- | --- | --- | --- | --- | --- |
> | 1 | 10 | 10 | 30 | 3 | 10 |
> | 2 | 10 | 10 | 30 | 3 | 10 |
> | 3 | 10 | 10 | 30 | 3 | 10 |
> | 4 | 10 | 10 | 30 | 3 | 10 |
> | 5 | 10 | 10 | 30 | 3 | 10 |
> | 6 | 10 | 10 | 30 | 3 | 10 |
> | 7 | 10 | 10 | 30 | 3 | 10 |
> | 8 | 10 | 10 | 30 | 3 | 10 |

## 权限

G_1，管理员级别命令组；G_2，操作员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| DRGROUPID | 容灾组标识 | 可选必选说明：必选参数<br>参数含义：该参数用来标识一个容灾组，该标识必须存在于容灾组信息中，容灾组信息可以使用<br>[**LST DRGROUPINFO**](查询容灾组信息（LST DRGROUPINFO）_74835153.md)<br>命令查询。<br>数据来源：全网规划<br>取值范围：整数类型，取值范围是1~8。<br>默认值：无。<br>配置原则：无 |
| HELLOINTERVAL | 心跳消息传递间隔(100毫秒) | 可选必选说明：可选参数<br>参数含义：该参数用于配置容灾组内网元间发送心跳消息的间隔时间。<br>数据来源：全网规划<br>取值范围：整数类型，取值范围是1~100。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRHBNEGOTIMER查询当前参数配置值。<br>配置原则：无 |
| NEGOINTERVAL | 协商消息传递间隔(100毫秒) | 可选必选说明：可选参数<br>参数含义：该参数用于配置容灾组内网元间发送协商消息的间隔时间。<br>数据来源：对端协商<br>取值范围：整数类型，取值范围是1~100。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRHBNEGOTIMER查询当前参数配置值。<br>配置原则：无 |
| TIMES | 重传次数 | 可选必选说明：可选参数<br>参数含义：该参数用于配置容灾组内网元间发送心跳和协商消息的重传次数。<br>数据来源：全网规划<br>取值范围：整数类型，取值范围是1~60，单位是次。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRHBNEGOTIMER查询当前参数配置值。<br>配置原则：无 |
| OMHBTIMES | OM通道心跳重传次数 | 可选必选说明：可选参数<br>参数含义：该参数用于配置容灾组内OM通道发送心跳的重传次数。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~30。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRHBNEGOTIMER查询当前参数配置值。<br>配置原则：无 |
| OMHBINTERVAL | OM通道心跳探测时长(100毫秒) | 可选必选说明：可选参数<br>参数含义：该参数用于配置容灾组OM通道发送心跳消息的间隔时间。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~100。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRHBNEGOTIMER查询当前参数配置值。<br>配置原则：无 |

## 操作的配置对象

- [[configobject/UDG/20.15.2/DRHBNEGOTIMER]] · 容灾定时器（DRHBNEGOTIMER）

## 使用实例

设置容灾组标识为1的心跳重传间隔为1秒，协商消息重传的间隔为2秒，重传次数为3次:

```
%%SET DRHBNEGOTIMER: DRGROUPID=1, HELLOINTERVAL=10, NEGOINTERVAL=20, TIMES=3;%%
RETCODE = 0  操作成功

---    END
```

## 证据

- 原始手册：`evidence/UDG/20.15.2/设置容灾定时器（SET-DRHBNEGOTIMER）_23714710.md`
