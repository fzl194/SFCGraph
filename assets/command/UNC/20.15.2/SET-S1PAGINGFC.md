---
id: UNC@20.15.2@MMLCommand@SET S1PAGINGFC
type: MMLCommand
name: SET S1PAGINGFC（设置S1寻呼流控参数）
nf: UNC
version: 20.15.2
verb: SET
object_keyword: S1PAGINGFC
command_category: 配置类
applicable_nf:
- MME
effect_mode: 立即生效
is_dangerous: false
category_path:
- 业务服务管理
- Pre 5G接入业务管理
- 控制面管理
- 操作维护
- 设备管理
- 流控管理
- 业务流控管理
- S1寻呼流控管理
status: active
---

# SET S1PAGINGFC（设置S1寻呼流控参数）

## 功能

![](设置S1寻呼流控参数(SET S1PAGINGFC)_26146156.assets/notice_3.0-zh-cn_2.png)

如果S1寻呼流控功能被关闭或者相关流控参数设置不正确，可能影响S1寻呼流控的效果。

**适用网元：MME**

该命令用于设置S1模式寻呼流控功能的相关参数。

S1模式寻呼流控功能会对触发S1模式寻呼的消息（下面简称“寻呼”）处理速率进行控制，这些消息包括：从S-GW收到的Downlink Data Notification、Delete Bearer Request、Update Bearer Request、Create Bearer Request消息，从SGs接口收到的SGsAP Paging Request消息。

对寻呼的控制主要有如下两个场景：突发预防和拥塞控制。

- 突发预防：系统正常运行时，UNC会学习最近3天的寻呼速率历史峰值，用于自动调整当前系统允许处理的寻呼速率和增长速率；在学习过程中可能会有少量寻呼消息被丢弃；在寻呼高倍突发时，本功能可以有效缓解其对系统的冲击，避免系统进入重度拥塞。
- 拥塞控制：当出S1接口的UNC的包转发速率、Fabric通道流量拥塞或者UNC的CPU过载（下面简称“拥塞条件”）时，系统会进入寻呼流控状态，自动调整并控制系统允许处理的寻呼速率，保证系统过载情况下业务的正常进行，避免造成整个系统瘫痪。

S1模式寻呼流控过程有三种状态：正常状态、流控状态和恢复状态。流控状态为满足拥塞条件的起控状态；恢复状态为系统处于流控状态下，不满足拥塞条件时，系统进入的状态；正常状态为系统初始状态，或者恢复状态下连续一段时间没有消息丢弃而进入的状态。

## 注意事项

- 系统初次上电运行时，会执行系统初始设置值。
- 该命令执行后立即生效。
- S1模式寻呼流控功能在UNC架构生效。
- 如果S1寻呼流控功能被关闭或者相关流控参数设置不正确，可能影响S1寻呼流控的效果。

## 权限

manage-ug；system-ug
G_1，管理员级别命令组；G_2，操作员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| FCSWITCH | S1寻呼流控功能开关 | 可选必选说明：可选参数<br>参数含义：该参数用于设置是否开启S1模式寻呼流控功能。<br>数据来源：整网规划<br>取值范围：<br>- “ON(开启)”<br>- “OFF(关闭)”<br>系统初始设置值：<br>“ON(开启)” |
| SGSSWITCH | SGs寻呼流控开关 | 可选必选说明：条件可选参数<br>参数含义：该参数用于设置在S1模式寻呼流控功能开启时，是否对SGs接口触发的寻呼消息（Paging Request）进行流控。<br>前提条件: 该参数在<br>“S1寻呼流控功能开关(FCSWITCH)”<br>参数配置为<br>“ON(开启)”<br>时生效。<br>数据来源：整网规划<br>取值范围：<br>- “ON(开启)”<br>- “OFF(关闭)”<br>系统初始设置值：<br>“ON(开启)” |
| S102SWITCH | S102寻呼流控开关 | 可选必选说明：条件可选参数<br>参数含义：该参数用于设置在S1模式寻呼流控功能开启时，是否对S102接口触发的寻呼消息（Paging Request）进行流控。<br>前提条件：该参数在<br>“S1寻呼流控功能开关(FCSWITCH)”<br>参数配置为<br>“ON(开启)”<br>时生效。<br>数据来源：整网规划<br>取值范围：<br>- “ON(开启)”<br>- “OFF(关闭)”<br>系统初始设置值：<br>“ON(开启)” |
| PAGINGRESENDSWITCH | 寻呼重发控制开关 | 可选必选说明：条件可选参数<br>参数含义：该参数用于控制寻呼流控功能开启时，是否对S1寻呼请求的重复发送功能进行控制。<br>前提条件：该参数在<br>“S1寻呼流控功能开关(FCSWITCH)”<br>参数配置为<br>“ON(开启)”<br>时生效。<br>数据来源：整网规划<br>取值范围：<br>- “ON(开启)”<br>- “OFF(关闭)”<br>系统初始设置值：<br>“ON(开启)”<br>说明：- 本参数设置为“ON(开启)”后，系统在正常运行时S1模式寻呼重发策略不改变；系统进入S1模式寻呼流控状态后，不重发S1模式寻呼消息；系统进入S1模式寻呼流控恢复状态后，最多再重发一次S1模式寻呼消息。<br>- 本参数设置为“OFF(关闭)”后，S1模式寻呼重发策略不改变。 |
| ANTIBURSTSWITCH | 寻呼突发预防功能开关 | 可选必选说明：条件可选参数<br>参数含义：该参数用于设置在系统正常运行时是否开启寻呼突发预防功能。<br>前提条件: 该参数在<br>“S1寻呼流控功能开关(FCSWITCH)”<br>参数配置为<br>“ON(开启)”<br>时生效。<br>数据来源：整网规划<br>取值范围：<br>- “ON(开启)”<br>- “OFF(关闭)”<br>系统初始设置值：<br>“ON(开启)”<br>说明：- 本参数设置为“ON(开启)”后，系统会学习最近3天正常运行时的寻呼速率历史峰值，用于控制当前接入系统的寻呼速率；在学习过程中可能会有少量触发S1模式寻呼的消息被丢弃；在寻呼高倍突发时可以有效缓解其对系统的冲击，避免系统进入重度拥塞。<br>- 本参数设置为“OFF(关闭)”后，系统在正常运行时对寻呼速率不进行控制。<br>- 推荐值为“ON(开启)”，如果要设置为“OFF(关闭)”，请联系华为技术支持，评估是否相应调整“寻呼流控速率门限(FCTHD)”。 |
| FCTHD | 寻呼流控速率门限（次/秒） | 可选必选说明：条件可选参数<br>参数含义：该参数用于设置应对寻呼突发的流控速率门限。<br>前提条件: 该参数在<br>“S1寻呼流控功能开关(FCSWITCH)”<br>参数配置为<br>“ON(开启)”<br>时生效。<br>数据来源：整网规划<br>取值范围：10～255<br>系统初始设置值：150<br>说明：- 如果“寻呼突发预防功能开关(ANTIBURSTSWITCH)”关闭，系统进入寻呼流控时，无论之前的寻呼速率多大，都会立即将寻呼速率控制在本参数设置的门限以下，此后根据所处状态自动进行控制速率的调整。<br>- 如果“寻呼突发预防功能开关(ANTIBURSTSWITCH)”打开，在正常运行时，会根据最近3天学习的寻呼速率历史峰值和本参数设置的门限的较小值，来控制当前接入系统的寻呼速率；输入的寻呼速率超过历史峰值和本参数设置的门限的较小值时，如果系统未进入寻呼流控，控制速率只能缓慢增长，否则降低控制速率。<br>- 本参数初始值为150次/秒，相当于每个SPP进程承载30000个EPC用户、每用户每小时18次寻呼的业务量，为相对较高的值，在“寻呼突发预防功能开关(ANTIBURSTSWITCH)”打开时不建议调整。<br>- 本参数配置的越小，则应对突发的自保能力越好，但是如果小于实际能力过多，可能在系统能容忍的有限的突发下发生过多的流控丢包。 |
| FCTHDMIN | 寻呼流控速率最小值（次/秒） | 可选必选说明：条件可选参数<br>参数含义：该参数用于设置触发S1模式寻呼的消息处理速率的最小值。<br>前提条件: 该参数在<br>“S1寻呼流控功能开关(FCSWITCH)”<br>参数配置为<br>“ON(开启)”<br>时生效。<br>数据来源：整网规划<br>取值范围：0～100<br>系统初始设置值：10<br>配置原则：本参数的取值不应超过<br>“寻呼流控速率门限(FCTHD)”<br>。 |
| RSVCAP | 寻呼预留处理量 | 可选必选说明：条件可选参数<br>参数含义：该参数用于设置给触发寻呼的语音和信令消息预留的每秒处理能力。<br>前提条件: 该参数在<br>“S1寻呼流控功能开关(FCSWITCH)”<br>参数配置为<br>“ON(开启)”<br>时生效。<br>数据来源：整网规划<br>取值范围：0～50<br>系统初始设置值：15<br>说明：本参数值包含了两部分：一部分是预留给语音和信令消息共用的处理能力，一部分是仅预留给语音消息的处理能力（由参数<br>“语音寻呼预留处理量(RSVVOCAP)”<br>配置）； 触发寻呼的语音消息包括从SGs接口收到的CS call触发的SGsAP Paging Request消息、从S-GW收到的IMS语音相关的Downlink Data Notification消息、Update Bearer Request消息和Create Bearer Request消息、从HSS收到的Insert Subscription Data Request消息触发的寻呼请求消息；触发寻呼的信令消息包括从S-GW收到的Delete Bearer Request消息、非IMS语音的Update Bearer Request消息和Create Bearer Request消息。 |
| RSVVOCAP | 语音寻呼预留处理量 | 可选必选说明：条件可选参数<br>参数含义：该参数用于设置给触发寻呼的语音消息预留的每秒处理能力。<br>前提条件: 该参数在<br>“S1寻呼流控功能开关(FCSWITCH)”<br>参数配置为<br>“ON(开启)”<br>时生效。<br>数据来源：整网规划<br>取值范围：0～50<br>系统初始设置值：5<br>配置原则：本参数的取值不应超过<br>“寻呼预留处理量(RSVCAP)”<br>。 |
| PAEFCSWITCH | PAE反压流控功能开关 | 可选必选说明：条件可选参数<br>参数含义：该参数用于设置是否开启基于link-pod的PAE反压流控功能。当过载的link-pod超过<br>[SET REVERSEFCPARA](../PAE寻呼反压流控管理/设置PAE寻呼反压流控启动_恢复阈值(SET REVERSEFCPARA)_58751785.md)<br>命令配置的阈值，触发PAE反压流控。<br>数据来源：整网规划<br>取值范围：<br>- “ON(开启)”<br>- “OFF(关闭)”<br>系统初始设置值：<br>“ON(开启)” |

## 操作的配置对象

- [[configobject/UNC/20.15.2/S1PAGINGFC]] · S1寻呼流控参数（S1PAGINGFC）

## 使用实例

1. 设置“S1寻呼流控功能开关(FCSWITCH)”参数为“ON(开启)”、“寻呼重发控制开关(PAGINGRESENDSWITCH)”参数为“OFF(关闭)”、“寻呼预留处理量(RSVCAP)”参数为20，配置格式如下：SET S1PAGINGFC: FCSWITCH=ON, PAGINGRESENDSWITCH=OFF, RSVCAP=20;
2. 设置“S1寻呼流控功能开关(FCSWITCH)”参数为“OFF(关闭)”：SET S1PAGINGFC: FCSWITCH=OFF;

## 证据

- 原始手册：`evidence/UNC/20.15.2/设置S1寻呼流控参数(SET-S1PAGINGFC)_26146156.md`
