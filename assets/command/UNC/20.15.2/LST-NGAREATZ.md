---
id: UNC@20.15.2@MMLCommand@LST NGAREATZ
type: MMLCommand
name: LST NGAREATZ（查询5G区域时区参数）
nf: UNC
version: 20.15.2
verb: LST
object_keyword: NGAREATZ
command_category: 查询类
applicable_nf:
- AMF
effect_mode: ''
is_dangerous: false
category_path:
- 业务服务管理
- 5G接入业务管理
- 移动性管理
- 多时区管理
- 5G区域时区参数
status: active
---

# LST NGAREATZ（查询5G区域时区参数）

## 功能

**适用NF：AMF**

此命令用于查询5G区域时区记录。

## 注意事项

无

## 权限

G_1，管理员级别命令组；G_2，操作员级别命令组；G_3，用户级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| MCC | MCC | 可选必选说明：可选参数<br>参数含义：该参数用于指定移动国家代码。<br>数据来源：全网规划<br>取值范围：字符串类型，输入长度是3。3位的十进制数字。<br>默认值：无<br>配置原则：无 |
| MNC | MNC | 可选必选说明：可选参数<br>参数含义：该参数用于指定移动网号。<br>数据来源：全网规划<br>取值范围：字符串类型，输入长度范围是2~3。2～3位的十进制数字。<br>默认值：无<br>配置原则：无 |
| BGNTAC | 跟踪区编码起始值 | 可选必选说明：可选参数<br>参数含义：该参数用于表示跟踪区编码的起始值。<br>数据来源：全网规划<br>取值范围：字符串类型，输入长度是6。字符串类型，输入长度是6。TAC编码为16进制数，按照字符串格式输入，字符串长度为6，只能由数字（0-9），字母（A-F、a-f）组成。字母大小写不敏感。<br>默认值：无<br>配置原则：无 |
| ENDTAC | 跟踪区编码结束值 | 可选必选说明：可选参数<br>参数含义：该参数用于表示跟踪区编码的结束值。<br>数据来源：全网规划<br>取值范围：字符串类型，输入长度是6。字符串类型，输入长度是6。TAC编码为16进制数，按照字符串格式输入，字符串长度为6，只能由数字（0-9），字母（A-F、a-f）组成。字母大小写不敏感。ENDTAC若不下发，则赋值为BGNTAC值。<br>默认值：无<br>配置原则：无 |
| TZID | 时区标识 | 可选必选说明：可选参数<br>参数含义：该参数用于指定该区域的时区标识。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~24。该参数需要从ADD NGTZLST命令中已配置的TZID参数中取值。<br>默认值：无<br>配置原则：无 |

## 操作的配置对象

- [[UNC@20.15.2@ConfigObject@NGAREATZ]] · 5G区域时区参数（NGAREATZ）

## 使用实例

- 查询一条“移动国家代码”为“460”，“移动网号”为“03”，“跟踪区编码起始值”为“000200”的5G区域时区记录，执行如下命令：
  ```
  %%LST NGAREATZ: MCC="460", MNC="03", BGNTAC="000112";%%
  RETCODE = 0  操作成功。

  输出结果如下
  --------------
      移动国家代码  =  460
          移动网号  =  03
  跟踪区编码起始值  =  000112
  跟踪区编码结束值  =  000350
          时区标识  =  2
  (结果个数 = 1)

  ---    END
  ```
- 查询所有5G区域时区记录，执行如下命令：
  ```
  %%LST NGAREATZ:;%%
  RETCODE = 0  操作成功。

  输出结果如下
  --------------
   移动国家代码  移动网号  跟踪区编码起始值  跟踪区编码结束值  时区标识

   460           01        000360            000460            1       
   460           03        000112            000350            2       
  (结果个数 = 2)

  ---    END
  ```

## 证据

- 原始手册：`evidence/UNC/20.15.2/LST-NGAREATZ.md`
