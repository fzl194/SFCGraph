---
id: UNC@20.15.2@MMLCommand@SET TLBGLBCONF
type: MMLCommand
name: SET TLBGLBCONF（设置TLB全局配置）
nf: UNC
version: 20.15.2
verb: SET
object_keyword: TLBGLBCONF
command_category: 配置类
effect_mode: 立即生效
is_dangerous: false
category_path:
- 平台服务管理
- HTTP功能管理
- HTTP管理
- HTTP服务端负载管理
- 整系统负载管理
- 全局属性
status: active
---

# SET TLBGLBCONF（设置TLB全局配置）

## 功能

![](设置TLB全局配置（SET TLBGLBCONF）_69954926.assets/notice_3.0-zh-cn_2.png)

该命令中TLBGLBSW参数用于设置TLB全局功能开关，开启TLB功能后会改变内部处理TCP SYN包的流程，影响建链性能。

用于设置HTTP服务端TCP链路整系统负载均衡（TLB）的功能开关及相关参数。

## 注意事项

- 该命令执行后立即生效。

- 此命令中的TLBGLBSW开关配置与HTTPSRVLBSW（HTTP服务端负载均衡功能开关）中的LBFUNSWITCH开关不能同时打开，若期望打开TLB开关则请先执行[**LST HTTPSRVLBSW**](../../POD内负载管理/查询HTTP服务端负载重均衡功能（LST HTTPSRVLBSW）_29053331.md)命令确认LBFUNSWITCH配置为OFF。
- 适用场景：1、当服务端链路在HTTP进程上分布不均匀导致HTTP进程CPU不均匀问题时，可以通过打开TLB开关解决，以避免达到性能瓶颈而不满足服务规格。2、当不同对端IP间的链路流量不均匀，同一对端IP下的链路流量均匀时，需要打开TLB开关后并重建服务端链路以解决HTTP进程CPU不均匀问题。3、当同一对端IP下的链路流量也不均匀时，无法通过开启TLB开关解决HTTP进程CPU不均匀问题。
- 操作风险：1、打开TLB开关后会引起可靠性的弱化。SBILINK、LBC或SDRC主实例复位期间若TLB开关处于打开状态，将导致无法新建服务端链路，如果此时服务端链路发生异常断链，会导致业务受损。2、TLB五元组策略系统规格为80000，TLBGLBSW开关开启后最多可能占用80000个CSLB策略资源。CSLB在mini模板配置下策略资源规格为100000，normal模板配置下策略资源规格为200000。[**DSP POLICYNUM**](../../../../../CSLB功能管理/业务管理/服务管理/策略/查询策略数量（DSP POLICYNUM）_29627053.md)命令可以查询策略资源的已占用情况，[**DSP DBGSTAT**](../../../../../CSLB功能管理/操作维护/系统调测/公共调测/调试信息/查询调试信息（DSP DBGSTAT）_29627109.md)命令可以查询策略资源的预申请情况。当策略资源占用满时，无法下发CSLB策略，导致系统异常，业务受损。开启TLBGLBSW开关前需要排查系统是否存在“ALM-82420 CSLB负载均衡资源使用率超过阈值”告警，并通过上述命令评估策略资源占用是否有可能超过CSLB策略资源规格，如有此类情况，不允许打开TLBGLBSW开关。无法准确评估时，请联系华为技术支持。
- 不支持NP场景。当前NP场景下打开TLB开关会导致业务受损。

- 系统部署完成后，已经存在初始记录，参数的初始记录值如下表：

| TLBGLBSW | LBTYPE | RELBDEVTHD | RELBMINLINK | RELBMINITR | TLBRCVSYNTHD | RPTDTSYNTHD | TMPPLYPERIOD | TUPLECHKSW | TUPLECHKPRD | TUPLEAGETHD | TLBTBETHD | TLBHALTTHD | TLBRESTORETHD | SYNSENDDELAY |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| OFF | LINKNUMCTRL | 10 | 5 | 30 | 2000 | 200 | 5 | ON | 30 | 5 | 1000 | 95 | 85 | 20 |

## 权限

G_1，管理员级别命令组；G_2，操作员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| TLBGLBSW | TLB全局开关 | 可选必选说明：可选参数<br>参数含义：该参数用于设置TLB功能全局开关。如果打开开关，则控制所有HTTP服务端TCP链路的分布，使所有HTTP进程间负载均衡。<br>数据来源：本端规划<br>取值范围：<br>- ON（打开）<br>- OFF（关闭）<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| LBTYPE | 均衡模式 | 可选必选说明：可选参数<br>参数含义：该参数用于指定均衡控制模式。<br>数据来源：本端规划<br>取值范围：<br>- LINKNUMCTRL（链路数控制）<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| RELBDEVTHD | 重均衡偏差阈值（%） | 可选必选说明：可选参数<br>参数含义：该参数用于设置服务端链路重均衡的负载偏差门限值。如果某个HTTP进程的负载超过平均负载的百分比数大于或等于该参数的值，则记录该进程满足重均衡的一个条件。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是5~50。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| RELBMINLINK | 重均衡最小链路数阈值 | 可选必选说明：该参数在"LBTYPE"配置为"LINKNUMCTRL"时为条件可选参数。<br>参数含义：该参数用于设置服务端链路重均衡的最小链路数的门限值。如果某个HTTP进程的链路数大于该参数的值，则记录该进程满足重均衡的一个条件。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~1024。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| RELBMINITR | 重均衡最小时间间隔（秒） | 可选必选说明：可选参数<br>参数含义：该参数用于设置服务端链路重均衡的最小时间间隔。启动重均衡检测时，如果距离上一次均衡调整结束的时长未达到该参数设置的最小间隔时长，则不启动重均衡。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是10~600。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| TLBRCVSYNTHD | 接收TCP SYN报文的流控阈值（包/秒） | 可选必选说明：可选参数<br>参数含义：该参数用于设置TLB控制器接收TCP SYN报文的速率流控阈值。TCP SYN报文数量过大，可能导致系统负荷过高，无法接收正常的TCP连接请求。设置该参数可以预防开启TLB功能后的TCP SYN Flood攻击，保护系统正常运行。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是100~5000。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| RPTDTSYNTHD | 上报直通TCP SYN报文的流控阈值（包/秒） | 可选必选说明：可选参数<br>参数含义：该参数用于设置HTTP服务上报非预期的TCP SYN报文的速率流控阈值。对于开启负载均衡控制的链路，如果TCP SYN报文直接发到HTTP服务，则认为是非预期的TCP SYN报文，上报给TLB控制器处理，并且按照该参数的值限制上报的报文速率。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是50~500。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| TMPPLYPERIOD | 临时五元组策略有效期（秒） | 可选必选说明：可选参数<br>参数含义：该参数用于设置服务端链路五元组临时策略的有效期。TLB控制器接收到TCP SYN后，生成五元组的临时策略，如果在该参数设置的时长内未成功建链，则删除临时策略。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是3~60。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| TUPLECHKSW | 五元组核查开关 | 可选必选说明：可选参数<br>参数含义：该参数用于设置服务端链路五元组信息核查开关。<br>数据来源：本端规划<br>取值范围：<br>- ON（打开）<br>- OFF（关闭）<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| TUPLECHKPRD | 五元组核查周期（分钟） | 可选必选说明：该参数在"TUPLECHKSW"配置为"ON"时为条件可选参数。<br>参数含义：该参数用于设置服务端链路五元组信息核查周期时长。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是5~720。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| TUPLEAGETHD | 五元组老化阈值 | 可选必选说明：该参数在"TUPLECHKSW"配置为"ON"时为条件可选参数。<br>参数含义：该参数用于设置服务端链路五元组信息老化阈值。TLB控制器周期核查5元组与物理链路的一致性，如果连续多个周期都没有查到五元组的物理链路信息，且周期数超过该参数设置的值，则删除五元组信息。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是3~24。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| TLBTBETHD | TLB表项流控阈值（个/秒） | 可选必选说明：可选参数<br>参数含义：该参数用于设置TLB策略表项操作速率的流控阈值。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是500~2000。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |
| TLBHALTTHD | 暂停TLB功能的阈值（%） | 可选必选说明：可选参数<br>参数含义：该参数用于设置暂停TLB功能的五元组占用率阈值，当五元组数量占用系统规格的比例超过该阈值后，暂停TLB的功能，即不做链路均衡控制。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是10~100。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：<br>该值必须大于“恢复TLB功能的阈值”。 |
| TLBRESTORETHD | 恢复TLB功能的阈值（%） | 可选必选说明：可选参数<br>参数含义：该参数用于设置恢复TLB功能的五元组占用率阈值，当五元组数量占用系统规格的比例低于该阈值后，恢复TLB的功能。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~100。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：<br>该值必须小于“暂停TLB功能的阈值”。 |
| SYNSENDDELAY | SYN包转发时延（毫秒） | 可选必选说明：可选参数<br>参数含义：当参数“TLB全局开关”打开时，该参数用于设置服务端链路建链过程中SYN包的转发延迟时间，以保障建链时TLB策略已生效，避免建链失败。在NP场景下建议配置为50毫秒。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是0~500，单位是毫秒。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST TLBGLBCONF查询当前参数配置值。<br>配置原则：无 |

## 操作的配置对象

- [TLB全局配置（TLBGLBCONF）](configobject/UNC/20.15.2/TLBGLBCONF.md)

## 使用实例

如果要把TLB的开关打开，可以执行如下命令：

```
SET TLBGLBCONF: TLBGLBSW=ON;
```

## 证据

- 原始手册：`evidence/UNC/20.15.2/设置TLB全局配置（SET-TLBGLBCONF）_69954926.md`
