---
id: UNC@20.15.2@MMLCommand@RMV NGPEIPSPLCY
type: MMLCommand
name: RMV NGPEIPSPLCY（删除基于分组的智能寻呼策略）
nf: UNC
version: 20.15.2
verb: RMV
object_keyword: NGPEIPSPLCY
command_category: 配置类
applicable_nf:
- AMF
effect_mode: ''
is_dangerous: false
category_path:
- 业务服务管理
- 接口管理
- N2接口管理
- NGAP接口寻呼管理
- 智能寻呼策略
status: active
---

# RMV NGPEIPSPLCY（删除基于分组的智能寻呼策略）

## 功能

**适用NF：AMF**

该命令用于删除基于分组的智能寻呼策略。

## 注意事项

下一次移动性流程生效。

## 权限

G_1，管理员级别命令组；G_2，操作员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| SUBRANGE | 用户范围 | 可选必选说明：必选参数<br>参数含义：该参数用于指定寻呼分组标识下发的用户范围。<br>数据来源：全网规划<br>取值范围：<br>- “ALL_USER（所有用户）”：所有用户。<br>- “HOME_USER（本网用户）”：本网用户。<br>- “FOREIGN_USER（外网用户）”：外网用户。<br>- “IMSI_PREFIX（IMSI前缀）”：IMSI前缀。<br>- “SPECIFIC_IMSI（指定IMSI）”：指定IMSI。<br>默认值：无<br>配置原则：无 |
| IMSIPRE | IMSI前缀 | 可选必选说明：该参数在"SUBRANGE"配置为"IMSI_PREFIX"时为条件必选参数。<br>参数含义：该参数用于指定应用基于分组的智能寻呼策略的用户IMSI前缀。<br>数据来源：全网规划<br>取值范围：字符串类型，输入长度范围是5~14。只允许输入十进制数字（0-9）。<br>默认值：无<br>配置原则：无 |
| IMSI | IMSI | 可选必选说明：该参数在"SUBRANGE"配置为"SPECIFIC_IMSI"时为条件必选参数。<br>参数含义：该参数用于指定应用基于分组的智能寻呼策略的用户IMSI（完整IMSI）。<br>数据来源：全网规划<br>取值范围：字符串类型，输入长度范围是14~15。只允许输入十进制数字（0-9）。<br>默认值：无<br>配置原则：无 |
| FEATURECOND | 特征条件组合 | 可选必选说明：必选参数<br>参数含义：该参数用于指示特征条件的组合。<br>数据来源：全网规划<br>取值范围：<br>- “NONE（不指定特征条件）”：不指定特征条件。<br>- “DNN（仅指定DNN条件）”：仅指定DNN条件。<br>- “NSSAI（仅指定切片条件）”：仅指定切片条件。<br>- “DNN_NSSAI（仅指定DNN和切片条件）”：仅指定DNN和切片条件。<br>- “PAGINGPRO（仅指定寻呼概率条件）”：仅指定寻呼概率条件。<br>默认值：无<br>配置原则：无 |
| DNN | DNN | 可选必选说明：该参数在"FEATURECOND"配置为"DNN_NSSAI"、"DNN"时为条件必选参数。<br>参数含义：该参数用于指定需要进行智能寻呼用户的DNN。<br>数据来源：本端规划<br>取值范围：字符串类型，输入长度范围是1~63。只能由“-”、数字、大小写字母和“.”组成，不能以“.”开头且不能出现连续两个“.”。不支持空格及“_”、“#”、“$”、“&”、“%”、“^”、“（”、“）”、“，”、“/”、“;”、“:”、“””、“`”等特殊字符，不区分大小写。<br>默认值：无<br>配置原则：<br>该参数来源于SMF上已通过ADD APN配置的DNN。 |
| SST | 切片业务类型 | 可选必选说明：该参数在"FEATURECOND"配置为"DNN_NSSAI"、"NSSAI"时为条件必选参数。<br>参数含义：该参数用于指定需要进行智能寻呼用户的签约切片业务类型特征。如eMBB（1）、URLLC（2）、MIoT（3）等协议定义的标准SST，或者运营商自定义的SST。<br>数据来源：全网规划<br>取值范围：整数类型，取值范围是0~255。<br>默认值：无<br>配置原则：<br>该参数来源于AMF上已通过ADD PLMNNS配置的切片。<br>该值若不设置，默认值为65535。 |
| SD | 切片细分标识 | 可选必选说明：该参数在"FEATURECOND"配置为"DNN_NSSAI"、"NSSAI"时为条件可选参数。<br>参数含义：该参数用于指定需要进行智能寻呼用户的签约切片细分标识特征。<br>数据来源：全网规划<br>取值范围：字符串类型，输入长度是6。采用十六进制表示（无须输入“0x”前缀），只能由数字（0-9），字母（A-F、a-f）组成。字母大小写不敏感。<br>默认值：无<br>配置原则：<br>该参数来源于AMF上已通过ADD PLMNNS配置的切片。<br>该参数在"FEATURECOND"配置为"NONE"、"DNN"时，值为"FFFFFF"。 |
| PAGINGPRO | 寻呼概率 | 可选必选说明：该参数在"FEATURECOND"配置为"PAGINGPRO"时为条件必选参数。<br>参数含义：该参数用于指定需要进行智能寻呼终端的寻呼概率。<br>数据来源：全网规划<br>取值范围：<br>- “P00（等于0%）”：用户计算的寻呼概率为0%。<br>- “P05（小于等于5%）”：用户计算的寻呼概率大于0%，小于等于5%。<br>- “P10（小于等于10%）”：用户计算的寻呼概率大于5%，小于等于10%。<br>- “P15（小于等于15%）”：用户计算的寻呼概率大于10%，小于等于15%。<br>- “P20（小于等于20%）”：用户计算的寻呼概率大于15%，小于等于20%。<br>- “P25（小于等于25%）”：用户计算的寻呼概率大于20%，小于等于25%。<br>- “P30（小于等于30%）”：用户计算的寻呼概率大于25%，小于等于30%。<br>- “P35（小于等于35%）”：用户计算的寻呼概率大于30%，小于等于35%。<br>- “P40（小于等于40%）”：用户计算的寻呼概率大于35%，小于等于40%。<br>- “P45（小于等于45%）”：用户计算的寻呼概率大于40%，小于等于45%。<br>- “P50（小于等于50%）”：用户计算的寻呼概率大于45%，小于等于50%。<br>- “P55（小于等于55%）”：用户计算的寻呼概率大于50%，小于等于55%。<br>- “P60（小于等于60%）”：用户计算的寻呼概率大于55%，小于等于60%。<br>- “P65（小于等于65%）”：用户计算的寻呼概率大于60%，小于等于65%。<br>- “P70（小于等于70%）”：用户计算的寻呼概率大于65%，小于等于70%。<br>- “P75（小于等于75%）”：用户计算的寻呼概率大于70%，小于等于75%。<br>- “P80（小于等于80%）”：用户计算的寻呼概率大于75%，小于等于80%。<br>- “P85（小于等于85%）”：用户计算的寻呼概率大于80%，小于等于85%。<br>- “P90（小于等于90%）”：用户计算的寻呼概率大于85%，小于等于90%。<br>- “P95（小于等于95%）”：用户计算的寻呼概率大于90%，小于等于95%。<br>- “P100（小于等于100%）”：用户计算的寻呼概率大于95%，小于等于100%。<br>默认值：无<br>配置原则：<br>该参数在"FEATURECOND"配置为"NONE"、"DNN"、"NSSAI"、"DNN_NSSAI"时，值为“小于等于100%”。 |

## 操作的配置对象

- [基于分组的智能寻呼策略（NGPEIPSPLCY）](configobject/UNC/20.15.2/NGPEIPSPLCY.md)

## 使用实例

删除IMSI为“123450001234567”的用户智能寻呼策略，指定特征条件组合为“仅指定DNN条件”，指定DNN为“HUAWEI.COM”，执行如下命令：

```
RMV NGPEIPSPLCY: SUBRANGE=SPECIFIC_IMSI, IMSI="123450001234567", FEATURECOND=DNN, DNN="HUAWEI.COM";
```

## 证据

- 原始手册：`evidence/UNC/20.15.2/删除基于分组的智能寻呼策略（RMV-NGPEIPSPLCY）_35398669.md`
