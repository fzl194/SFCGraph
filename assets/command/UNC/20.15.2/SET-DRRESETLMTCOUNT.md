---
id: UNC@20.15.2@MMLCommand@SET DRRESETLMTCOUNT
type: MMLCommand
name: SET DRRESETLMTCOUNT（设置复位限制的次数）
nf: UNC
version: 20.15.2
verb: SET
object_keyword: DRRESETLMTCOUNT
command_category: 配置类
effect_mode: 立即生效
is_dangerous: false
category_path:
- 平台服务管理
- 可靠性管理
- 微服务可靠性管理
status: active
---

# SET DRRESETLMTCOUNT（设置复位限制的次数）

## 功能

此命令用于设置在24小时内整系统复位的最大次数：

- 在负荷分担容灾模式下，限制由于关键服务故障或者周边网元故障引发的整系统复位的次数。
- 在冷备容灾模式下，限制运行主由于关键服务故障或周边网元故障叠加通道异常时引发的整系统复位的次数，以及限制运行备由于关键服务故障或周边网元故障引发的整系统复位的次数。
- 在热备容灾模式下，限制运行备由于关键服务故障引发的整系统复位的次数。

## 注意事项

- 该命令执行后立即生效。

- 该命令只用于在UEG/UEN/UEG+网元执行。
- 组成容灾关系的网元需要分别进行配置，两边保持一致。
- 每日凌晨0点或去使能容灾模式时，会将已记录的复位次数清零。
- 若网元复位完成时，时间在凌晨0点过后的保护期长度的时间内，会将已记录的复位次数清零。若超过保护期，则不会清零。
- 可通过OPR命令手动清零已记录的复位次数。
- 当此命令与其他命令冲突时，以以下原则生效：

- 以521软参的优先级为最高，当521软参设置为1时，忽略是否超过复位限制的次数。当设置为0时，则再以DRSTBYRSTCTRL命令设置为准。
- 以DRSTBYRSTCTRL命令的优先级为更高，当设置为“否”时，忽略是否超过复位限制的次数。当设置为“是”时，则再以此命令设置为准。

- 系统部署完成后，已经存在初始记录，参数的初始记录值如下表：

| RESETLMTCOUNT | PROTECTTIME |
| --- | --- |
| 3 | 30 |

## 权限

G_1，管理员级别命令组；G_2，操作员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| RESETLMTCOUNT | 复位限制的次数 | 可选必选说明：可选参数<br>参数含义：此参数用于指定复位限制的次数。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是0~64。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRRESETLMTCOUNT查询当前参数配置值。<br>配置原则：<br>若配置为0，则表示无复位次数的限制，可进行无数次复位处理。 |
| PROTECTTIME | 刷新保护期(分钟) | 可选必选说明：可选参数<br>参数含义：此参数用于指定每日凌晨刷新复位次数时的保护期。（单位:分钟）。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是10~120。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DRRESETLMTCOUNT查询当前参数配置值。<br>配置原则：无 |

## 操作的配置对象

- [[configobject/UNC/20.15.2/DRRESETLMTCOUNT]] · 复位限制的次数（DRRESETLMTCOUNT）

## 使用实例

设置在24小时内由于关键服务故障引发的整系统复位的最大次数。

```
%%SET DRRESETLMTCOUNT: RESETLMTCOUNT=3;%%
RETCODE = 0  操作成功

---    END
```

## 证据

- 原始手册：`evidence/UNC/20.15.2/SET-DRRESETLMTCOUNT.md`
