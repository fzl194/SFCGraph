---
id: UNC@20.15.2@MMLCommand@SET DEFEPSQOS
type: MMLCommand
name: SET DEFEPSQOS（设置EPS缺省QoS参数）
nf: UNC
version: 20.15.2
verb: SET
object_keyword: DEFEPSQOS
command_category: 配置类
applicable_nf:
- PGW-C
- SGW-C
effect_mode: 对新用户生效
is_dangerous: false
category_path:
- 业务服务管理
- 会话管理
- 接入管理
- QoS
- EPS QoS配置
- 全局EPS QoS纠错
status: active
---

# SET DEFEPSQOS（设置EPS缺省QoS参数）

## 功能

**适用NF：PGW-C、SGW-C**

该命令用来配置在SAE架构下的缺省QoS参数。当UNC收到GTPv2版本消息中携带的QoS字段不合法时，使用该配置作为缺省值。

PGW-C根据协议检查用户携带的QoS参数的合法性，如果参数非法，则根据该命令配置的缺省QoS参数进行替换。对于各业务类型的相关QoS参数，如果用户请求的QoS属性值为非法值，则QoS协商值等于系统配置值。如果用户请求的QoS属性值为合法值，则QoS协商值等于用户请求值。

## 注意事项

- 该命令执行后只对新激活用户生效。

- 上行保证带宽不能大于上行最大带宽。
- 下行保证带宽不能大于下行最大带宽。

- 系统部署完成后，已经存在初始记录，参数的初始记录值如下表：

| QOSCLASSID | ARPVALUE | GBRDL | GBRUL | MBRDL | MBRUL | APNAMBRDL | APNAMBRUL |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 5 | 3 | 400 | 400 | 500 | 500 | 2000 | 2000 |

## 权限

G_1，管理员级别命令组；G_2，操作员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| QOSCLASSID | QCI值 | 可选必选说明：可选参数<br>参数含义：该参数用于配置QoS流量级别。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~255。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DEFEPSQOS查询当前参数配置值。<br>配置原则：无 |
| ARPVALUE | ARP值 | 可选必选说明：可选参数<br>参数含义：该参数用于配置分配/保持优先等级。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~15。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DEFEPSQOS查询当前参数配置值。<br>配置原则：无 |
| GBRDL | 下行保证带宽(千比特/秒) | 可选必选说明：可选参数<br>参数含义：该参数用于配置保证的下行比特率。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是0~2000000。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DEFEPSQOS查询当前参数配置值。<br>配置原则：无 |
| GBRUL | 上行保证带宽(千比特/秒) | 可选必选说明：可选参数<br>参数含义：该参数用于配置保证的上行比特率。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是0~2000000。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DEFEPSQOS查询当前参数配置值。<br>配置原则：无 |
| MBRDL | 下行最大带宽(千比特/秒) | 可选必选说明：可选参数<br>参数含义：该参数用于配置最大的下行比特率。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是0~2000000。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DEFEPSQOS查询当前参数配置值。<br>配置原则：无 |
| MBRUL | 上行最大带宽(千比特/秒) | 可选必选说明：可选参数<br>参数含义：该参数用于配置最大的上行比特率。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是0~2000000。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DEFEPSQOS查询当前参数配置值。<br>配置原则：无 |
| APNAMBRDL | 下行APN AMBR(千比特/秒) | 可选必选说明：可选参数<br>参数含义：该参数用于配置下行集合最大比特速率。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是0~2000000。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DEFEPSQOS查询当前参数配置值。<br>配置原则：无 |
| APNAMBRUL | 上行APN AMBR(千比特/秒) | 可选必选说明：可选参数<br>参数含义：该参数用于配置上行集合的最大比特速率。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是0~2000000。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST DEFEPSQOS查询当前参数配置值。<br>配置原则：无 |

## 操作的配置对象

- [[configobject/UNC/20.15.2/DEFEPSQOS]] · EPS缺省QoS参数（DEFEPSQOS）

## 使用实例

配置QCI 6的EPS缺省QoS参数：

```
SET DEFEPSQOS: QOSCLASSID=6, ARPVALUE=1, GBRDL=200, GBRUL=230, MBRDL=300, MBRUL=300, APNAMBRDL=1000, APNAMBRUL=1000;
```

## 证据

- 原始手册：`evidence/UNC/20.15.2/SET-DEFEPSQOS.md`
