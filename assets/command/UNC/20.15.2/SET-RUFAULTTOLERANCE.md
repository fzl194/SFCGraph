---
id: UNC@20.15.2@MMLCommand@SET RUFAULTTOLERANCE
type: MMLCommand
name: SET RUFAULTTOLERANCE（设置资源单元故障容忍时间）
nf: UNC
version: 20.15.2
verb: SET
object_keyword: RUFAULTTOLERANCE
command_category: 配置类
effect_mode: 立即生效
is_dangerous: false
category_path:
- 平台服务管理
- 单体服务公共功能管理
- 系统管理
- 资源管理
- RU管理
status: active
---

# SET RUFAULTTOLERANCE（设置资源单元故障容忍时间）

## 功能

该命令用于设置资源单元故障容忍时间，包括正向容忍时间、反向容忍时间、软仲裁选主时间和软仲裁停主时间。软仲裁是用来确定两个主控资源单元节点的主备身份。

正向容忍时间是指网络断开后主控资源单元节点经过多长时间认为业务资源单元节点出现故障。反向容忍时间是指网络断开后业务资源单元节点经过多长时间复位。软仲裁选主时间是指网络断开后主控资源单元备节点变成主控资源单元主节点所消耗的时间。软仲裁停主时间是指防脑裂模式下网络断开后主控资源单元主节点经过多长时间放弃主身份。

当需要更快的感知资源单元的故障时，可以适当缩短资源单元故障容忍时间。合理的资源单元故障容忍时间既能保证及时感知资源单元的故障，又能避免因为网络闪断而造成资源单元故障的误判。

## 注意事项

- 该命令执行后立即生效。
- 正向容忍时间和反向容忍时间支持以秒为单位和以毫秒为单位配置，实际生效的时间取决于用户使用哪项配置，不支持同时配置秒和毫秒。
- DELAYTMMS、REVDELAYTMMS、VOTELEADERTMMS、STOPLEADERTMMS四个参数下发的时候要满足如下约束，否则配置命令下发失败。DELAYTMMS > REVDELAYTMMS；REVDELAYTMMS >= VOTELEADERTMMS + 2000；VOTELEADERTMMS >= STOPLEADERTMMS。
- DELAYTMMS、REVDELAYTMMS和STOPLEADERTMMS实际可配粒度是100 ms，参数值按100 ms粒度向下取整生效，VOTELEADERTMMS实际可配粒度是1000 ms，参数值按1000 ms粒度向下取整生效。
- DELAYTMMS和REVDELAYTMMS的误差范围是±150 ms。
- 在网络中断时调大STOPLEADERTMMS可能会使业务受损。
- 该命令至少需要输入一个可选参数。
- 该命令存在系统初始记录，参数的初始设置值如下表：

| DELAYTIME | REVDELAYTIME | DELAYTMMS | REVDELAYTMMS | VOTELEADERTMMS | STOPLEADERTMMS |
| --- | --- | --- | --- | --- | --- |
| 6 | 5 | NULL | NULL | 4000 | 4000 |

## 权限

G_1，管理员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| DELAYTIME | RU故障容忍时间（s） | 可选必选说明：可选参数<br>参数含义：该参数用于表示资源单元节点发生故障后，主控资源单元主节点需要多长时间来判断该资源单元节点故障。需要更快感知资源单元节点故障时，可以配置更短的时间。需要降低因为网络闪断而造成的资源单元节点故障误判的概率时，可以配置更长的时间。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为0～10，单位是秒。<br>默认值：无 |
| REVDELAYTIME | RU故障反向容忍时间（s） | 可选必选说明：可选参数<br>参数含义：该参数用于表示主控资源单元节点发生故障后，非主控资源单元节点需要多长时间来判断主控资源单元节点故障。需要更快感知主控资源单元节点故障时，可以配置更短的时间。需要降低因为网络闪断而造成的主控资源单元节点故障误判的概率时，可以配置更长的时间。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为5～9，单位是秒。<br>默认值：无 |
| DELAYTMMS | RU故障容忍时间（ms） | 可选必选说明：可选参数<br>参数含义：该参数用于表示资源单元节点发生故障后，主控资源单元主节点需要多长时间来判断该资源单元节点故障。需要更快感知资源单元节点故障时，可以配置更短的时间。需要降低因为网络闪断而造成的资源单元节点故障误判的概率时，可以配置更长的时间。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为6000～83000，单位是毫秒。<br>默认值：无 |
| REVDELAYTMMS | RU故障反向容忍时间（ms） | 可选必选说明：可选参数<br>参数含义：该参数用于表示主控资源单元节点发生故障后，非主控资源单元节点需要多长时间来判断主控资源单元节点故障。需要更快感知主控资源单元节点故障时，可以配置更短的时间。需要降低因为网络闪断而造成的主控资源单元节点故障误判的概率时，可以配置更长的时间。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为5000～30000，单位是毫秒。<br>默认值：无 |
| VOTELEADERTMMS | 软仲裁选主容忍时间（ms） | 可选必选说明：可选参数<br>参数含义：该参数用于表示主控资源单元主节点发生故障后，主控资源单元备节点需要多长时间来判断该主控资源单元主节点故障。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为3000～28000，单位是毫秒。<br>默认值：无 |
| STOPLEADERTMMS | 软仲裁停主容忍时间（ms） | 可选必选说明：可选参数<br>参数含义：该参数用于表示主控资源单元主节点发现选举成员少于半数时，主控资源单元主节点经过多长时间来复位。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围为3000～28000，单位是毫秒。<br>默认值：无 |
| SERVICEINSTANCE | 服务实例 | 可选必选说明：必选参数<br>参数含义：该参数表示大颗粒服务实例名称。<br>数据来源：本端规划<br>取值范围：字符串类型，通过LST VNFC命令获取。<br>默认值：无<br>配置原则：只能填写通过LST VNFC命令查询到的管理代理标识。 |

## 操作的配置对象

- [资源单元故障容忍时间（RUFAULTTOLERANCE）](configobject/UNC/20.15.2/RUFAULTTOLERANCE.md)

## 使用实例

设置正向容忍时间为9000毫秒，反向容忍时间为8000毫秒，软仲裁选主时间为4000毫秒，软仲裁停主时间为4000毫秒：

```
SET RUFAULTTOLERANCE:DELAYTMMS=9000,REVDELAYTMMS=8000,VOTELEADERTMMS=4000,STOPLEADERTMMS=4000
,SERVICEINSTANCE="vnfc"
;
```

## 证据

- 原始手册：`evidence/UNC/20.15.2/设置资源单元故障容忍时间（SET-RUFAULTTOLERANCE）_20857597.md`
