---
id: UNC@20.15.2@MMLCommand@ADD M3LNK
type: MMLCommand
name: ADD M3LNK（增加M3UA信令链路）
nf: UNC
version: 20.15.2
verb: ADD
object_keyword: M3LNK
command_category: 配置类
applicable_nf:
- SGSN
- MME
- SMSF
effect_mode: 立即生效
is_dangerous: false
max_records: 1280
category_path:
- 业务服务管理
- Pre 5G接入业务管理
- 控制面管理
- 信令传输管理
- M3UA管理
- M3UA链路
status: active
---

# ADD M3LNK（增加M3UA信令链路）

## 功能

**适用网元：SGSN、MME、SMSF**

该命令用于增加M3UA信令链路。

## 注意事项

- 该命令执行后立即生效。
- 该表的最大记录数为1280。
- 每个M3UA链路集最多只能配置16条M3UA链路。
- 不同链路的本地端口号、本地IP地址、对端端口号、对端IP地址（四元组），不能完全相同。
    - 如果本地端口号不同，则为有效的数据配置。
    - 如果本地端口号相同，两条链路的两个本地IP地址完全不同，则为有效的数据配置。
    - 如果本地端口号相同，而两条链路的两个本地IP地址有一个相同，会进行进一步的检查：
          - 如果两条链路的“C/S模式”不同，则为无效的数据配置。
          - 如果两条链路的“C/S模式”均为服务端模式，则为无效的数据配置。
          - 如果两条链路的“C/S模式”均为客户端模式，则需要进一步检查：如果对端端口号不相同，则为有效的数据配置；如果对端端口号相同，而对端的IP地址完全不重复，则为有效的数据配置，否则就为无效的数据配置。
    - 如果本地端口号相同，而两条链路的两个本地IP地址完全相同，会进行进一步的检查：
          - 如果两条链路的“C/S模式”不同，则为无效的数据配置
          - 如果两条链路的“C/S模式”相同，则需要进一步检查：
                  - 如果两条链路的“C/S模式”均为服务端模式，但两条链路的校验和算法不同，则为无效的数据配置
                  - 如果对端端口号不相同，则为有效的数据配置
                  - 如果对端端口号相同，而对端的IP地址完全不重复，则为有效的数据配置，否则就为无效的数据配置
- 判断两条链路的两个本地或对端IP地址是否相同与位置无关，即取一条链路的本地或对端IP地址1与另一条链路的本地或对端IP地址1和2分别比较，再取本地或对端IP地址2进行相同比较，最终确定两条链路的本地或对端IP地址是否有相同。
- 该命令在版本升级过程中禁止执行。
- IP地址和vpn名称必须在SERVICEIP中配置，可以用[**LST SERVICEIP**](../../../业务IP管理/业务IP/查询业务IP(LST SERVICEIP)_72226047.md)查询。

## 权限

manage-ug；system-ug
G_1，管理员级别命令组；G_2，操作员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| LNK | 链路号 | 可选必选说明：必选参数<br>参数含义：该参数用于指定M3UA链路号。该参数用来在系统范围内部唯一标识一条M3UA链路。<br>数据来源：本端规划<br>取值范围：0~1279<br>默认值：无 |
| VPNNAME | VPN名称 | 可选必选说明：可选参数<br>参数含义：该参数用于指定VPN名称。<br>数据来源：全网规划<br>取值范围：1～31位字符串<br>默认值：无 |
| IPT | IP类型 | 可选必选说明：可选参数<br>参数含义：该参数用于标识IP地址类型。<br>数据来源：整网规划<br>取值范围：<br>- “IPV4（IPv4）”<br>- “IPV6（IPv6）”<br>默认值：<br>“IPV4（IPV4地址类型）”<br>配置原则：根据选择的IP地址类型，输入相应格式的IP地址。 |
| LOCIPV41 | 本地IPv4地址1 | 可选必选说明：条件必选参数<br>参数含义：该参数用于指定SGSN网元侧分发M3UA协议消息的IP地址。<br>前提条件：该参数在<br>“IP类型”<br>配置为<br>“IPV4（IPV4地址类型）”<br>后生效。<br>数据来源：整网规划<br>取值范围：0.0.0.0~255.255.255.255。<br>默认值：无<br>配置原则：<br>- “本地IPv4地址1”必须输入。<br>- 同一条链路的“本地IPv4地址1”和“本地IPv4地址2”不能重复。 |
| LOCIPV42 | 本地IPv4地址2 | 可选必选说明：条件可选参数<br>参数含义：该参数用于指定SGSN网元侧分发M3UA协议消息的IP地址。如果输入了有效的IP地址，当本地IP地址1故障的时候，系统会自动切换到本参数指定的IP地址来与对端通信。<br>前提条件：该参数在<br>“IP类型”<br>配置为<br>“IPV4（IPV4地址类型）”<br>后生效。<br>数据来源：整网规划<br>取值范围：0.0.0.0~255.255.255.255<br>默认值：255.255.255.255<br>配置原则：<br>- 链路优先使用本地IPv4地址1和对端IP地址1进行通信，当其中一个地址故障时，如果配置了本地IPv4地址2，可以采用本地IPv4地址2进行通信。 |
| LOCIPV61 | 本地IPv6地址1 | 可选必选说明：条件必选参数<br>参数含义：该参数用于指定SGSN网元侧分发M3UA协议消息的IP地址。<br>前提条件：该参数在<br>“IP类型”<br>配置为<br>“IPV6（IPV6地址类型）”<br>后生效。<br>数据来源：整网规划<br>取值范围：::~FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF<br>默认值：无<br>配置原则：<br>- “本地IPv6地址1”必须输入。<br>- 同一条链路的“本地IPv6地址1”和“本地IPv6地址2”不能重复。<br>- IPv6地址必须是全球单播地址，不能为::、FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF、环回地址(::1)、链路本地地址(FE80::/10)和组播地址(FF00::/8)。 |
| LOCIPV62 | 本地IPv6地址2 | 可选必选说明：条件可选参数<br>参数含义：该参数用于指定SGSN协议消息的IP地址。如果输入了有效的IP地址，当本地IP地址1故障的时候，系统会自动切换到本参数指定的IP地址来与对端通信。<br>前提条件：该参数在<br>“IP类型”<br>配置为<br>“IPV6（IPV6地址类型）”<br>后生效。<br>数据来源：整网规划<br>取值范围：::~FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF<br>默认值：无<br>配置原则：<br>- 链路优先使用本地IPv6地址1和对端IP地址1进行通信，当其中一个地址故障时，如果配置了本地IPv6地址2，可以采用本地IPv6地址2进行通信。<br>- IPv6地址必须是全球单播地址，不能为FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF、环回地址(::1)、链路本地地址(FE80::/10)和组播地址(FF00::/8)。 |
| LOCPORT | 本地端口号 | 可选必选说明：可选参数<br>参数含义：该参数用于指定SGSN网元侧M3UA协议消息所使用的SCTP端口号。<br>数据来源：本端规划<br>取值范围：1024~65534<br>默认值：2905<br>配置原则：不同链路的本地端口号、本地IP地址、对端端口号、对端IP地址（四元组），不能完全相同。 |
| PEERIPV41 | 对端IP地址1 | 可选必选说明：条件必选参数<br>参数含义：该参数用于指定对端的IP地址。<br>前提条件：该参数在<br>“IP类型”<br>配置为<br>“IPV4（IPV4地址类型）”<br>后生效。<br>数据来源：整网规划<br>取值范围：0.0.0.0~255.255.255.255<br>默认值：无<br>配置原则：对端IP地址1必须输入。 |
| PEERIPV42 | 对端IP地址2 | 可选必选说明：条件可选参数<br>参数含义：该参数用于指定对端的IP地址。如果输入了有效的IP地址，当SGSN网元侧检测到对端IP地址1故障的时候，会自动切换到本参数指定的IP地址进行通信。<br>前提条件：该参数在<br>“IP类型”<br>配置为<br>“IPV4（IPV4地址类型）”<br>后生效。<br>数据来源：整网规划<br>取值范围：0.0.0.0~255.255.255.255<br>默认值：255.255.255.255<br>配置原则：链路优先使用本地IPv4地址1和对端IP地址1进行通信，当其中一个地址故障时，如果配置了地址2，可以采用地址2进行通信。 |
| PEERIPV61 | 对端IP地址1 | 可选必选说明：条件必选参数<br>参数含义：该参数用于指定对端的IP地址。<br>前提条件：该参数在<br>“IP类型”<br>配置为<br>“IPV6（IPV6地址类型）”<br>后生效。<br>数据来源：整网规划<br>取值范围：::~FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF<br>默认值：无<br>配置原则：<br>- 对端IP地址1必须输入<br>- IPv6地址必须是全球单播地址，不能为::、FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF、环回地址(::1)、链路本地地址(FE80::/10)和组播地址(FF00::/8)。 |
| PEERIPV62 | 对端IP地址2 | 可选必选说明：条件可选参数<br>参数含义：该参数用于指定对端的IP地址。如果输入了有效的IP地址，当SGSN网元侧检测到对端IP地址1故障的时候，会自动切换到本参数指定的IP地址进行通信。<br>前提条件：该参数在<br>“IP类型”<br>配置为<br>“IPV6（IPV6地址类型）”<br>后生效。<br>数据来源：整网规划<br>取值范围：::~FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF<br>默认值：无<br>配置原则：<br>- 链路优先使用本地IPv6地址1和对端IP地址1进行通信，当其中一个地址故障时，如果配置了地址2，可以采用地址2进行通信。<br>- IPv6地址必须是全球单播地址，不能为FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF、环回地址(::1)、链路本地地址(FE80::/10)和组播地址(FF00::/8)。 |
| PEERPORT | 对端端口号 | 可选必选说明：可选参数<br>参数含义：该参数用于指定SG或SCP侧M3UA协议消息所使用的SCTP端口号。<br>数据来源：与对端网元协商<br>取值范围：1024~65534<br>默认值：2905<br>配置原则：不同链路的本地端口号、本地IP地址、对端端口号、对端IP地址（四元组），不能完全相同。 |
| CS | C/S模式 | 可选必选说明：必选参数<br>参数含义：该参数用于指定SGSN网元侧在M3UA链路中的应用模式。<br>数据来源：整网规划<br>取值范围：<br>- “C（客户端模式）”<br>- “S（服务端模式）”<br>默认值：无<br>配置原则：<br>- 当链路集的工作模式为“ASP”，则M3UA链路中的应用模式一般设为“CLIENT”；<br>- 当链路集的工作模式为“SGP”，则M3UA链路中的应用模式一般设为“SERVER”；<br>- 当链路集的工作模式为“IPSP”，则M3UA链路中的应用模式可以设为“CLIENT”或“SERVER”，但同一链路集中链路模式必须保持一致。<br>- 本端和对端M3UA链路的应用模式不能相同。 |
| LSX | 链路集索引 | 可选必选说明：必选参数<br>参数含义：该参数用于指定M3UA链路所属的M3UA链路集。<br>数据来源：本端规划<br>取值范围：0~1279<br>默认值：无<br>说明：“链路集索引”<br>必须在M3UA链路集表中已经存在。M3UA链路集表的信息，请参考<br>[**ADD M3LKS**](../M3UA链路集/增加M3UA信令链路集(ADD M3LKS)_72225989.md)<br>命令。 |
| ASF | 主备标识 | 可选必选说明：可选参数<br>参数含义：该参数用于指定该条M3UA链路在正常情况下的工作状态。<br>数据来源：本端规划<br>取值范围：<br>- “STD（备用）”<br>- “ACT（主用）”<br>默认值：<br>“ACT（主用）”<br>配置原则：<br>- 该配置与链路的传输模式(即[**ADD M3LKS**](../M3UA链路集/增加M3UA信令链路集(ADD M3LKS)_72225989.md)命令中的“业务模式”参数的设置)密切相关：若采用主备用模式，则同一链路集下的所有M3UA链路只能有一条链路被设置为ACTIVE（主用）状态，其余链路则必须被设置为STANDBY（备用）状态；若采用负荷分担模式，则同一链路集下的所有M3UA链路必须全部被设置为ACTIVE（主用）状态。 |
| PRI | 优先级 | 可选必选说明：可选参数<br>参数含义：该参数用于指定M3UA链路的优先级。即链路被选择使用的优先级。<br>数据来源：整网规划<br>取值范围：0~254<br>默认值：0<br>配置原则：0为最高优先级，254为最低优先级。 |
| CONSTHD | 拥塞开始门限 | 可选必选说明：可选参数<br>参数含义：保留参数，不建议自行配置。 |
| CONETHD | 拥塞结束门限 | 可选必选说明：可选参数<br>参数含义：保留参数，不建议自行配置。 |
| CROSSIP | 交叉路径是否可用 | 可选必选说明：可选参数<br>参数含义：该参数用于表明交叉路径是否可用。交叉路径是指本端第一个IP地址到对端第二个IP地址和本端第二个IP地址到对端第一个IP地址所组成的路径。<br>数据来源：整网规划<br>取值范围：<br>- “YES（是）”<br>- “NO（否）”<br>默认值：<br>“NO（否）” |
| SCTPINDX | SCTP 参数索引 | 可选必选说明：必选参数<br>参数含义：该参数为SCTPPARA表的索引号，用于指定本M3链路使用的SCTP协议栈参数。<br>前提条件：SCTPPARA表中要有对应的SCTP参数索引号，否则输入的索引号无效，无法正确添加记录。SCTP参数索引号，请参考<br>[**ADD SCTPPARA**](../../SCTP管理/增加SCTP协议参数(ADD SCTPPARA)_26306150.md)<br>命令。<br>数据来源：本端规划<br>取值范围：0~65534<br>默认值：无<br>说明：- 需要在[**LST SCTPPARA**](../../SCTP管理/查询SCTP协议参数(LST SCTPPARA)_72226019.md)时可以查询到相应的链路索引。<br>- 根据SCTPPARA索引号，在SCTPPARA表中查询对应的校验和算法。 |
| EXCHGMODE | 交换模式 | 可选必选说明：可选参数<br>参数含义：该参数用于设置在链路集工作模式为“IPSP”时，SGSN网元和对端网元的交换模式。<br>数据来源：对端协商<br>取值范围：<br>- “SINGLE（单交换模式）”<br>- “DOUBLE（双交换模式）”<br>默认值：<br>“SINGLE（单交换模式）”<br>说明：- 当链路集的工作模式为“ASP”或者“SGP”，本参数将不生效。<br>- 本参数设置值需要和对端网元的交换模式保持一致。 |
| LKN | 链路名 | 可选必选说明：可选参数<br>参数含义：该参数用于指定链路名称，标识M3UA链路。<br>数据来源：本端规划<br>取值范围：1~32位字符串<br>默认值：<br>“noname” |

## 操作的配置对象

- [[configobject/UNC/20.15.2/M3LNK]] · M3UA信令链路（M3LNK）

## 使用实例

1. 增加IPV4类型的M3UA链路：
  ADD M3LNK: LNK=0, LOCIPV41="10.11.12.13", PEERIPV41="192.168.44.211", CS=C, LSX=0, SCTPINDX=1, EXCHGMODE=SINGLE, LKN="TEST", VPNNAME="_abc_";

## 证据

- 原始手册：`evidence/UNC/20.15.2/增加M3UA信令链路(ADD-M3LNK)_72225983.md`
