---
id: UNC@20.15.2@MMLCommand@SET AGFOUTFC
type: MMLCommand
name: SET AGFOUTFC（设置出局流控参数）
nf: UNC
version: 20.15.2
verb: SET
object_keyword: AGFOUTFC
command_category: 配置类
applicable_nf:
- NCG
effect_mode: 立即生效
is_dangerous: false
category_path:
- 业务服务管理
- NCG业务及策略管理
- AGF流控参数信息
status: active
---

# SET AGFOUTFC（设置出局流控参数）

## 功能

**适用NF：NCG**

该命令用于设置出局流控参数。

当AGF转发消息给对端网元(如OCS或SCP)，对端网元出现异常无法处理计费请求时，为避免大量的异常消息导致服务内部资源过载，通过该命令设置流控参数，控制NCG转发计费消息到对端网元的频率，减少服务内部资源的消耗和对对端网元的冲击压力。

该命令有4种模式：全局流控，会话级流控，隔离全部OCS，按消息步长流控。

1）会话级流控模式：会话级流控模式，分为3种状态：正常状态、流控状态、待决状态。当设置为会话级流控模式时，。

正常状态或待决状态下：AGF会转发在线计费消息给对端网元。

流控状态下：AGF则会直接代应答该会话的在线消息。

2）全局流控模式：全局流控模式，也分为3种状态:正常状态、流控状态、待决状态。当设置为全局流控模式时，。

正常状态下：AGF会转发所有的在线计费消息给对端网元。

流控状态下：AGF会直接代应答转发给该对端网元的所有在线消息。

待决状态：AGF会转发所有的在线计费消息给对端网元，拥塞计数次数达到阈值，会直接进入拥塞状态。

3）隔离全部OCS模式。当设置为隔离全部OCS模式时，AGF会直接代应答所有的在线消息，不转发给对端网元。

4）消息步长流控模式。当设置为消息步长流控模式时，在线计费消息按照条数间隔转发OCS。

## 注意事项

- 该命令执行后立即生效。

- 系统部署完成后，已经存在初始记录，参数的初始记录值如下表：

| FCID | GFCSWITCHTHOD | GFCPERIOD | SFCMCTIME | SFCRECOVERYTIME | MSGINTERVAL | GFCRECOVERYTHOD | TOPOFAILCOUNT | GFCTIMER |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ALL | 20000 | 60 | 60 | 120 | 10 | 4000 | TRUE | 600 |

## 权限

G_1，管理员级别命令组；G_2，操作员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| FCID | 出局流控模式 | 可选必选说明：必选参数<br>参数含义：该参数用于控制流控模式。取值说明:<br>值为ALL时，默认开启会话级流控处理模式和OCS全局流控处理模式。<br>值为CLOSE时，关闭流控处理功能。<br>值为SESSION时，仅开启会话级流控处理模式。<br>值为GLOBAL时，仅开启OCS全局拥塞流控处理模式。<br>值为ISOLATE_ALL时，开启隔离全部对端网元，对所有在线计费消息直接代应答模式。<br>值为MSG_BY_STEP时，开启在线计费消息按照条数间隔转发给OCS模式。<br>数据来源：本端规划<br>取值范围：<br>- ALL（会话级和全局流控模式）<br>- CLOSE（关闭流控）<br>- SESSIOIN（会话级流控）<br>- GLOBAL（全局流控）<br>- ISOLATE_ALL（隔离OCS）<br>- MSG_BY_STEP（消息步长流控）<br>默认值：无。<br>配置原则：无 |
| GFCSWITCHTHOD | 全局流控阈值(次) | 可选必选说明：该参数在"FCID"配置为"ALL"、"GLOBAL"时为条件可选参数。<br>参数含义：该参数用于调整在设置OCS全局流控模式下，在参数GFCPERIOD设置的时间内，拥塞统计次数达到该值后，切换至拥塞状态，默认取值20000次。如当参数GFCPERIOD设置为60s,该参数设置为10000时，表示60s内转发给某个OCS的计费消息出现10000次拥塞统计后切换至拥塞状态。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~2147483647，单位是次。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST AGFOUTFC查询当前参数配置值。<br>配置原则：无 |
| GFCPERIOD | 全局统计周期(秒) | 可选必选说明：该参数在"FCID"配置为"ALL"、"GLOBAL"时为条件可选参数。<br>参数含义：该参数用于调整在设置OCS全局流控模式下，设置全局流控统计周期，单位秒。如该参数设置为30秒，表示统计转发给某个OCS的计费消息出现代应答次数的周期为30秒。30秒过后，统计次数清零，进行新的30秒统计。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~2147483647，单位是秒。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST AGFOUTFC查询当前参数配置值。<br>配置原则：无 |
| SFCMCTIME | 会话级流控状态最大退避时长(分钟) | 可选必选说明：该参数在"FCID"配置为"SESSIOIN"、"ALL"时为条件可选参数。<br>参数含义：该参数用于调整会话级流控模式下，会话级拥塞状态切换至待决状态的时间的退避上限，单位为分钟，默认60分钟。比如该参数设置为120时，表示会话级拥塞状态最大持续时间为120分钟，当超过120分钟，切换为待决状态。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~65535，单位是分钟。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST AGFOUTFC查询当前参数配置值。<br>配置原则：无 |
| SFCRECOVERYTIME | 会话级流控退出时长(分钟) | 可选必选说明：该参数在"FCID"配置为"SESSIOIN"、"ALL"时为条件可选参数。<br>参数含义：该参数用于调整在会话级流控模式下，调整会话级别待决状态切换至正常状态的无代应答时长，单位分钟。比如该参数设置为60时，表示处于会话级待决状态下，超过60分钟没有出现代应答，则切换至会话级正常状态。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~255，单位是分钟。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST AGFOUTFC查询当前参数配置值。<br>配置原则：无 |
| MSGINTERVAL | 消息间隔条数(条) | 可选必选说明：该参数在"FCID"配置为"MSG_BY_STEP"时为条件可选参数。<br>参数含义：该参数用于调整在在线计费消息按照条数间隔转发给OCS模式下，用于调整消息条数间隔。比如该参数值为10时，表示每隔10条在线计费消息转发一次给OCS。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~2147483647，单位是条。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST AGFOUTFC查询当前参数配置值。<br>配置原则：无 |
| GFCRECOVERYTHOD | 全局流控恢复阈值(次) | 可选必选说明：该参数在"FCID"配置为"ALL"、"GLOBAL"时为条件可选参数。<br>参数含义：该参数用于调整在设置OCS全局流控模式下，在参数GFCPERIOD设置的时间内，拥塞计数次数少于该值后，切换至正常状态，默认取值4000次。如当参数GFCPERIOD设置为60s,该参数设置为2000时，表示60s内转发给某个OCS的计费消息出现少于2000次拥塞计数后，由待决状态切换至正常状态。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是1~2147483647，单位是次。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST AGFOUTFC查询当前参数配置值。<br>配置原则：无 |
| TOPOFAILCOUNT | 是否统计TOPO发现失败次数 | 可选必选说明：该参数在"FCID"配置为"SESSIOIN"、"ALL"时为条件可选参数。<br>参数含义：该参数用于调整会话级流控模式下，是否统计TOPO发现失败次数。比如该参数设置为false时，表示会话级定时器时，出现TOPO发现失败时，也不会进入会话级流控状态。<br>数据来源：本端规划<br>取值范围：<br>- TRUE(TRUE)<br>- FALSE(FALSE)<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST AGFOUTFC查询当前参数配置值。<br>配置原则：无 |
| GFCTIMER | 全局流控状态最大退避时长(秒) | 可选必选说明：该参数在"FCID"配置为"ALL"、"GLOBAL"时为条件可选参数。<br>参数含义：该参数用于调整在设置OCS全局流控模式下，全局拥塞状态切换至待决状态的时间的退避上限，单位为分钟，默认600s。比如该参数设置为600时，表示全局拥塞状态最大持续时间为600s，当超过600s，切换为待决状态。<br>数据来源：本端规划<br>取值范围：整数类型，取值范围是0~2147483647。<br>默认值：无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST AGFOUTFC查询当前参数配置值。<br>配置原则：无 |

## 操作的配置对象

- [[configobject/UNC/20.15.2/AGFOUTFC]] · 出局流控参数（AGFOUTFC）

## 使用实例

设置AGF出局流控参数，将流控模式设置为打开会话级和全局模式，全局流控下切换拥塞状态的次数阈值设置为20000；

```
SET AGFOUTFC:FCID=ALL,GFCSWITCHTHOD=20000;
```

## 证据

- 原始手册：`evidence/UNC/20.15.2/设置出局流控参数（SET-AGFOUTFC）_87446492.md`
