---
id: UNC@20.15.2@MMLCommand@SET CHGCHAR
type: MMLCommand
name: SET CHGCHAR（设置计费属性参数）
nf: UNC
version: 20.15.2
verb: SET
object_keyword: CHGCHAR
command_category: 配置类
applicable_nf:
- SGSN
effect_mode: 立即生效
is_dangerous: false
category_path:
- 业务服务管理
- Pre 5G接入业务管理
- 计费管理
- 计费属性参数配置
status: active
---

# SET CHGCHAR（设置计费属性参数）

## 功能

**适用网元：SGSN**

该命令用于设置某个计费属性的参数配置。

## 注意事项

- 系统初次运行时，会执行系统初始设置值。
- 该命令执行后立即生效。
- 只有[**ADD CHGCG**](../计费控制/增加CG配置参数（ADD CHGCG）_72225055.md)命令配置的CG，才能在此命令、[**ADD CHGBEHA**](../计费行为参数配置/增加计费行为参数(ADD CHGBEHA)_26145366.md)和[**ADD CHGPLMNCG**](../PLMN CG 配置/增加PLMN-CG配置参数（ADD CHGPLMNCG）_72225067.md)命令中生效。
- 不同命令配置的CG优先级从高到低的次序是：[**ADD CHGPLMNCG**](../PLMN CG 配置/增加PLMN-CG配置参数（ADD CHGPLMNCG）_72225067.md)，[**ADD CHGBEHA**](../计费行为参数配置/增加计费行为参数(ADD CHGBEHA)_26145366.md)，[**SET CHGCHAR**](设置计费属性参数(SET CHGCHAR)_26145368.md)，[**ADD CHGCG**](../计费控制/增加CG配置参数（ADD CHGCG）_72225055.md)。
- 设置最大计费条件变更次数前，要与客户计费中心确认是否能够正常处理多流量节点话单，避免出现多流量节点话单被丢弃的情况。

## 权限

manage-ug；system-ug
G_1，管理员级别命令组；G_2，操作员级别命令组

## 参数

| 参数标识 | 参数名称 | 参数说明 |
| --- | --- | --- |
| CC | 计费属性 | 可选必选说明：必选参数<br>参数含义：该参数指运营商可以用不同的计费方法对用户实施计费。<br>数据来源：整网规划<br>取值范围：<br>- “NORMAL（普通计费）”：表示普通计费属性，按照此种方式计费的用户按照常规的方式支付费用。<br>- “PREPAID（预付费）”：表示预付费计费属性，按照此种方式计费的用户在获取某种服务之前需要预支付一定的费用。<br>- “FLATRATE（包月制）”：表示包月制计费属性，按照此种方式计费的用户在一个月内的收费是固定的。<br>- “HOTBILLING（实时计费）”：表示实时计费属性，按照此种方式计费的用户将在短时间或流量达到某个值时及时生成话单，保证运营商对此类用户及时收费。<br>系统初始设置：系统为每种计费方式（普通计费、预付费、包月制、实时计费）均提供了计费相关参数的系统初始设置值。 |
| MP | 生成M-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户是否生成M-CDR标志。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：NO（不生成） |
| MPCP | 周期生成M-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户是否周期生成部分M-CDR标志。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：YES（生成） |
| MPL | M-CDR生成周期（min） | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户生成部分M-CDR的周期。<br>数据来源：整网规划<br>取值范围：1min～1440min<br>系统初始设置值：60min |
| MLCP | 位置更新生成M-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户位置更新是否生成部分M-CDR标志。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：YES（生成） |
| MLCL | M-CDR位置更新最大次数 | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户位置更新生成部分M-CDR的最大位置更新次数。系统如果允许该计费属性用户位置更新生成部分M-CDR，则位置更新次数达到此最大值时该计费属性用户生成部分M-CDR。<br>数据来源：整网规划<br>取值范围：1～10<br>系统初始设置值：5 |
| RPCMCDR | 精简M-CDR | 可选必选说明：可选参数<br>参数含义；该参数用于指定计费属性用户是否生成精简部分M-CDR标志。系统如果允许该计费属性用户生成精简部分M-CDR，当该计费属性用户按照R4协议生成部分M-CDR话单时将会有精简部分M-CDR生成。精简话单是指部分话单中只包含必选的话单域，以及其他和前一张部分话单相比发生变化的话单域。<br>数据来源：整网规划<br>取值范围：<br>- “NO（否）”：表示不生成精简M-CDR。<br>- “YES（是）”：表示生成精简M-CDR。<br>系统初始设置值：NO（否） |
| SP | 生成S-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户是否生成S-CDR标志。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：YES（生成） |
| SPP | 周期生成S-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户是否周期生成部分S-CDR标志。<br>数据来源：整网规划<br>取值范围：<br>- “FORBID（不生成）”：表示不周期生成S-CDR。<br>- “PERMIT（周期生成）”：表示周期生成S-CDR。<br>- “VOLUMN（有流量时周期生成）”：表示有流量时周期生成S-CDR。<br>系统初始设置值：VOLUMN（有流量时周期生成） |
| SPL | S-CDR生成周期（min） | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户生成部分S-CDR的周期。<br>数据来源：整网规划<br>取值范围：1min～1440min<br>系统初始设置值：60min |
| SVP | 流量生成S-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定有流量时该计费属性用户是否生成部分S-CDR标志。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：YES（生成） |
| SVL | 生成S-CDR流量阈值（KB） | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户流量生成部分S-CDR的流量阈值。系统如果允许该计费属性用户流量生成部分S-CDR，则流量达到此阈值时生成部分S-CDR。<br>数据来源：整网规划<br>取值范围：1KB～1000000KB<br>系统初始设置值：1000KB<br>说明：该参数取值需大于等于<br>**SET PFRES**<br>中配置的流量上报门限（2G对应<br>**SET PFRES**<br>配置中的<br>**RPTF**<br>参数，3G对应<br>**SET PFRES**<br>配置中的<br>**RPTF3G**<br>参数），否则基于流量生成的话单可能不按实际配置的流量阈值生效。 |
| SCCP | 计费条件变更生成S-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户在计费条件发生变更时（QoS变更或者费率时段变更）是否生成部分S-CDR标志。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：YES（生成） |
| SCCL | 最大计费条件变更次数 | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户在计费条件变更时生成部分S-CDR的最大条件变更次数。系统如果允许该计费属性用户条件变更生成部分S-CDR，则计费条件变更次数达到此最大值时生成部分S-CDR。<br>数据来源：整网规划<br>取值范围： 1～10<br>系统初始设置值：3 |
| SLCP | 位置更新生成S-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户位置更新是否生成部分S-CDR的标志。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”：不会导致inter RAU SGSN新侧不创建话单，只是位置更新时不会生成话单，流量或时间达到阈值仍会生成话单。<br>- “YES（生成）”：位置更新时生成话单。<br>系统初始设置值：NO（不生成） |
| PCC | PLMN变化生成特定原因值S-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户位置更新时，如果PLMN发生改变，是否生成原因值为“101”的部分S-CDR话单。<br>数据来源：整网规划<br>取值范围：<br>“NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：NO（不生成）<br>配置原则：修改该参数将导致用户位置更新且PLMN改变时发送给CG的部分S-CDR话单携带的原因值改变，可能造成CG无法解析。 |
| RPCSCDR | 精简S-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户是否生成精简部分S-CDR标志。系统如果允许该计费属性用户生成精简部分S-CDR，当该计费属性用户按照R4协议生成部分S-CDR话单时将会有精简部分S-CDR生成。精简话单是指部分话单中只包含必选的话单域，以及其他和前一张部分话单相比发生变化的话单域。<br>数据来源：整网规划<br>取值范围：<br>- “NO（否）”<br>- “YES（是）”<br>系统初始设置值：NO（否） |
| SMOP | 生成S-SMO-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户是否生成S-SMO-CDR标志。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：YES（生成） |
| SMTP | 生成S-SMT-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户是否生成S-SMT-CDR标志。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：YES（生成） |
| LCSMOP | 生成LCS-MO-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户是否生成LCS-MO-CDR标志。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：YES（生成） |
| LCSMTP | 生成LCS-MT-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户是否生成LCS-MT-CDR的标志。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：YES（生成） |
| LCSNIP | 生成LCS-NI-CDR | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户是否生成LCS-NI-CDR。<br>数据来源：整网规划<br>取值范围：<br>- “NO（不生成）”<br>- “YES（生成）”<br>系统初始设置值：YES（生成） |
| SR | 计费限呼标志 | 可选必选说明：可选参数<br>参数含义：该参数用于指定当话单池缓存满时是否限制该计费属性用户业务。<br>数据来源：整网规划<br>取值范围：<br>- “UNRESTRICT（不限制业务）”：表示不限制业务，在缓存满时保证网上用户和新接入用户正常进行业务，但是会丢失话单。<br>- “RESTRICT（禁止激活PDP和收发短消息）”：表示新接入用户禁止激活PDP和收发短消息，在缓存满时只保证已经上网的业务（包括激活PDP和收发短消息）正常进行，但是会丢失话单。<br>- “INTERRUPT（中止当前业务）”：表示终止业务，在缓存满时限制所有业务的进行，即一方面强制中断网上业务，同时限制所有新接入业务，话单不会丢失。<br>系统初始设置值：UNRESTRICT（不限制业务） |
| CGIP | CG的IPv4地址 | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户的话单优先发往CG的IP地址。该CG的优先级高于GGSN、其他CG推荐的CG或运营商配置的CG，但低于计费行为指定的CG。<br>数据来源：整网规划<br>取值范围：0.0.0.0～255.255.255.255<br>系统初始设置值：0.0.0.0<br>配置原则：“0.0.0.0”和“255.255.255.255”是无效的IP地址。 |
| CGIPV6 | CG的IPv6地址 | 可选必选说明：可选参数<br>参数含义：该参数用于指定计费属性用户的话单优先发往CG的IPv6地址。该CG的优先级高于GGSN、其他CG推荐的CG或运营商配置的CG，但低于计费行为指定的CG。<br>数据来源：整网规划<br>取值范围：::～FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF<br>系统初始设置值：::<br>配置原则：<br>- IPv6地址必须是全球单播地址，不能为FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF、环回地址(::1)、链路本地地址(FE80::/10)和组播地址(FF00::/8)。 |
| TARIACCFLG | 费率变更允许的用户类型 | 可选必选说明：可选参数<br>参数含义：该参数是用以配置允许零点费率变更的用户类型。<br>数据来源：整网规划<br>取值范围：<br>- “HOME_USER（本地用户）”<br>- “ROAM_USER（漫游用户）”<br>- “VISIT_USER（拜访用户）”<br>系统初始设置值：全部选中 |

## 操作的配置对象

- [[configobject/UNC/20.15.2/CHGCHAR]] · 计费属性参数（CHGCHAR）

## 使用实例

设置实时计费属性的“M-CDR生成周期(min)”为120分钟，设置“生成S-SMO-CDR”为不生成，配置格式如下：

SET CHGCHAR: CC=HOTBILLING, MPL=120, SMOP=NO;

## 证据

- 原始手册：`evidence/UNC/20.15.2/SET-CHGCHAR.md`
