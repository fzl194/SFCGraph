---
id: UNC@20.15.2@Task@0-00214
type: Task
task_layer: atom
task_logical_name: 增加IPv4 ACL规则组
ref: UNC@20.15.2@MMLCommand@ADD ACLGROUPIPSEC
task_intent: 增加 IPv4 ACL 规则组（容器），为 IPv4 报文过滤/IPSec 保护流提供规则集合；按规则组类型（基本/高级）与匹配顺序（配置优先/深度优先）两维度组织配置空间
status: draft
---

# 增加IPv4 ACL规则组（ADD ACLGROUPIPSEC）

> 命令静态知识见 [ADD ACLGROUPIPSEC](command/UNC/20.15.2/ADD-ACLGROUPIPSEC.md)。本页只讲配置生成实例化时这条命令**怎么配**。

## 配置方法

该命令创建一个 IPv4 ACL 规则组（容器），后续通过 `ADD ACLRULEADV4IPSEC` 向其挂载具体规则。配置空间由两个维度决定：**规则组类型**（基本 vs 高级）、**匹配顺序**（配置优先 vs 深度优先）。

### 配置维度1：规则组类型（参数 ACLTYPE）

| 取值 | 作用 | 配套参数 | 典型场景 |
|---|---|---|---|
| Advance（高级 ACL） | 基于五元组匹配 IPv4 报文（默认，当 ACLNAME 为非数字时） | — | IPSec 保护流匹配（IPv4 隧道） |
| Basic（基本 ACL） | 仅基于源 IP 匹配 | — | 简单源地址过滤 |

### 配置维度2：规则组标识（参数 ACLNAME）

| 取值形式 | 作用 | 约束 |
|---|---|---|
| 数字字符串（"3000"~"3999"） | 用整数编号标识高级 ACL | 兼容 CE 版本允许其他数值但不生效 |
| 非数字字符串（a-z/A-Z 开头，1~32 字符） | 用名称标识 | ACLTYPE 默认为高级 ACL |

### 配置维度3：匹配顺序（参数 ACLMATCHORDER）

| 取值 | 作用 | 适用场景 |
|---|---|---|
| Config（配置优先，默认） | 规则按配置顺序排序 | 默认场景 |
| Auto（深度优先） | 规则按深度优先自动排序 | 精细化匹配顺序控制 |

**步骤位置**：在所属 feature（IPFD-016000 IPSec功能）编排中，是 IPv4 IPSec 隧道建立的第一步——`ADD ACLRULEADV4IPSEC`（向本规则组挂规则）与 `ADD IPSECPOLICY`（ACLNUMBER 引用本规则组）之前执行。

## 决策点

（无。本命令在所有特性下用法一致，无分支。）

## 约束

- **立即生效**（info）：命令执行后立即生效 — 后果：无延迟
- **非数字 ACLNAME 默认高级 ACL**（info）：当 ACLNAME 为非数字时，ACLTYPE 参数默认为高级 ACL — 后果：不显式指定也按高级处理
- **最多 65535 条记录**（warning）：本命令最多可输入 65535 条记录 — 后果：超规格新增失败

## 关联

- 命令 wiki: [ADD ACLGROUPIPSEC](command/UNC/20.15.2/ADD-ACLGROUPIPSEC.md)
- 配置对象: [ACLGROUPIPSEC](configobject/UNC/20.15.2/ACLGROUPIPSEC.md)
- 配套组: [ADD ACLRULEADV4IPSEC](task/UNC/20.15.2/0-00215.md)（向本规则组挂规则）、[ADD ACLGROUP6IPSEC](task/UNC/20.15.2/0-00213.md)（IPv6 版本）、[ADD IPSECPOLICY](task/UNC/20.15.2/0-00241.md)（策略引用本规则组）
- 被引用于: [IPFD-016000](feature/UNC/20.15.2/IPFD-016000.md) IPSec功能；引用该命令的特性清单见汇总 md ②段
- 证据: 激活 md（`激活国密IPsec支持IKEv1功能（普通IPv4 IPsec隧道）_03728909.md`）待拷贝到 `evidence/UNC/20.15.2/IPFD-016000/`
