# UNC atom 重构批次跟踪

> 范围：UNC 已有 atom 212 条，按编号 0-XXXXX 顺序分批（10 条/批）
> 状态：✅ 已重构 / ⏳ 待重构 / 🔄 进行中
> 第一阶段：旧式 evidence_only → 新 SOP 模板（4章节 + DP/rule 编号化 + status:draft）
> 已完成：批次1（10条）

## 重构简版模板

1. frontmatter 7 字段（id/type/task_layer/task_logical_name/ref/task_intent/status:draft）
2. 配置方法段：**配置方法字典**（按维度组织，每维度 1 表：取值+作用+配套参数），**不**逐特性罗列
3. 决策点段：`## 决策点：{名}（DP 0-XXXXX）`
4. 约束段：bullet `- **{名}**（{severity}，{rule_id}）：{约束} — {后果}`
5. 关联段：命令 wiki + 配置对象 + 配套组 atom + 被引用于

## 简化原则
- **不**做精细自检（主 Agent 验证）
- **不**改 _numbering.json 已建编号
- 遇分歧以原始命令 md 为准
- 每条 50-100 行
- rule_id 防撞号：前置 `grep -h "rule-0-" assets/task/UNC/20.15.2/*.md | sort -u`

## 实战教训（UDG APN-1 沉淀）
1. **rule_id 撞号主动避让**：已用段位前置 grep，撞号时用 0-01XXX 避让
2. **task_intent 单引号**：含 `:` 的整字段必须单引号包
3. **source_evidence_ids 不指 `_intermediates/`**：拷到 `assets/evidence/UNC/20.15.2/` 或指向原始 OM 路径
4. **status: draft 不可降级**：旧 `evidence_only` 必须改为 `draft`
5. **4 子 Agent 并行是甜区**：每 Agent 2-3 条，主 Agent 验证不可省

## 进度

| 批次 | 范围 | 状态 | 完成日期 | 命令 |
|---|---|---|---|---|
| 批次1 | 0-00001 ~ 0-00010 | ✅ 已重构 | 2026-07-11 | ADD URR、ADD ADCPARA、ADD AMUEPLCYCTRL、ADD APN、ADD APNCHGMODE、ADD APNDEACTQFPLCY、ADD APNPOLICYMODE、ADD APNUSRPROFG、ADD CCT、ADD CDRFIELDTEMP |
| 批次2 | 0-00011 ~ 0-00020 | ⏳ 待重构 |  | ADD CGBINDING、ADD CGGROUP、ADD CGGRPBINDING、ADD CHARGECHAR、ADD CHARGEMETHOD、ADD CHGAPN、ADD CHGCG、ADD CHGDCHAR、ADD CHGHOLI、ADD CHGIMSICFG |
| 批次3 | 0-00021 ~ 0-00030 | ⏳ 待重构 |  | ADD CHGPLMNCFG、ADD CHGTARI、ADD CPCGBINDING、ADD CPCGGRP、ADD DCCTEMPLATE、ADD DEACTQFPLCY、ADD DIAMCONNECTION、ADD DIAMCONNGRP、ADD DIAMLOCINFO、ADD DIAMPEERADDR |
| 批次4 | 0-00031 ~ 0-00040 | ⏳ 待重构 |  | ADD FESTIVAL、ADD FLOWFILTER、ADD GLBOFCTEMPLATE、ADD GLBPCRFGROUP、ADD GUAMI、ADD GXUPFGROUP、ADD HTTPLE、ADD HTTPLEGRP、ADD IMSIMSISDNSEG、ADD L3VPNINST |
| 批次5 | 0-00041 ~ 0-00050 | ⏳ 待重构 |  | ADD LOCALHOSTBIND、ADD LOCALHOSTGRP、ADD LOGICINF、ADD LOGICIP、ADD N40DIAGTRIGGER、ADD N40MSGTEMP、ADD NGMMSUBDATA、ADD NGPLMN、ADD NGSGWPLMN、ADD OFCTEMPLATE |
| 批次6 | 0-00051 ~ 0-00060 | ⏳ 待重构 |  | ADD PCCCHGMODEBYPCFID、ADD PCCPBINDUPG、ADD PCCPOLICYGRP、ADD PCCTEMPLATE、ADD PCFSELPLCY、ADD PCFSSCOPE、ADD PCFSSCOPEBIND、ADD PCRF、ADD PCRFBINDGRP、ADD PCRFGROUP |
| 批次7 | 0-00061 ~ 0-00070 | ⏳ 待重构 |  | ADD PCRFGRPBNDAPN、ADD PDUSCACT、ADD PDUTRIGGER、ADD PNFGPSI、ADD PNFSUPI、ADD QOSPROP、ADD QUOTAEXHAUSTACT、ADD RESULTCODECTRL、ADD RGRCACT、ADD RGTRIGGER |
| 批次8 | 0-00071 ~ 0-00080 | ⏳ 待重构 |  | ADD ROAMCHGMODE、ADD RULE、ADD RULEBINDDNAI、ADD RULEBINDING、ADD SBIAPLE、ADD SELCHFGBIMSISEG、ADD SELCHFGBYIMSI、ADD SELECTCCTBYCC、ADD SELECTCHFGBYCC、ADD SGWSEGGCHGMETH |
| 批次9 | 0-00081 ~ 0-00090 | ⏳ 待重构 |  | ADD SPECIFICAPNVAL、ADD SRROUTE、ADD SUBSCRIBERIDSEGGRP、ADD TARIFFGROUP、ADD TNFBINDGRP、ADD TNFGRP、ADD TNFINS、ADD TNFINSIP、ADD TSTPCFBINDING、ADD UPBINDUPG |
| 批次10 | 0-00091 ~ 0-00100 | ⏳ 待重构 |  | ADD UPFGBINDGXUPFGRP、ADD UPFGLOCGBNDGRP、ADD URR、ADD URRGROUP、ADD USERPROFILE、ADD USRLOCATION、ADD USRLOCATIONGRP、ADD USRPROBINDDNAI、ADD USRPROFGROUP、ADD USRTAIRANGE |
| 批次11 | 0-00101 ~ 0-00110 | ⏳ 待重构 |  | ADD VPNINST、ADD VPNINSTAF、ADD WEEKDAY、DEA SMCTX、DSP CGSTATUS、DSP CHGCDR、DSP CHGGA、DSP CPPDPCHGINFO、DSP PCCSESSINFO、DSP PCRFGRPSTATUS |
| 批次12 | 0-00111 ~ 0-00120 | ⏳ 待重构 |  | DSP PCRFSTATUS、DSP PDUSESSION、DSP SMPDPNUM、EXP MML、MOD AMUEPLCYCTRL、MOD APNCHGMODE、MOD CCT、MOD CHARGECHAR、MOD CHARGEMETHOD、MOD CHGAPN |
| 批次13 | 0-00121 ~ 0-00130 | ⏳ 待重构 |  | MOD CHGIMSICFG、MOD CHGPLMNCFG、MOD NFPROFILE、MOD PCCPOLICYGRP、MOD PCRF、MOD PCRFBINDGRP、MOD PDUSCACT、MOD PDUTRIGGER、MOD RGTRIGGER、MOD RULE |
| 批次14 | 0-00131 ~ 0-00140 | ⏳ 待重构 |  | MOD SELECTCHFGBYCC、MOD TNFBINDGRP、MOD TNFINS、MOD URR、MOD URRGROUP、MOD VPNINSTAF、RMV CHARGECHAR、RMV CHGAPN、RMV CHGDCHAR、RMV CHGIMSICFG |
| 批次15 | 0-00141 ~ 0-00150 | ⏳ 待重构 |  | RMV CHGPLMNCFG、RMV DIAMCONNECTION、RMV DIAMLOCINFO、RMV LOGICINF、RMV PCRF、RMV RULE、RMV RULEBINDING、RMV TSTPCFBINDING、RST PCCTIMER、SET AMFPEERSELFUNC |
| 批次16 | 0-00151 ~ 0-00160 | ⏳ 待重构 |  | SET AMFPLCYFUNC、SET APNCHARGECTRL、SET APNIDLETIME、SET APNPCCFUNC、SET APNREPORTATTR、SET CDRSTORAGECTRL、SET CDRSTRGSTATUS、SET CDRTRANSFER、SET CDRTRIGGER、SET CHARGECTRL |
| 批次17 | 0-00161 ~ 0-00170 | ⏳ 待重构 |  | SET CHFINIT、SET CHGCDR、SET CHGCHAR、SET CHGGA、SET CHGMODE、SET CHGPLMNCHAR、SET CHGWKDY、SET CNVRGDCHGPARA、SET CONCENPOINT、SET CONTAINERTRIGGER |
| 批次18 | 0-00171 ~ 0-00180 | ⏳ 待重构 |  | SET CTXSTARTRATING、SET DFTGLBPCRFGRP、SET DFTIDLETIME、SET FAILHANDLING、SET FHBYPASS、SET GLBCDRFLDTEMP、SET GLBCGGROUP、SET GLBCHARGECHAR、SET GLBDFTCHFGROUP、SET GLBTARIFFGROUP |
| 批次19 | 0-00181 ~ 0-00190 | ⏳ 待重构 |  | SET GLBURRGROUP、SET HTTPCONF、SET LICENSESWITCH、SET LTEMRATVALUE、SET MASTERPCRF、SET N40APIVER、SET N40MSGSTG、SET N40QUOTACTRL、SET N4CHGMSGCTRL、SET N7RCVATTRCTRL |
| 批次20 | 0-00191 ~ 0-00200 | ⏳ 待重构 |  | SET N7SNDATTRCTRL、SET NBIOTRATVALUE、SET OFCCDRPARA、SET OFCTHRESHOLD、SET PCCFAILACTION、SET PCCFUNC、SET PCCMSGATTR、SET PCCPCRFMSGATTR、SET PCCTIMER、SET POLICYMODE |
| 批次21 | 0-00201 ~ 0-00210 | ⏳ 待重构 |  | SET RGRESCTRL、SET SGWAPNCHGMETH、SET SGWCHARGECFG、SET SGWCHGMETH、SET SMCOMMFUNC、SET SMFSOFTPARA、SET STGALARMCTRL、SET STGTRIGGER、SET URRGRPBINDING、SET USRPROFCHARGE |
| 批次22 | 0-00211 ~ 0-00212 | ⏳ 待重构 |  | SET ZEROCHGSKIPSW、TST CHGCDR |
