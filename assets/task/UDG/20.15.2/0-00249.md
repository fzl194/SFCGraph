---
id: UDG@20.15.2@Task@0-00249
type: Task
task_layer: atom
task_logical_name: 设置用户面接口模式(入不转板总开关)
ref: UDG@20.15.2@MMLCommand@SET DATAPLANEINFMODE
task_intent: '开启入不转板总开关(SET DATAPLANEINFMODE): MODE=Instance 固定, ROUTEENHANCEDSW 控制路由增强功能(VNF+DC-GW 配合减少 DC-GW 对承载网发布的用户下行路由条数)。注意区分 SET DATAPLANEGIINFMODE(仅下行, 误用会导致上行入转板)。'
status: inferred
---

# 设置用户面接口模式(入不转板总开关)（SET DATAPLANEINFMODE）

> 命令静态知识见 [SET DATAPLANEINFMODE](command/UDG/20.15.2/SET-DATAPLANEINFMODE.md)。本页只讲配置生成实例化时这条命令**怎么配**。

## 配置方法

本任务是 GWFD-020482（激活入不转板功能）的**全局总开关**第 2 步——设置数据面接口模式为 Instance，开启入不转板（上下行均不转板）。ROUTEENHANCEDSW 控制路由增强功能，通过 VNF 与 DC-GW 配合减少 DC-GW 对承载网发布的用户下行路由条数。适用 NF：SGW-U、PGW-U、UPF。

**参数取值规划（数据规划表样例）**

| 参数 | 含义 | 取值样例 | 获取方法 / 取值规律 |
|---|---|---|---|
| MODE | 模式 | `Instance` | **固定取值**（开启入不转板必须用样例值 Instance，默认 Group 为不入不转板） |
| ROUTEENHANCEDSW | 路由增强功能开关 | `ENABLE` | 本端规划，枚举 ENABLE/DISABLE（本特性必填） |

**脚本样例**

```
SET DATAPLANEINFMODE:MODE=Instance,ROUTEENHANCEDSW=ENABLE;
```

**路由增强生效条件**：ROUTEENHANCEDSW=ENABLE **且** ADD AUTOSCALINGBGPLF 命令中的"发布的团体属性"（UDADVCOM）不为 0，二者同时满足才生效。UDADVCOM 属 BGP 自动化配置，不在本特性命令域。

**防误用（关键）**：开启入不转板必须用 **SET DATAPLANEINFMODE**（总开关，上下行均不转板）；不可误用 **SET DATAPLANEGIINFMODE**（仅下行业务入不转板，上行仍入转板）。

**操作步骤定位**：本命令为入不转板流程全局开关，之后执行配置逻辑接口（ADD LOGICINF）等后续步骤，并通过 DSP IPSUIT 查询每个 IP Suit 的逻辑接口绑定情况。

## 约束

- 本命令属于**高危命令**，操作不当会导致配置失效。
- 修改本命令前需要**删除所有用户**（前置约束）。
- 配置前需检查当前 **POD Type 是否为 ISU/APU POD**，否则不能开启入不转板（前置检查 1）。
- 配置前需检查 SET CPTEIDUALLOC 命令的 SWITCH 是否为 DISABLE，否则不能开启（前置检查 2）。[SET CPTEIDUALLOC](command/UDG/20.15.2/SET-CPTEIDUALLOC.md)
- 当有**逻辑接口绑定切片**时，不能开启入不转板功能（切片约束）。
- ROUTEENHANCEDSW 变更（DISABLE↔ENABLE）前需将用户下线。
- 命令最大记录数 **1**；命令执行后**立即生效**。
- MODE 固定 Instance（特性 md 数据规划表强约束）。

## 关联

- 命令 wiki：[SET DATAPLANEINFMODE](command/UDG/20.15.2/SET-DATAPLANEINFMODE.md)
- 配置对象：[DATAPLANEINFMODE](configobject/UDG/20.15.2/DATAPLANEINFMODE.md)
- 证据：[激活入不转板功能](evidence/UDG/20.15.2/激活入不转板功能_28210142.md)
- 后置：[设置地址分配规则(入不转板)](task/UDG/20.15.2/0-00250.md)（SET IPALLOCRULE，入不转板流程后续步骤）
