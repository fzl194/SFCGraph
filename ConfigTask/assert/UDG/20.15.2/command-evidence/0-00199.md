# 命令证据包：ADD MQCBEHAVIOR（atom 0-00199）
> 原始命令 md：`output/UDG_Product_Documentation_CH_20.15.2/OM参考/命令/UDG MML命令/平台服务管理/VNRS功能管理/IP服务/安全管理/MQC/分类行为/增加流行为（ADD MQCBEHAVIOR）_50281310.md`
> atom status：inferred | 用该命令的特性数：1

## ① atom + atom-挂 rule/DP（已抽 yaml）
```yaml
task_id: UDG@20.15.2@Task@0-00199
nf: UDG
version: 20.15.2
task_layer: atom
task_logical_name: 增加流行为(MQC)
task_intent: '增加MQC流行为对象(QoS复杂流分类三件套之一: 流策略/流分类/流行为 CB对)。流动作通过 ADD QOSACTRDRNHP/ADD
  SQOSREMARK/SET QOSACTFILTER 等挂载'
task_category: configure
ref: UDG@20.15.2@MMLCommand@ADD MQCBEHAVIOR
status: inferred
source_evidence_ids:
- IPFD-012001/激活QoS复杂流分类功能（VNRS_VNFC）_13119479.md
parameter_bindings:
- binding_id: UDG@20.15.2@Task@0-00199:BEHAVIORNAME
  parameter_ref: UDG@20.15.2@CommandParameter@ADD MQCBEHAVIOR:BEHAVIORNAME
  binding_type: variable
  variable_source: local_planned
  requiredness: required
  source_evidence_ids:
  - IPFD-012001/激活QoS复杂流分类功能（VNRS_VNFC）_13119479.md
notes: '流行为(MQCBEHAVIOR)是CB对的B侧。流动作 permit 可与其他共存，其余互斥(重定向/重标记/deny/级联/URPF/CAR)。CommandParameter
  待抽取。命令级审查(2026-07-02)：bound=1/1 与命令真相一致, 0 缺口。MQCBEHAVIOR 为空行为容器, 仅 BEHAVIORNAME
  单参, 命令无内嵌动作分叉；流动作通过 ADD QOSACTRDRNHP/ADD SQOSREMARK/SET QOSACTFILTER 等子命令挂载(后续 IPFD-012001
  子 atom 待审)。rule 0-00357 投影 cardinality 8192 + 立即生效 + 前置 ADD MQCCLASSIFIER (MQC 三件套:
  classifier/behavior/policy, B 侧须在 C/P 侧绑定前先建)。'
```
**rule 0-00357** (dependency_rule) 0-00199 ADD MQCBEHAVIOR cardinality 8192 + 立即生效 + 前置ADD MQCCLASSIFIER + 流动作互斥挂载
```yaml
rule_id: UDG@20.15.2@TaskRule@0-00357
owner_task_ref: UDG@20.15.2@Task@0-00199
rule_name: 0-00199 ADD MQCBEHAVIOR cardinality 8192 + 立即生效 + 前置ADD MQCCLASSIFIER +
  流动作互斥挂载
rule_type: dependency_rule
rule_scope: atom
condition:
  expression_type: always
  expression: ''
constraint:
  constraint_type: requires
  target_refs:
  - UDG@20.15.2@Task@0-00199
  expression: 'ADD MQCBEHAVIOR(atom 0-00199) 配置约束：

    (1) 上限：单命令最大记录数为 8192(命令 notes 强约束)；超限→ADD 失败。

    (2) 立即生效：该命令执行后立即生效(命令 notes)。

    (3) 空行为容器：本命令仅含 BEHAVIORNAME 单参——MQCBEHAVIOR 为空行为容器, 流动作(permit/redirect/remark/deny/cascade/URPF/CAR)
    全部通过后续 ADD/SET 子命令挂载。

    (4) 流动作互斥：permit 可与其他共存(deny 除外), 其余动作(重定向/重标记/deny/级联/URPF/CAR) 互斥(特性 md IPFD-012001
    注)；同一 BEHAVIORNAME 下多个动作冲突时, 最后执行的动作生效或命令拒收。

    (5) 前置依赖：典型路径下, MQC 三件套(流分类 ADD MQCCLASSIFIER + 流行为 ADD MQCBEHAVIOR + 流策略 ADD
    MQCPOLICY) 中, 流行为 B 侧须在 C 侧绑定前先建(不在本 atom 命令域, 由 feature task dependency_rule
    编排)。

    (6) 流动作挂载子命令：permit/deny 通过 SET QOSACTFILTER；重定向通过 ADD QOSACTRDRNHP；重标记通过 ADD
    SQOSREMARK；级联通过 ADD MQCCLASSIFIER 引用；URPF 通过 SET QOSACTFILTER 配套；CAR 通过 ADD QOSACTCAR——后续
    IPFD-012001 子 atom 待审(不在 C6b 范围)。

    '
violation_effect: 上限超限→ADD 失败；流动作互斥错配→策略行为不符合预期；未配套 MQCCLASSIFIER→流策略无法引用 B 侧
severity: warning
rule_expression_mode: inferred
rule_source_kind: command
status: inferred
source_evidence_ids:
- IPFD-012001/激活QoS复杂流分类功能（VNRS_VNFC）_13119479.md
notes: 命令级审查(2026-07-02)新增 rule。属 atom-level dependency_rule。投影命令 notes 2 大约束(cardinality
  8192/立即生效) + 流动作挂载子命令注 + MQC 三件套前置(ADD MQCCLASSIFIER)。流动作(redirect/remark/deny/cascade/URPF/CAR)
  子命令后续 IPFD-012001 子 atom 待审。
```
## ② 命令真相（mml_commands.jsonl）
**功能**：该命令用于创建流行为；在复杂流分类的QoS策略中，如果需要对某类流执行相同的流行为，则需要用该命令创建流行为并在流行为中配置流动作。
**notes（规格/上限→应投影 atom rule）**：
- - 该命令执行后立即生效。
- 该命令最大记录数为8192。

**参数真相表（代码解析）**：

| 参数 | 描述 | 数据来源 | 必选 | 默认 | 取值范围 |
|---|---|---|---|---|---|
| BEHAVIORNAME | 行为名称 | local_planned | required | 无 | 字符串类型，输入长度范围为1～31。 |

## ③ 各特性的配置范式（代码从激活 md 抽取 + 原始上下文）

### IPFD-012001

**md：`IPFD-012001/激活QoS复杂流分类功能（VNRS_VNFC）_13119479.md`**
- 任务示例脚本（该命令行）：
  `ADD MQCBEHAVIOR: BEHAVIORNAME="behavior1";`
- 操作步骤上下文（±2 行原文）：
  L107:
    > 2. 增加流行为。
    >   在 “MML命令行-UDG” 窗口上执行：
    >   [**ADD MQCBEHAVIOR**](../../../../../../../../OM参考/命令/UDG MML命令/平台服务管理/VNRS功能管理/IP服务/安全管理/MQC/分类行为/增加流行为（ADD MQCBEHAVIOR）_50281310.md) :BEHAVIORNAME="行为名称";
    > 3. 增加对应的流动作。
    >   在 “MML命令行-UDG” 窗口上执行：
  L197:
    > 
    > ```
    > ADD MQCBEHAVIOR: BEHAVIORNAME="behavior1";
    > ```
    > 

## ④ 自动比对
- atom 已绑参数：['BEHAVIORNAME']
- 命令真相参数：['BEHAVIORNAME']
- 缺口（真相有、atom 未绑）：[]
