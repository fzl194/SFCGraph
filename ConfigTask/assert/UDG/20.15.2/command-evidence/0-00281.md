# 命令证据包：ADD FINGERIDENT（atom 0-00281）
> 原始命令 md：`output/UDG_Product_Documentation_CH_20.15.2/OM参考/命令/UDG MML命令/用户面服务管理/协议识别/SA指纹识别/SA指纹识别/增加SA指纹识别配置（ADD FINGERIDENT）_86851945.md`
> atom status：inferred | 用该命令的特性数：1

## ① atom + atom-挂 rule/DP（已抽 yaml）
```yaml
task_id: UDG@20.15.2@Task@0-00281
nf: UDG
version: 20.15.2
task_logical_name: 配置SA指纹识别
task_layer: atom
task_intent: 开启HTTPS业务行为识别功能(ADD FINGERIDENT)，对HTTPS业务报文做TLS指纹识别提升未携带SNI场景的业务识别精度
task_category: configure
ref: UDG@20.15.2@MMLCommand@ADD FINGERIDENT
status: inferred
source_evidence_ids:
- GWFD-110404/激活HTTPS业务行为识别特性_56038531.md
parameter_bindings:
- binding_id: UDG@20.15.2@Task@0-00281:CFGPROTMODE
  parameter_ref: UDG@20.15.2@CommandParameter@ADD FINGERIDENT:CFGPROTMODE
  binding_type: decision_driven
  variable_source: local_planned
  requiredness: required
  decision_ref: UDG@20.15.2@DecisionPoint@0-00042
  source_evidence_ids:
  - GWFD-110404/激活HTTPS业务行为识别特性_56038531.md
- binding_id: UDG@20.15.2@Task@0-00281:PROTOCOLNAME
  parameter_ref: UDG@20.15.2@CommandParameter@ADD FINGERIDENT:PROTOCOLNAME
  binding_type: variable
  variable_source: local_planned
  requiredness: conditional
  decision_ref: UDG@20.15.2@DecisionPoint@0-00042
  source_evidence_ids:
  - GWFD-110404/激活HTTPS业务行为识别特性_56038531.md
- binding_id: UDG@20.15.2@Task@0-00281:FINGERIDENTCTRL
  parameter_ref: UDG@20.15.2@CommandParameter@ADD FINGERIDENT:FINGERIDENTCTRL
  binding_type: variable
  variable_source: local_planned
  requiredness: optional
  source_evidence_ids:
  - GWFD-110404/激活HTTPS业务行为识别特性_56038531.md
- binding_id: UDG@20.15.2@Task@0-00281:CFGDOMAINNAME
  parameter_ref: UDG@20.15.2@CommandParameter@ADD FINGERIDENT:CFGDOMAINNAME
  binding_type: variable
  variable_source: local_planned
  requiredness: conditional
  source_evidence_ids:
  - GWFD-110404/激活HTTPS业务行为识别特性_56038531.md
notes: 'GWFD-110404 HTTPS业务行为识别专属。命令 ADD FINGERIDENT 在 mml_commands.jsonl，parameter_references
  为空(§6-B)，参数从 parameter_description 文本抽取。

  特性 md 样例 CFGPROTMODE=ALL(全协议指纹识别)，PROTOCOLNAME 未列(SPECIFIC 路径)。CFGPROTMODE 取 ALL/SPECIFIC
  的协议配置方式分叉由 DP 0-00042 表达。

  命令级审查(2026-07-02)：4/4 参数绑定完整；修正 binding_type 自相矛盾(CFGPROTMODE 改 decision_driven
  由 feature-挂 DP 0-00042 驱动)；去除 FINGERIDENTCTRL 的 value 字段(binding_type variable 不应附
  value)校正 requiredness 为 optional(命令真相默认 DISABLE)；PROTOCOLNAME 增加 decision_ref(由
  DP 0-00042 编排 requiredness 切换)。新增 atom rule 0-00397(cardinality 128 + 立即生效 + ALL/SPECIFIC
  同时配置时 SPECIFIC 优先 + 指纹识别只增强不替代特征库)。LOD SIGNATUREDB 前置依赖(特征库)由 feature-级/特性编排表达。

  '
```
**rule 0-00397** (cardinality_rule) ADD FINGERIDENT 规格 128 + 立即生效 + ALL/SPECIFIC SPECIFIC 优先 + 指纹识别只增强不替代
```yaml
rule_id: UDG@20.15.2@TaskRule@0-00397
owner_task_ref: UDG@20.15.2@Task@0-00281
rule_name: ADD FINGERIDENT 规格 128 + 立即生效 + ALL/SPECIFIC SPECIFIC 优先 + 指纹识别只增强不替代
rule_type: cardinality_rule
rule_scope: command
condition:
  expression_type: none
  expression: ''
constraint:
  constraint_type: max_records
  target_refs:
  - UDG@20.15.2@MMLCommand@ADD FINGERIDENT
  expression: 'ADD FINGERIDENT 命令最大记录数为 128（命令 notes 强约束），超出被拒。

    该命令执行后立即生效（命令 notes）。

    SA 指纹识别功能开启后，如果用户访问开启该功能的业务，系统会在正常的特征库识别之外，对业务做更进一步的指纹识别，以增强识别精度（命令 notes 功能定位）。

    可以通过 FINGERIDENTCTRL 参数控制指纹识别结果只用于统计，不用于业务控制。如果同时配置了指定协议和所有协议的 FINGERIDENTCTRL
    参数，则优先使用指定协议的配置（命令 notes SPECIFIC 优先）。

    并不是所有特征库都能被指纹识别（命令 notes 范围限制）。

    LOD SIGNATUREDB（加载协议特征库文件）前置依赖：执行本命令前需先加载特征库文件（命令 notes 说明；前置命令不在本 atom）。

    CFGPROTMODE 跨特性分叉（ALL/SPECIFIC）由 feature-挂 DP 0-00042 驱动。

    PROTOCOLNAME requiredness 切换（ALL=排除/SPECIFIC=必填）由 DP 0-00042 编排。

    '
violation_effect: 上限超限→ADD 失败；FINGERIDENTCTRL 同时配置 ALL+SPECIFIC→未按 SPECIFIC 优先；特征库未加载→命令拒收
severity: warning
rule_expression_mode: explicit
rule_source_kind: config
status: inferred
source_evidence_ids:
- GWFD-110404/激活HTTPS业务行为识别特性_56038531.md
notes: 命令级审查(2026-07-02) 新建。投影自命令 notes 全集：规格 128 + 立即生效 + SA 指纹识别只增强不替代特征库 + FINGERIDENTCTRL
  同时配置 ALL+SPECIFIC 时 SPECIFIC 优先。CFGPROTMODE 跨特性 ALL/SPECIFIC 分叉由 feature-挂 DP 0-00042（指纹识别协议范围）驱动；本
  atom 已将 CFGPROTMODE 改为 decision_driven（decision_ref=0-00042），PROTOCOLNAME 加 decision_ref
  同源驱动 requiredness 切换。LOD SIGNATUREDB 前置依赖由 task_relations propagated_context 表达。
```
## ② 命令真相（mml_commands.jsonl）
**功能**：**适用NF：PGW-U、UPF**

该命令用来开启SA指纹识别功能。
**notes（规格/上限→应投影 atom rule）**：
- - 该命令执行后立即生效。
- 该命令最大记录数为128。
- SA指纹识别功能开启后，如果用户访问开启该功能的业务，系统会在正常的特征库识别之外，对业务做更进一步的指纹识别，以增强识别精度。
- 可以通过FingerIdentCtrl参数控制指纹识别结果只用于统计，不用于业务控制。如果同时配置了指定协议和所有协议的FingerIdentCtrl参数，则优先使用指定协议的配置。
- 并不是所有特征

**参数真相表（代码解析）**：

| 参数 | 描述 | 数据来源 | 必选 | 默认 | 取值范围 |
|---|---|---|---|---|---|
| CFGPROTMODE | 协议配置方式 | local_planned | required | 无 | <br>- ALL：所有协议。 |
| PROTOCOLNAME | 协议名称 | local_planned | conditional | 无 | 不支持空格，不区分大小写。 |
| FINGERIDENTCTRL | 指纹识别结果控制开关 | local_planned | optional | DISABLE | <br>- DISABLE：SA指纹识别结果既用于统计，又参与业务控制。 |
| CFGDOMAINNAME | 配置域名称 | local_planned | conditional | 无 | 字符串类型，输入长度范围为1～31。 |

## ③ 各特性的配置范式（代码从激活 md 抽取 + 原始上下文）

### GWFD-110404

**md：`GWFD-110404/激活HTTPS业务行为识别特性_56038531.md`**
- 数据规划表（该命令的参数行）：
  | **[ADD FINGERIDENT](../../../../../OM参考/命令/UDG MML命令/用户面服务管理/协议识别/SA指纹识别/SA指纹识别/增加SA指纹识别配置（ADD FINGERIDENT）_86851945.md)** | 协议配置方式<br>（CFGPROTMODE） | ALL | 本端规划 | 该参数用于设置SA指纹识别功能协议的配置方式。<br>- ALL：所有协议。<br>- SPECIFIC：指定协议。 |
- 任务示例脚本（该命令行）：
  `ADD FINGERIDENT:CFGPROTMODE=ALL;`
- 操作步骤上下文（±2 行原文）：
  L38:
    >   [**LOD SIGNATUREDB**](../../../../../OM参考/命令/UDG MML命令/用户面服务管理/协议识别/特征库管理/识别特征库/加载协议特征库文件（LOD SIGNATUREDB）_82837716.md)
    > 3. 开启HTTPS业务行为识别功能。
    >   [**ADD FINGERIDENT**](../../../../../OM参考/命令/UDG MML命令/用户面服务管理/协议识别/SA指纹识别/SA指纹识别/增加SA指纹识别配置（ADD FINGERIDENT）_86851945.md)
    >   > **说明**
    >   > 可通过 [**LST SUPPFINGERIDEN**](../../../../../OM参考/命令/UDG MML命令/用户面服务管理/协议识别/SA指纹识别/SA指纹识别/查询SA指纹识别配置（LST FINGERIDENT）_86723149.md) 命令查询系统内哪些协议支持指纹识别功能。
  L65:
    > 
    > ```
    > ADD FINGERIDENT:CFGPROTMODE=ALL;
    > ```

## ④ 自动比对
- atom 已绑参数：['CFGDOMAINNAME', 'CFGPROTMODE', 'FINGERIDENTCTRL', 'PROTOCOLNAME']
- 命令真相参数：['CFGDOMAINNAME', 'CFGPROTMODE', 'FINGERIDENTCTRL', 'PROTOCOLNAME']
- 缺口（真相有、atom 未绑）：[]
- 各特性数据规划表"获取方法"列分布：{'本端规划': 1}（多值→atom 应考虑 decision_driven）
