rule_id: UDG@20.15.2@TaskRule@0-00400
owner_task_ref: UDG@20.15.2@Task@0-00031   # 挂 atom ADD ADCPARA
rule_name: ADD ADCPARA 最大记录数规格
rule_type: cardinality_rule
rule_scope: parameter
condition: {expression_type: none, expression: ""}
constraint:
  constraint_type: max_records
  target_refs:
    - UDG@20.15.2@MMLCommand@ADD ADCPARA
  expression: "ADD ADCPARA 命令最大记录数为 8000；超过规格新增失败。命令执行后立即生效。"
violation_effect: 超规格新增失败
severity: warning
rule_expression_mode: explicit
rule_source_kind: config
status: inferred
source_evidence_ids: [GWFD-020357/激活增强的ADC基本功能_84866820.md]
notes: "命令级审查(2026-07-02) 补审新建。投影自命令 notes '该命令最大记录数为8000'+'执行后立即生效'。ADD ADCPARA 仅 GWFD-020357 ADC Event-Trigger 场景使用(每 FLOWFILTER 一条 ADC 参数)。"
