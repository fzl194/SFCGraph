# 命令证据包：ADD CHGHOLI
> 原始命令 md：`output/UNC 20.15.2 产品文档(裸机容器) 05/OM参考/命令/UNC MML命令/业务服务管理/Pre 5G接入业务管理/计费管理/计费节假日配置/增加节假日配置(ADD CHGHOLI)_26145382.md`
> 用该命令的特性数：1

## ② 命令真相（mml_commands.jsonl）
**功能**：**适用网元：SGSN**

该命令用于配置普通计费属性用户、预付费计费属性用户、包月制计费属性用户或实时计费属性用户的节假日。该命令的配置是为了实现对S-CDR话单进行不同费率时段的计费。该命令与费率时段配置（ [**ADD CHGTARI**](../计费费率时段配置/增加费率时段配置(ADD CHGTARI)_26305208.md) ）相结合，进行灵活的费率时段控制。
**notes（规格/上限→应投影 atom rule）**：
- - 该命令执行后立即生效。
- 同一计费属性的节假日总数不能超过50，本表记录总数不能超过200。
- 在节假日配置表中，“年”、“月”、“日”和“计费属性”唯一确定一条记录。

**参数真相表（代码解析）**：

| 参数 | 描述 | 数据来源 | 必选 | 默认 | 取值范围 |
|---|---|---|---|---|---|
| YEAR | 年 | 整网规划 | required | 无 | 1998～2099 |
| MONTH | 月 | 整网规划 | required | 无 | 1～12 |
| DAY | 日 | 整网规划 | required | 无 | 1～31 |
| CC | 计费属性 | 整网规划 | required | 无 | <br>- “NORMAL(普通计费)” |

## ③ 各特性的配置范式（代码从激活 md 抽取 + 原始上下文）

### WSFD-011201

**md：`WSFD-011201/配置离线计费费率切换（SGSN）_01731138.md`**
- 数据规划表（该命令的参数行）：
  | **ADD CHGHOLI** | 年（YEAR） | 2010 | 全网规划 | 增加节假日配置。 |
  | **ADD CHGHOLI** | 月（MONTH） | 10 | 全网规划 | 增加节假日配置。 |
  | **ADD CHGHOLI** | 日（DAY） | 1 | 全网规划 | 增加节假日配置。 |
  | **ADD CHGHOLI** | 计费属性（CC） | NORMAL | 全网规划 | 增加节假日配置。 |
- 任务示例脚本（该命令行）：
  `ADD CHGHOLI: YEAR=2010, MONTH=10, DAY=1, CC=NORMAL;`
- 操作步骤上下文（±2 行原文）：
  L58:
    > 1. 进入 “MML命令行-UNC” 窗口。
    > 2. **可选：**增加节假日配置。
    >   **ADD CHGHOLI**
    > 3. **可选：**设置星期配置。
    >   **SET CHGWKDY**
  L75:
    > 
    > ```
    > ADD CHGHOLI: YEAR=2010, MONTH=10, DAY=1, CC=NORMAL;
    > ```
    > 

## ④ 自动比对
- 命令真相参数（4）：['CC', 'DAY', 'MONTH', 'YEAR']
- atom 未建（pre-build 模式）；建 atom 时按真相 + ③ 范式决定绑定集
- 各特性数据规划表"获取方法"列分布：{'全网规划': 4}（多值→atom 应考虑 decision_driven）
