# 命令证据包：SET STGALARMCTRL
> 原始命令 md：`output/UNC 20.15.2 产品文档(裸机容器) 05/OM参考/命令/UNC MML命令/业务服务管理/会话管理/计费管理/计费缓存/缓存控制/设置融合计费话单缓存告警上报的控制参数（SET STGALARMCTRL）_88248962.md`
> 用该命令的特性数：1

## ② 命令真相（mml_commands.jsonl）
**功能**：**适用NF：SMF、PGW-C**

该命令可通过配置告警产生门限、告警监控时长，对融合计费话单缓存告警（ALM-81025）的上报方式进行控制。
**notes（规格/上限→应投影 atom rule）**：
- - 该命令执行后立即生效。

- SMF定期对硬盘上的融合计费消息缓存文件进行检查，检查周期受SMF软参DWORD507控制。

- 系统部署完成后，已经存在初始记录，参数的初始记录值如下表：

| ALMTHD | MONITORTIME |
| --- | --- |
| 5120 | 60 |

**参数真相表（代码解析）**：

| 参数 | 描述 | 数据来源 | 必选 | 默认 | 取值范围 |
|---|---|---|---|---|---|
| ALMTHD | 告警产生门限(KB) | local_planned | optional | 无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST STGA | 整数类型，取值范围是0~1048576，单位是千字节。 |
| MONITORTIME | 告警监控时长(分) | local_planned | optional | 无。执行命令并不输入该参数时，该参数保持系统当前配置不变，可通过LST STGA | 整数类型，取值范围是0~65535，单位是分钟。 |

## ③ 各特性的配置范式（代码从激活 md 抽取 + 原始上下文）

### WSFD-011206

**md：`WSFD-011206/配置计费消息缓存_31702748.md`**
- 数据规划表（该命令的参数行）：
  | ****SET STGALARMCTRL**** | 告警产生门限（ALMTHD） | 5120 | 本端规划 | 配置告警产生门限。 |
  | ****SET STGALARMCTRL**** | 告警监控时长（MONITORTIME） | 60 | 本端规划 | 配置告警监控时长。 |
- 任务示例脚本（该命令行）：
  `SET STGALARMCTRL: ALMTHD=5120, MONITORTIME=60;`
- 操作步骤上下文（±2 行原文）：
  L69:
    >   **SET CDRSTORAGECTRL**
    > - 配置融合计费话单缓存告警上报的控制参数。
    >   ****SET STGALARMCTRL****
    >   > **说明**
    >   > - **SET STGALARMCTRL**命令的“ALMTHD”参数控制SMF上报**“ALM-81025 话单缓存”**告警的缓存文件占用空间阈值。缓存文件超过该阈值时，上报告警；缓存文件全部被回放后，清除告警。
  L71:
    >   ****SET STGALARMCTRL****
    >   > **说明**
    >   > - **SET STGALARMCTRL**命令的“ALMTHD”参数控制SMF上报**“ALM-81025 话单缓存”**告警的缓存文件占用空间阈值。缓存文件超过该阈值时，上报告警；缓存文件全部被回放后，清除告警。
    >   > - **SET STGALARMCTRL**命令的“MONITORTIME”参数控制告警监控时长，当SMF检查到缓存文件存在且不超过占用空间阈值时，超过告警监控时长后，SMF上报**“ALM-81025 话单缓存”**告警。建议该参数配置为**SET N40MSGSTG**命令配置的融合计费消息回放的最小间隔的2倍以上。
    >   >   如果 “MONITORTIME” 参数配置为0，则告警监控时长受软参 **DWORD507** 控制，该软参用于控制扫描硬盘上缓存文件的周期。
  L72:
    >   > **说明**
    >   > - **SET STGALARMCTRL**命令的“ALMTHD”参数控制SMF上报**“ALM-81025 话单缓存”**告警的缓存文件占用空间阈值。缓存文件超过该阈值时，上报告警；缓存文件全部被回放后，清除告警。
    >   > - **SET STGALARMCTRL**命令的“MONITORTIME”参数控制告警监控时长，当SMF检查到缓存文件存在且不超过占用空间阈值时，超过告警监控时长后，SMF上报**“ALM-81025 话单缓存”**告警。建议该参数配置为**SET N40MSGSTG**命令配置的融合计费消息回放的最小间隔的2倍以上。
    >   >   如果 “MONITORTIME” 参数配置为0，则告警监控时长受软参 **DWORD507** 控制，该软参用于控制扫描硬盘上缓存文件的周期。
    > - 配置N4接口计费消息相关控制参数。当阈值配置过小，比如流量阈值较低，会导致N4接口频繁上报阈值到，当CHF性能不足响应较慢时，会导致N4接口上报消息堆积，超过最大可缓存的消息规格，用户被去活。该命令主要用于配置SMF在突发和持续高带宽下载场景，用户不被去活。

## ④ 自动比对
- 命令真相参数（2）：['ALMTHD', 'MONITORTIME']
- atom 未建（pre-build 模式）；建 atom 时按真相 + ③ 范式决定绑定集
- 各特性数据规划表"获取方法"列分布：{'本端规划': 2}（多值→atom 应考虑 decision_driven）
